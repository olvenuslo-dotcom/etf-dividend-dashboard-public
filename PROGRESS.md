# 진행 기록 (PROGRESS)

## 1단계 MVP — 수집 + 분류 (2026-07-26 완료)

### 한 일
1. **환경**: `venv` 생성, `requirements.txt`(requests·pandas·openpyxl·holidays) 설치
2. **수집** (`src/collect.py`)
   - 세이브로 분배금지급현황 내부 요청(`callServletService.jsp`)을 파이썬으로 재현
   - 방식: 세션 쿠키 확보(페이지 GET) → Referer 헤더 붙여 POST → 30행씩 페이징
   - 최근 12개월(20250726~20260726) **3,334건** → `data/raw/seibro_분배금_20260726.csv`
3. **분류** (`src/classify.py`) → `output/분배ETF목록.xlsx` (시트: 전체 / 월·위클리)

### 결과 요약
- 청산분배 46건 제외 → 이익분배 3,288건 / **955개 종목**
- 분배주기별 종목 수:
  | 위클리 | 격주 | 월 | 분기 | 반기 | 연 |
  |---|---|---|---|---|---|
  | 0 | 0 | 164 | 195 | 67 | 529 |
- 월·위클리 시트: **164개**
- 비고(확인필요) 플래그: 65개

### ⭐ 핵심 발견 (이름 아닌 데이터로 판단한 결과)
- 이름에 **"위클리"**가 들어간 커버드콜 ETF들(예: `KODEX 200타겟위클리커버드콜`)은
  실제로는 **월 1회(연 12회) 분배**한다. → "위클리"는 분배주기가 아니라
  **커버드콜 옵션 전략** 이름. 데이터값 기준으로 전부 **월**로 분류됨.
- 즉 이 데이터에는 **실제 주간분배 ETF가 없다**(최대 빈도 = 월).
  → CLAUDE.md의 "이름으로 추정 금지" 규칙이 정확히 이 함정을 걸러냄.

### 판정 기준 (기록용)
- 분배주기 = 최근 12개월 **지급 횟수**: 40+위클리 / 18–39격주 / 7–17월 / 3–6분기 / 2반기 / 1연
- 교차검증: 지급 **간격 중앙값(일)** 로도 판정해 횟수 기준과 다르면 비고에 `확인필요(간격기준=X)`
  (신규 상장이라 횟수가 적은 월배당 종목 등을 잡아냄 — 65건)
- 국내/해외 = `유형` 데이터값에 "해외" 포함 여부 (이름 아님)
- 매수마감일 = 지급기준일의 2영업일 전 (주말 + 한국 공휴일 제외, `holidays` 사용)

### 스펙 대비 추가/조정한 점 (확인 요망)
- 스펙의 5개 주기 외에 **격주** 구간을 추가(데이터에 존재 가능성 대비, 이번엔 0개)
- 컬럼 추가: `국내해외`, `지급횟수_12개월`, `중앙값간격_일`, `최근_매수마감일`, `비고`, `ISIN`
- 시트2 `월·위클리`는 스펙대로 월+위클리만 (격주 미포함)

### 다음 단계 후보
- 매수마감일 달력 뷰 / NAV 침식 분석 / Streamlit 대시보드
- 비고 65건 개별 검토(신규상장 vs 실제 불규칙) 후 주기 확정 로직 보강

---

## 2단계 — 매수마감일/배당락일 캘린더(.ics) 생성 (2026-07-28 완료)

### 한 일
- `src/calendar_ics.py` 신규 → `output/분배캘린더.ics` 생성 (구글/아웃룩 구독용)
- 입력: 1단계 `output/분배ETF목록.xlsx`(전체 시트) / 청산분배는 1단계에서 이미 제외
- 영업일: **exchange_calendars XKRX**(한국거래소 개장일) 사용 → `requirements.txt`에 추가
  - `holidays`(KR)는 근로자의날(5/1)·연말 폐장일(12/31)을 놓쳐 배당락·매수마감이 틀릴 수 있어 배제
- 계산: 배당락일 = 지급기준일 1영업일 전 / 매수마감일 = 2영업일 전
- 범위: 오늘(2026-07-26)~향후 6개월. 미래 지급기준일은 '최근 기준일 + 분배주기'로 추정 → 예상 기준일을 개장일로 스냅. 이벤트에 (예상) 명시

### 결과
- 이벤트 종목 **776개** / 총 이벤트 **3,582개**(매수마감 1,791 + 배당락 1,791)
- 이벤트 1개 = 하루짜리(all-day), 제목 `[매수마감] 종목명` / `[배당락] 종목명`
- description: 운용사·분배주기·예상 지급기준일·예상 지급일·비고

### 검증 (통과)
- XKRX 날짜 손계산 대조: 2026-06-30→배당락 6/29·매수마감 6/26 등 정확
- 경계 케이스: 연말 폐장일(12/31)·근로자의날(5/1)·추석 2026(대체공휴일 없음, 9/28 개장) 정확 반영
- .ics 구조 검증: VCALENDAR 1/1, VEVENT 3582/3582, UID 전부 유일, 줄 폴딩(UTF-8)·CRLF 정상, icalendar 파싱 성공
- 버그 수정 이력: 격주 카테고리 제거 / previous_session 스냅 / 비고 nan 제거 / 윈도우 \r\r\n(CRLF 중복) 방지(newline="")

### 분배주기 카테고리 (격주 제거 확정)
- 위클리 / 월 / 분기 / 반기 / 연 (5종). 데이터상 최대 지급 13회라 위클리·격주 모두 0개

### 다음 단계 후보
- 실제 공시일로 예상일 보정

---

## 3단계-A — NAV 침식 분석 (2026-07-30 완료)

### 한 일
- `src/nav_erosion.py` 신규: KRX 종가·NAV·괴리율 조회, 기간별 CSV 캐시, 세이브로 분배금 결합
- `src/dashboard.py`의 NAV 탭 구현: 종목·기간 선택, 새로고침, 지표 5개, 정규화 차트, 분배 이력
- 지표: NAV 변화율, 누적 분배금, 시작 NAV 대비 분배율, 총수익률 대용치, NAV 최대낙폭
- 캐시: `data/nav/{종목코드}_{시작일}_{종료일}.csv`

### 데이터 정확성 수정
- 세이브로 응답의 `BUNBE`는 분배율, `ESTM_STDPRC`는 실제 주당분배금임을 원본 값으로 확인
- 이전 수집본의 잘못된 컬럼명(`주당분배금`/`결산과표기준가`)을 기존·신규 모두 호환하도록 수정
- `output/분배ETF목록.xlsx` 재생성: 955개 종목, 월·위클리 164개 유지
- 신형 영숫자 KRX 코드(예: `0004G0`)를 ISIN에서 정확히 추출하도록 지원

### 검증
- 자동 테스트 41개 통과, Python 문법 검사 통과
- 실제 KRX `152100` 1년치 244거래일 조회 및 캐시 성공
- PLUS 200 누적 분배금 940원, 분배율 2.10%, 총수익률 대용치 115.33% 계산 확인
- Streamlit AppTest에서 NAV 분석 컨트롤 렌더링 통과

### 환경 동기화·재검증 (2026-07-31)
- 이 venv에 전이 의존성 누락 발견·해결: `pyparsing`(pykrx→matplotlib), `pygments`(pytest)
  → `pip install -r requirements.txt`로 동기화, `pyparsing==3.3.2`를 requirements에 명시
- 재검증: pytest 41개 통과 / NAV 파이프라인 스모크(1Q 미국배당TOP30, KRX 243행 조회) /
  Streamlit AppTest 3탭 렌더 오류 0 / 헤드리스 health 200
- ~~`from src.nav_erosion` import가 streamlit 런타임에서 정상(AppTest로 확인) — 경로 방어코드 불필요~~
  → **2026-08-02 정정: 틀린 결론이었음.** AppTest는 pytest 아래서 도니 CWD가 sys.path에
  들어가 있어 통과한 것. 실제 `streamlit run` 은 CWD가 아니라 **스크립트 폴더(src)** 를 넣어서
  `ModuleNotFoundError: No module named 'src'` 로 죽는다. 4단계에서 경로 방어코드 추가함
- **산출물 재생성**: 매핑 수정 반영 위해 `분배ETF목록.xlsx` 재생성
  (최근_주당분배금이 옛 분배율 값 → 실제 금액(원)으로 교정, 예: 1Q 미국배당TOP30 0.2→20.0),
  `분배캘린더.ics`도 2026-07-31 기준으로 재생성

### 해석 주의
- 총수익률 대용치는 `(종료 NAV - 시작 NAV + 누적 분배금) / 시작 NAV`
- 분배금 재투자·세금·보수·거래비용을 반영한 공식 총수익률은 아님

---

## 3단계-B — Streamlit 대시보드 (2026-07-30 완료)

### 한 일
- `src/dashboard.py` 신규. `requirements.txt`에 streamlit·plotly·icalendar 추가
- 입력: `output/분배ETF목록.xlsx` + `output/분배캘린더.ics` (새 수집 없음)
- 구성: 상단 KPI(종목수·월위클리·비고, 분배주기 막대·국내해외 도넛) / 사이드바 필터
  (분배주기·국내해외·운용사·종목검색·비고만보기) / 3탭(종목목록·다가오는일정·NAV침식)
- **NAV 침식 탭 구현 완료** — KRX NAV 조회·캐시와 세이브로 이익분배 결합
- 실행: `venv\Scripts\streamlit run src\dashboard.py` (README 4단계에 추가)

### 검증
- 데이터 로드 함수 단독 테스트: 955종목 / 캘린더 3,582건 정상 파싱
- 헤드리스 구동: health 200 ok, 메인 HTTP 200, 로그에 예외 없음

### 설계 의도 (사용자 요청)
- "A(NAV 침식)가 있는 B(대시보드)"가 의미 있음 → B를 먼저 만들되 A 자리를 마련.
  로드·파싱·NAV 섹션을 함수로 분리해 A를 함수 하나로 붙일 수 있게 함

### 다음 단계 후보
- 3단계-A: NAV 시계열 수집 → NAV 침식 탭 구현 (총수익률 vs 분배율 등)

---

## 4단계 — 대시보드 사용성 개선 14건 (2026-08-02 완료)

직전 세션이 중간에 끊겨 요청만 남아 있던 건을 이어서 처리. 12·13번은 입력 대기열에
있어 화면에 안 보였고, 세션 기록(jsonl)에서 복구함.

### 고친 버그
- **필터가 안 먹는 것처럼 보임**: 상단 KPI·차트가 필터 전 `df` 로 그려져 있었다.
  → 필터 결과로 그리도록 바꾸고, "전체 955개 중 164개" 를 캡션으로 명시.
  (표는 원래 바뀌고 있었지만 화면 위쪽이 그대로라 안 바뀐 것처럼 보였음)

### 요청 13건
| # | 요청 | 처리 |
|---|---|---|
| 1 | 운용사에서 "운용" 빼기 | `short_manager()` — 미래에셋자산운용→미래에셋, 한국투자신탁운용→한국투자 (27개사 전부 고유) |
| 2 | 국내형/해외형 → 국내/해외 | `classify.py` 값 자체를 변경 |
| 3~5 | 컬럼 줄바꿈·가로 스크롤 제거 | Streamlit 표 헤더는 **줄바꿈 불가**(단일 행) → 라벨을 짧게(횟수·분배금·기준일·매수마감) + `width="small"` + 뜻은 `help` 툴팁으로. `유형` 컬럼은 표에서 뺌(엑셀엔 유지) |
| 6 | 종류 매수/배당 두 가지 | .ics 제목은 `[매수마감]` 유지, 표 값만 매수/배당 |
| 7 | 일정 설명에서 운용사 제거 | `calendar_ics.py` DESCRIPTION 재구성 (검증: 운용사 포함 이벤트 0건) |
| 8 | 날짜 26-8-14 형식 | `ymd()` 를 calendar_ics·dashboard 양쪽에 |
| 9 | "분배주기" → "주기" | DESCRIPTION·표 헤더 |
| 10 | 직전 분배율 추가 | **KRX 추가 조회 불필요** — 세이브로 `BUNBE`(분배율)를 이미 수집 중이었음. `최근_분배율` 컬럼 신설 |
| 11 | NAV 침식을 ETF 테이블 + 별도 창 | `📊 NAV 비교표` 탭 신설. 필터 상위 N개 일괄 조회 → 표 → **행 클릭 시 상세**(`on_select="rerun"`). 기존 `📉NAV 침식`(단일 종목) 탭은 유지 |
| 12 | 막대그래프 위클리 → 주 | `CYCLE_LABEL` 로 표시만 변환(데이터 값은 "위클리" 유지) |
| 13 | 비고 "확인필요" 뜻 설명 | 비고=`주기 확인`(짧게) + `비고상세` 컬럼 신설 + 표 아래 접이식 설명 |

### 14번 — 시총·총보수 (요청 후 추가)
- `src/etf_info.py` 신규. **KRX 정보데이터시스템**에서 전 종목 한 번에:
  - [13104] 전종목 기본정보 → `ETF_TOT_FEE`(총보수), `LIST_DD`(상장일)
  - [13101] 전종목 시세 → `MKTCAP`(시가총액)
  - pykrx 공개 API에는 이 둘이 없어 내부 클래스(`pykrx.website.krx.etx.core`) 사용
- 종목마다 조회할 필요 없이 **2회 호출로 1,155개** 전부. → `data/raw/krx_etf정보_YYYYMMDD.csv`
- `classify.py`의 `attach_krx_info()` 가 ISIN으로 결합 → 표에서 **주기 왼쪽**에 시총(억)·총보수
- 함정: **휴장일에도 종목 목록 1,155행이 오지만 MKTCAP이 전부 빈 값**.
  행 수로 개장일을 판정하면 틀림 → 값이 있는 행만 남기고 없으면 하루씩 뒤로

### 결과
- 955개 중 **933개**에 시총·총보수 결합. 빠진 22개는 만기·상장폐지 종목(최근 지급이 2025년)
- 산출물 재생성: `분배ETF목록.xlsx`(컬럼 17개), `분배캘린더.ics`(3,582건)
- 종목 수·분배주기 분포는 이전과 동일 (955 / 월 164 / 분기 195 / 반기 67 / 연 529)

### 검증
- pytest **56개 통과** (신규 `tests/test_display_formatting.py` 15개)
- Streamlit AppTest: 오류 배너 0건, 4탭 렌더, **필터 '월' 선택 시 955→164개로 실제 반영** 확인
- .ics 파싱 검증: 운용사 잔존 0건, 분배율·짧은 날짜·"주기" 반영 확인

### 주의
- `python src/classify.py` 는 KRX 정보 CSV가 없어도 동작한다(시총·총보수만 빈칸).
  채우려면 `python src/etf_info.py` 를 먼저 실행
- pykrx 내부 클래스에 의존하므로 pykrx 업그레이드 시 `etf_info.py` 점검 필요

### 다음 단계 후보
- 상장일(이미 수집됨)로 비고 '주기 확인'의 신규상장 추정을 실제 검증
- 실제 공시일로 캘린더 예상일 보정

---

## 5단계 — 조회 기준 선택 + YTD·1년 수익률 (2026-08-02 완료)

4단계 직후 세션이 **파일 저장 도중** 끊겨 `dashboard.py` 가 깨진 채 남아 있었다.
`sort_candidates()` 를 호출은 하는데 정의가 없어 NAV 비교표 탭에서 NameError.
남은 편집을 마저 넣어 복구한 뒤 이어서 진행.

### 1) NAV 비교표 — "위에서부터 10개"의 정체
- 기존 `위에서부터 10개` = 표 정렬(주기→가나다) 앞 10개라 **비교 기준으로 무의미**했다
- 조회 기준 8개를 `st.pills` **버튼**으로 눌러 고르게 함
  (시총 큰/작은 · 분배율 · 총보수 · 지급횟수 · YTD · 1년 · 종목명)
- 각 기준의 **정의**는 `❓ 기준이 무슨 뜻인가요?` 접이식 + 위젯 `help` 툴팁
- `COMPARE_ORDER` 를 `(정렬컬럼, 오름차순, 설명)` 3튜플로 확장 — 설명이 곧 화면 문구
- 값 없는 종목은 `na_position="last"` 로 항상 뒤 (오름차순에서 결측이 1등 되는 것 방지)

### 2) YTD·1년 수익률 (`src/etf_returns.py` 신규)
- KRX [13103] 전종목 등락률(`pykrx.stock.get_etf_price_change_by_ticker`)
  → **요청 2회로 1,039개 전부**. 종목별 조회 불필요 (etf_info.py 와 같은 패턴)
- 구간 경계는 `exchange_calendars` XKRX 로 개장일 스냅
  - 기준일 = 마지막 개장일 / YTD 시작 = 올해 첫 개장일(1/1 휴장 → 1/2) / 1년 = 정확히 1년 전 개장일
- 결과: `data/raw/krx_etf수익률_YYYYMMDD.csv` → 대시보드가 **KRX코드**로 결합
- 붙는 종목: YTD 910개 / 1년 844개 (955개 중). 나머지는 상장 1년 미만·만기·상폐

### 3) 표 정리 (가로 스크롤 제거)
- **다가오는 일정**: 길던 `설명` 한 칸(주기·분배율·예상기준일·지급일이 전부 들어있던)을
  `_split_description()` 으로 쪼개 **주기 / 분배율 / YTD / 1년 / 예상기준일** 짧은 칸으로 → 8칸
- **종목 목록**: YTD·1년 추가하면서 `구분`(국내/해외) 은 뺌 — 사이드바 필터에 이미 있음 → 13칸

### 고친 버그
- `filter_valid_isin_rows()` 가 입력에 이미 `KRX코드` 가 있으면 **컬럼을 두 개** 만들었다.
  대시보드가 로드 시점에 KRX코드를 붙이게 되면서 드러남 → 기존 컬럼을 덮어쓰도록 수정

### 해석 주의 (화면에도 캡션으로 명시)
- **YTD·1년은 가격(종가) 수익률**이다. 분배금 미포함 → 분배한 만큼 가격이 빠진 값이 그대로 들어 있다.
  분배금까지 감안한 값은 NAV 비교표의 `총수익`(총수익률 대용치)을 봐야 한다

### 검증
- pytest **79개 통과** (신규 `tests/test_compare_order.py` 12개, `tests/test_etf_returns.py` 8개,
  `test_nav_erosion.py` 에 KRX코드 중복 회귀 테스트 1개)
- AppTest: 예외·에러 0건 / 표 컬럼 수 13·5·8 확인 / 정렬 기준 8개 전부 동작 확인
  (시총 큰 순 → KODEX 200·TIGER 미국S&P500…, YTD 높은 순 → 실제 상위 종목)

### 주의
- `etf_returns.py` 를 안 돌리면 YTD·1년 칸이 빈다(대시보드는 죽지 않고 안내 문구 표시).
  **수익률은 매일 바뀌므로 xlsx에 넣지 않고 CSV로 따로 두고 대시보드에서 결합**한다
- 총보수 0.0 인 종목이 1개(ACE 러시아MSCI(합성), 거래정지) — KRX 원본 값 그대로라
  '총보수 낮은 순' 1위로 뜬다. 이름으로 걸러내지 않음(데이터값 판단 원칙)

### 5단계-후속 — 행 클릭 선택 + 가로 스크롤 제거 (같은 날)

- **체크박스 없이 행 클릭**: `selection_mode=["single-row", "single-cell"]` 로 바꿈.
  `st.dataframe` 의 행 선택은 **체크박스를 눌러야만** 잡히지만, `single-cell` 을 함께 켜면
  아무 칸이나 클릭했을 때 `selection.cells = [(행번호, 컬럼명)]` 이 온다.
  `picked_row_index()` 가 rows(체크박스) → cells(셀 클릭) 순으로 읽어 둘 다 지원
- **가로 스크롤**: 칸 수를 고정하지 않고 `pick_columns()` 로 **켜고 끄게** 함
  (종목명은 항상 고정). 기본값을 종목명 포함 **9칸**으로 잡아 기본 상태에선 스크롤이 없다
  - 종목 목록 13 → **9칸** (운용사·시총·총보수·주기·분배율·YTD·1년·매수마감)
  - NAV 비교표 9 → **7칸** (주기·NAV변화·분배율·총수익·YTD·1년)
  - 켠 칸이 9개를 넘으면 표 위에 안내 문구를 띄운다
- 검증: pytest **83개** / AppTest에서 세션 상태로 셀 클릭을 흉내 내 **상세 지표 5개·표 2개가
  실제로 열리는 것** 확인 (컬럼 수 9 / 8 / 7 도 함께 확인)

### 다음 단계 후보
- 상장일로 비고 '주기 확인'의 신규상장 추정 검증
- 실제 공시일로 캘린더 예상일 보정
- 분배금 재투자 반영한 진짜 총수익률(YTD·1년) 계산

---

## 6단계 — 분류 축 확장 (지역·섹터·자산·전략·과세) (2026-08-02 완료)

### KRX가 주는 값 (추정 아님)
`etf_info.py` 의 [13104] 응답에 이미 들어 있던 필드를 꺼내 씀:
| 컬럼 | KRX 필드 | 값 |
|---|---|---|
| 자산분류 | `IDX_ASST_CLSS_NM` | 주식 660 · 채권 147 · 혼합자산 64 · 기타 25 · 원자재 16 · 부동산 14 · 통화 7 |
| 시장분류 | `IDX_MKT_CLSS_NM` | 국내 481 · 해외 412 · **국내&해외 40** |
| 운용방식 | `ETF_REPLICA_METHD_TP_CD` | 실물/합성 × 패시브/액티브 |
| 과세유형 | `TAX_TP_CD` | 비과세 252 · 배당소득세 646 · 해외주식전용 21 등 |
| 기초지수명 | `ETF_OBJ_IDX_NM` | 아래 추정의 근거 |

또 [13101] 응답에서 **종가·NAV** 도 함께 저장(7단계 포트폴리오 평가용).

### 추정한 값 (`src/etf_taxonomy.py` 신규)
세부 지역(미국/중국)과 섹터(반도체/2차전지)는 **KRX에 필드가 없다.**
사용자 승인 받아 **기초지수명 텍스트**에서 규칙으로 추출 (종목명은 안 씀 — CLAUDE.md 원칙).
- 투자지역 10종: 한국 481 · 미국 246 · 글로벌 60 · 중국 43 · 일본 12 · 인도 11 · 유럽 7 · 대만 · 베트남 · 신흥국, 못 뽑으면 기타 54
- 테마섹터 17종: 광범위지수 198 · 채권·금리 158 · AI·테크 105 · 배당주 65 · 반도체 44 · 개별종목 26 …, 기타 163
- 전략 5종: 일반 854 · 커버드콜 48 · 혼합 27 · 인버스 4
- **시장분류가 '국내'면 지수명과 무관하게 한국**으로 둔다(미국채 혼합형 오분류 방지)

### 규칙 만들며 실제로 잡은 오분류
- `\bAI\b` 가 **'온디바이스AI'를 못 잡음** (한글 뒤 영문은 단어경계가 안 생김)
  → `(?<![A-Za-z])AI(?![A-Za-z])` 로 교체. IT·EV·UK·SOX 등 약어 전부 같은 처리
- **`iSelect` 가 'Select' 규칙에 걸려 광범위지수로 오분류** — iSelect는 NH투자증권의
  지수 브랜드지 분류 근거가 아니다 → 규칙에서 제거 (섹터 기타 304 → 163)
- 특수채·국고채·KOFR·KTB·크레딧이 채권으로 안 잡히던 것 보강

### 한계 (기록)
- **레버리지·인버스는 펀드 구조**라 기초지수명에 안 나온다(KODEX 레버리지의 기초지수는 그냥 KOSPI 200).
  여기서 잡히는 건 지수 자체가 인버스인 4건뿐 — 전략 필터로 레버리지를 거르면 안 된다

### 대시보드
- 사이드바에 **투자지역·테마섹터·자산분류·전략·과세유형·운용사** 필터 추가.
  **아무것도 안 고르면 전체**(기존 분배주기·국내해외는 기본 전체 선택 유지)
- 종목 목록에 지역·섹터·전략·자산·과세 칸 추가(기본 표시는 지역·섹터까지)
- 추정값이라는 안내를 사이드바 캡션과 컬럼 툴팁에 명시

### 검증
- 필터 실제 동작: 미국 246 / 배당주 65 / 채권 147 / 커버드콜 48 / 비과세 252,
  **미국+커버드콜 겹치면 30개**(전부 실제 미국 커버드콜 ETF인 것 이름으로 확인)
- 신규 `tests/test_etf_taxonomy.py` 13개 포함 pytest 통과

---

## 7단계 — 내 포트폴리오 대시보드 (2026-08-02 완료)

### 입력
`data/portfolio/보유종목.csv` — `계좌,종목명,KRX코드,수량,매수단가,매수일,메모`
- 계좌 사진은 **클로드 코드 대화창에 올리면 내가 읽어 CSV로 만든다**(사용자 선택).
  대시보드에 API 키를 넣지 않는 방식 — 비용·키 관리 없음
- 화면 표(`st.data_editor`)에서 직접 고치고 **저장** 누르면 같은 CSV에 기록
- KRX코드가 비면 종목명으로 찾는다. **매수일이 비면 최근 12개월 전체**로 계산하고 화면에 경고

### 계산 (`src/portfolio.py` 신규) — 새 수집 없이 기존 데이터만 씀
- 현재가·NAV ← `krx_etf정보_*.csv`(전종목 시세) / 분배 이력 ← `seibro_분배금_*.csv`(이익분배만)
- 평가금액·평가손익·수익률 / **받은 분배금**(매수일 이후 주당분배금 합 × 수량)
- **총손익 = 평가손익 + 받은 분배금**, 총수익률
- **YoC(매수가 대비 분배율)** — 연 주당분배금 ÷ **내 매수단가**. 현재가 기준 분배율과 따로 보여줌
- 분배금 회수율(원금 중 몇 % 회수) / 연간 총보수(평가금액 × 총보수%) / 비중
- 예상 다음 분배금 = 직전 주당분배금 × 수량, **세후**(배당소득세 15.4%)도 병기

### 화면
- KPI 7개 → 보유 상세표(칸 고르기 가능) → **지역·섹터·계좌별 비중 도넛 3개**
  → **내 종목의 다가오는 일정**(예상 분배금·세후 금액 포함)
- 사이드바 필터와 **무관**하게 항상 전체 기준 (내 보유는 걸러질 이유가 없음)

### 검증
- 신규 `tests/test_portfolio.py` 12개: 평가손익·매수일 유무별 분배금·YoC·합계·비중·저장왕복
- AppTest 실제 3종목 주입: 평가금액 2,406,850원 / 평가손익 +316,850(+15.16%) /
  받은분배금 44,510 / 총손익 +361,360(+17.29%) — 오류 0, 도넛 3개·일정표 렌더 확인
- 전체 pytest **107개 통과**

### 주의
- 현재가는 **직전 개장일 종가**(실시간 아님). 매일 최신으로 보려면 `python src/etf_info.py`
  → 8단계에서 화면 안 **🔄 시세 새로고침** 버튼으로 대체
- 분배금은 **세전**. → 8단계에서 계좌유형별 과세로 개선
- 세이브로 원본에 없는 종목(만기·상폐)은 분배금이 0으로 나온다

---

## 8단계 — 계좌유형 과세 · 시세 새로고침 · 현금흐름 · 리밸런싱 · 실현손익 (2026-08-02 완료)

### 1) 계좌유형별 분배금 과세
보유종목 CSV에 **`계좌유형`** 컬럼 추가 (계좌 '이름'과 별개). 사진 올릴 때 계좌 종류를 알려주면 채운다.
| 계좌유형 | 받을 때 | 최종 | 근거 |
|---|---|---|---|
| 일반(위탁) | 15.4% | 15.4% | 배당소득세 원천징수 (금융소득 2천만↑ 종합과세) |
| 연금저축 | **0%** | 3.3~5.5% | 과세이연 → 연금 수령 시 연금소득세 |
| 퇴직연금(IRP/DC) | **0%** | 3.3~5.5% | 위와 동일, 중도해지 시 기타소득세 16.5% |
| ISA | **0%** | 0% / 9.9% | 순이익 200만(서민형 400만)까지 비과세, 초과분 분리과세 |
- **계좌유형이 비면 일반(위탁)** — 세금을 과소평가하지 않는 쪽으로
- 신설 지표 **절세효과** = 받은분배금 × (15.4% − 실제 원천징수율).
  즉 "일반 계좌였으면 뗐을 돈". 나중에 인출 시 과세된다는 점을 툴팁에 명시

### 2) 🔄 시세 새로고침 (요청: "실시간 눌러볼 수 있으면")
- 버튼 → `전종목시세_ETF` 재조회 → 평가금액·손익·괴리율·비중 전부 다시 계산
- **정직하게 표시**: KRX 제공값이라 실시간 체결가가 아니라 **약 20분 지연**.
  장중이면 그 시각 시세, 휴장이면 직전 개장일 종가. 화면에 기준일자를 같이 띄운다
- 실시간 체결가는 증권사 API가 있어야 해서 이 도구 범위 밖

### 3) 💰 월별 분배금 현금흐름
- **실제 받은 금액**(세이브로 이력 × 내 수량, 매수일 이후)과 **앞으로 받을 예상**을 한 차트에
- 예상: 직전 지급기준일 + 분배주기를 반복해 월별로 쌓음. 세전/세후 둘 다
- 6~24개월 슬라이더, 월 평균(세후) 지표. 위클리는 월 단위로 묶어서 표시

### 4) ⚖ 리밸런싱
- 기준 선택: **종목명 / 투자지역 / 테마섹터 / 자산분류 / 계좌** (6단계 분류를 그대로 재사용)
- 목표비중을 화면에서 입력·저장(`data/portfolio/목표비중.csv`). 처음엔 **현재 비중이 출발점**
- 허용 오차(기본 ±5%p) 밖이면 `더 사기`/`덜기` + **조정금액(원)** 계산
- 목표 합이 100%가 아니면 경고. 조정금액 합은 항상 0 (총액 고정, 비중만 이동)

### 5) 🧾 실현손익
- `data/portfolio/매매내역.csv` (계좌,종목명,KRX코드,일자,구분,수량,단가,수수료)
- **이동평균법** — 국내 증권사 대부분의 방식. 매수 수수료는 평균단가에 포함, 매도 수수료는 손익에서 차감
- 계좌+종목별로 평균단가를 따로 굴린다(같은 종목이라도 계좌가 다르면 평단이 다름)
- 매수 기록 없는 매도, 보유량 초과 매도도 죽지 않게 처리

### 화면 구조
`💼 내 포트폴리오` 안에 서브탭 4개 — **📌 보유 현황 / 💰 분배금 현금흐름 / ⚖ 리밸런싱 / 🧾 실현손익**

### 용어
- **YoC = Yield on Cost** (매수원가 대비 수익률). 연 주당분배금 ÷ **내 매수단가**.
  현재가 기준 분배율과 나란히 보여줘야 의미가 있어 `YoC(매수가대비)` / `현재가대비` 두 칸으로 분리

### 검증
- 신규 `tests/test_portfolio_advanced.py` **27개** → 전체 pytest **134개 통과**
- 계산 손검산: 매수 10주@1,000 + 10주@1,400 → 평단 1,200, 15주@1,600 매도(수수료 100)
  = 실현손익 **+5,900원**(32.78%) ✓ / 연금 절세효과 = 40,700 × 15.4% = **6,268원** ✓
- AppTest 실제 주입: 오류 0, 표 12개·차트 6개 렌더, 리밸런싱 판정·현금흐름 12개월 정상

### 주의
- 세율은 **'보통의 경우'** 참고값이다. 나이·인출방식·금융소득종합과세 여부로 실제와 다르다
- 리밸런싱 조정금액에 세금·수수료·1주 단위 거래 제약은 반영 안 함
- 국내 ETF 매매차익은 국내주식형이면 비과세, 그 외는 배당소득세 대상 — 실현손익 표는 **세전**

---

## 9단계 — 실제 계좌 캡처 → 보유종목.csv (2026-08-02)

캡처 5장(미래에셋 종합잔고·계좌별 3장, 삼성 mPOP 퇴직연금DC)에서 **27행 / 4계좌** 추출.

### 이름이 잘린 문제 → 종가로 역추적
미래에셋 종합잔고는 종목명이 `TIGER 코리아A...` 처럼 잘려 있었다.
**`평가금액 ÷ 수량 = 현재가`** 를 KRX 종가와 대조해 특정 (이름 앞부분 + 종가 둘 다 맞는 것만).
16종목 전부 후보가 **1개씩만** 나와 확정. 이름 추측이 아니라 데이터값 대조.

### 수량이 없는 화면 → 평가금액 ÷ 종가로 역산
삼성 DC·ISA 화면에는 수량이 없다. `평가금액 ÷ 종가` 로 역산하는데,
**7/31 종가로는 8종목 전부 정수가 안 나왔다.** 날짜를 바꿔가며 확인한 결과
**7/30 종가에서 8/8 전부 정수** → DC 잔고가 `잔고기준: 결제` 라 하루 늦게 평가된 것.
(7/31에 반도체·AI ETF가 실제로 급반등: TIGER 반도체TOP10 26,440→34,370 +30%.
 KRX 일별 시세로 확인함 — 우리 평가가 최신이고 DC 화면이 하루 전 값)

### 검산 (전부 통과)
| 계좌 | 유형 | 매수원금(우리) | 화면 | 차이 |
|---|---|---|---|---|
| 미래에셋 종합주식 | 일반(위탁) | 30,124,756 | 30,124,755 | +1 |
| 미래에셋 IRP | 퇴직연금(IRP/DC) | 3,676,830 | 3,676,830 | 0 |
| 미래에셋 ISA | ISA | 2,633,700 | 2,633,700 | 0 |
| 삼성 DC | 퇴직연금(IRP/DC) | 45,070,598 | 45,070,595 | +3 |
- 차이 ±3원은 **평단 소수점** 때문 (여러 번 나눠 산 종목: TIGER 반도체TOP10 27,884.85 등)
- 삼성 DC 평가손익도 화면(-5,316,281)과 정확히 일치하는 것 확인

### 계좌 구조 (중요 — 사용자가 증권사 직원이라 계좌별 ETF 매매 제한이 있음)
- `010-9555-8624-3 [종합_주식]` — **ETF 매매 가능**. 우리가 쓰는 국내 ETF 12종목이 여기 있다
- **`860…`으로 시작하는 계좌 — ETF 매매 불가**(임직원 제한).
  종합잔고(전체 계좌)에만 보이고 8624-3에는 없던 **SK하이닉스·현대차가 이 계좌 것**이다
- 그래서 "종합잔고 총 23건" vs "8624-3 12건"의 차이가 설명된다.
  8624-3의 12줄 매입금액 합 = 30,124,755원으로 계좌 총액과 정확히 일치 확인

### 제외한 것
- **개별주식** SK하이닉스·현대차 (860 계좌) — 이 도구는 ETF 분배금 기준
- **펀드·현금** 우리단기채권, 삼성 디폴트옵션 포트폴리오2, 현금성자산, 증금수시예금,
  미래에셋러시아 펀드 (ETF 아님)
- 종합잔고의 `현금`/`일반담보` 중복 행은 **같은 계좌의 담보 구분**이라 합산
  (SOL 조선TOP3플러스 593+102=695주, 매입 합계가 계좌 총액과 정확히 맞음)

### 결과
평가금액 94,417,105원 / 평가손익 +12,911,221원(+15.8%) / 받은 분배금 139,843원(세후 128,727) /
절세효과 10,419원 / 연간 총보수 378,486원

### 9단계-후속 — 거래내역 캡처로 매수일 채움 (같은 날)

거래내역 캡처 26장 추가 판독 (ISA 1 / 종합_주식 20 / 삼성DC 4 / IRP 1).

- **29행 중 26행에 매수일 확정.** 전부 거래내역의 체결금액이 보유 매입금액과 **정확히 일치**하는 것으로 교차검증
  - 종합_주식: 5종목 2026-05-07 / PLUS K방산 2026-03-09 / TIMEFOLIO 3건 2025-09-17~18
  - IRP: **4종목 전부 2025-11-26 교체매매** (1,007,400 · 1,312,470 · 642,980 · 713,980 전부 일치)
  - ISA: 2026-05-12 · 2025-11-04 · 2025-10-21
  - 삼성 DC: 2026-04-28 · 2026-04-20 · 2025-10-23 · 2025-07-14 등
- **삼성 DC 캡처가 역산 검증해 줌**: TIGER 미국우주테크 `463주 @10,890` — 8단계에서 종가로
  역산한 값과 정확히 일치. SOL AI반도체도 `376+179+119=674주`, 합계 15,217,610원 일치
- **분할매수는 로트로 분리**: SOL AI반도체 3행(수량·단가가 화면에 있음).
  TIGER 반도체TOP10(종합주식)은 4회 매수 금액 합만 일치하고 로트별 수량은 장중 체결가라
  종가로 못 맞춰(445 vs 462주) 한 행 + 가장 이른 날짜로 표기

### 매수일을 못 채운 3행 (메모에 사유 기록)
| 계좌 | 종목 | 사유 |
|---|---|---|
| 종합주식 | SOL 조선TOP3플러스 695주 | 2024년부터 계속 매매 중이라 이동평균 로트 특정 불가. **연 1회 분배라 영향 작음** |
| 삼성 DC | TIGER 반도체TOP10 146주 | 4/14·4/15·5/7 매수 + 5/29 매도 146주가 얽힘 |
| 삼성 DC | PLUS K방산 133주 | 3/3·4/23 매매 얽힘 |
→ 이 3행은 '최근 12개월'로 계산되고 화면에 경고가 뜬다

**TIGER 차이나항셍테크는 해결**: 2025/03/10 `38,900원 = 4주 @9,725` 매수(평단과 정확히 일치)
→ 2025/04/09에 2주 매도, 현재 2주. 매수일 `2025-03-10` 확정

### 결과 (매수일 반영 후)
받은 분배금 139,843 → **132,564원**(세후 122,569)으로 정확해짐 / 총손익 +13,043,963원(+16.00%)
매수원금 재검산: 4계좌 모두 화면과 **±2원**

### 담보융자 관련 거래종류 구분 (사용자 확인)
미래에셋 종합_주식 계좌는 **주식담보융자**를 쓰고 있어 거래내역에 융자 관련 행이 섞인다.
셋을 반드시 구분해야 한다:
| 거래종류 | 주식 이동 | 매매인가 |
|---|---|---|
| `일반담보용자현금상환출금` | **없음** | ❌ 아니다. 담보로 잡힌 상태에서 **현금을 넣어 융자금을 갚은 것** |
| `담보용자설정` | 없음 | ❌ 담보 설정 기록일 뿐 |
| `일반담보용자매도상환출고` | **출고됨** (세금+수수료 발생) | ✅ 실제 매도. 팔아서 융자를 갚음 |
- 예: 2026/07/16 · 2026/06/26 현금상환(SOL 조선TOP3)은 **보유 수량이 그대로**다
- 예: 2026/04/13 SOL 조선TOP3 3,563,700 / 2026/03/05·03/03 PLUS K방산은 **진짜 매도**

### 실현손익은 넣지 않음 (판단)
**※ 정정**: 처음에 "캡처에 날짜 구간이 빈다(2025/10/16~22)"고 적었으나 **틀렸다.**
캡처 20장을 전부 확인하니 경계가 연속으로 이어지고(같은 날짜에서 행이 쪼개짐),
2025/10/16~22와 2022/03~2024/08은 **그냥 그 계좌에 매매가 없던 기간**이었다.
종합_주식의 국내 ETF 매매 이력은 **2024/08/06부터 완전하다**(그 이전은 해외주식만).

진짜 걸림돌은 따로 있다: **거래내역에 `거래금액`만 있고 `수량`이 없다.**
이동평균 평단을 굴리려면 건별 수량이 필요한데, 종가로 역산하면 장중 체결가와 달라 어긋난다
(검증: TIGER 반도체TOP10 4회 매수를 종가로 역산 → 445주, 실제 462주).
→ M-STOCK 거래내역 표는 **가로 스크롤(헤더 오른쪽 `>`)** 하면 수량·단가 컬럼이 더 있다.
   그 상태로 다시 캡처하면 실현손익까지 계산 가능.

### 남은 것 (2026-08-02 저녁에 이어서)
**사용자가 찍어 오기로 한 캡처**
1. **필수** — `010-9555-8624-3 [종합_주식]` 거래내역(계좌별 → 매매 → 2021.08.02~2026.08.02)을
   **표를 가로로 민 상태**(헤더 오른쪽 `>`)로 재캡처. **수량·단가 컬럼이 보여야 함**
   → 실현손익 계산 + SOL 조선TOP3플러스 695주 매수일 확정
2. **선택** — 삼성 DC(mPOP) **카드형 보기**로 `26-04-23`보다 아래까지 추가 캡처
   → DC의 TIGER 반도체TOP10 146주 · PLUS K방산 133주 매수일 확정

**안 찍어도 되는 것**: ISA(이미 체결량 있음) · IRP(단일 매수·매도 없음) · 860 계좌(ETF 없음)

> **※ 위 "남은 것"은 2026-08-03에 취소됨.** 아래 10단계 참조.

---

## 10단계 — HTS 잔고로 종합_주식 교차검증 · 실현손익 추적 중단 (2026-08-03)

### 1) 검증 결과: CSV 수정 없음
PC HTS 잔고 화면(`010-9555-8624-3 [종합_주식]`) 캡처 1장으로 대조.
**12종목 전부 수량·평균단가 일치.** 고칠 행 없었다.

- 어제 종가 역산으로 445주가 나와 미심쩍었던 **TIGER 반도체TOP10 462주 / 27,885 확정**
- **SOL 조선TOP3플러스** 교차검증이 정확히 맞음:
  현금 11,468,027 + 일담 1,972,578 = 13,440,605 ÷ 695주 = **19,339** (CSV 값과 동일)
  → 9단계의 "현금/일담은 같은 계좌의 담보 구분이라 합산" 판단이 옳았음을 재확인

### 2) 실현손익은 넣지 않기로 최종 결정 (사용자 판단)
거래내역 재캡처를 요청했으나 **사용자가 중단 결정**. 이유: 앞으로의 현금흐름이 중요하지
과거 실현손익은 부수적. **있는 데이터로만 가고, 없으면 없는 대로 둔다.**

- 따라서 `data/portfolio/매매내역.csv`는 **채우지 않는다**
- SOL 조선TOP3플러스 695주 **매수일은 빈칸으로 확정**(연 1회 분배라 영향 작음)
- DC의 TIGER 반도체TOP10 146주 · PLUS K방산 133주 **매수일도 빈칸 유지**
- 향후 이 3건 때문에 캡처를 다시 요청하지 말 것

### 3) 알게 된 것 — 앞으로는 캡처 대신 엑셀
사용자는 **PC HTS를 쓴다.** 잔고 화면 우측 상단에 **`엑셀` 버튼**이 있다.
9단계에서 캡처 31장을 판독한 작업이 이 버튼 한 번으로 끝난다. 판독 오차도 없다.
→ **다음에 계좌 데이터가 필요하면 캡처 말고 엑셀 export를 먼저 요청할 것.**

### 4) 포트폴리오 표 가로 스크롤 제거
`내 포트폴리오`의 두 표 모두 칸 너비를 **픽셀로 직접 지정**했다 (Streamlit 1.60).

문제는 `width="small"`(75px) / `"medium"`(200px)이 **한글 값에 너무 좁다**는 것이었다.
`TIGER 코리아AI전력기기TOP3플러스`, `미래에셋 종합주식`이 잘렸고 편집 표는 `메모`가 화면 밖으로 나갔다.

| 표 | 위치 | 조치 |
|---|---|---|
| 보유종목 편집(`st.data_editor`) | `dashboard.py:702` | 8칸 합 965px + 행선택칸 40px |
| 보유 현황(`st.dataframe`) | `dashboard.py:822` | 기본 9칸 합 약 960px |

- 종목명 250px / 계좌 115px / 계좌유형 130px 가 한글 기준 안 잘리는 최소치다
- `메모`는 내용이 길어 셀 안에서는 여전히 잘린다. **칸이 화면 밖으로 안 나가게만** 했다
- 칸을 더 켜면 스크롤이 생기는 건 그대로 — `NO_SCROLL_LIMIT = 9` 경고가 잡아준다

**주의**: 첫 로딩 중 표가 빈칸으로 잠깐 보일 때가 있다. 렌더링이 끝나면 정상이고 코드 문제는 아니다.

### 5) 현재 상태
`data/portfolio/보유종목.csv` 29행 / 4계좌 — **종합_주식 12행은 HTS 원본과 대조 완료**.
캡처 32장 `data/private/screenshots/`. 대시보드 `http://localhost:8501` 실행 중.
**pytest 134개 통과**(표 너비 수정 후 재확인).
실행: `venv\Scripts\streamlit run src\dashboard.py`

**현재 상태**: 대시보드 종료함. 캡처 31장은 `data/private/screenshots/` 로 이동 완료.
`data/portfolio/보유종목.csv` 29행 / 4계좌 / 26행 매수일 확정 / 매수원금 4계좌 모두 ±2원.
pytest 134개 통과. 실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 10단계: 사이드바 범위 선택 — 화이트리스트 / 보유종목 (2026-08-05)

**한 일**: 사이드바 맨 위에 **서로 독립된 라디오 2개**를 뒀다.
전에는 화이트리스트만 있었고 그것도 다른 필터들 사이에 묻혀 있었다.

| 선택 | 값 | 근거 파일 |
|---|---|---|
| 화이트리스트 | 전체 / 화이트리스트만 / 화이트리스트 제외 | `data/reference/whitelist.csv` |
| 내 보유종목 | 전체 / 보유종목만 / 보유종목 제외 | `data/portfolio/보유종목.csv` |

둘은 AND 로 걸린다 → `화이트리스트만 + 보유종목 제외` = **아직 안 산 매매가능 종목**.
파일이 없거나 하나도 안 맞물리면 그 라디오는 아예 안 그린다(예전 화이트리스트 동작 그대로).

**코드**
- `portfolio.mark_holdings(df)` 신규 — ETF 표에 `보유`(불리언)·`보유수량` 칸을 붙인다.
  매칭은 `whitelist.mark` 와 같은 규칙: **KRX코드 우선, 코드가 비었을 때만 종목명**.
  `A152100` 처럼 A 접두어가 붙어도 맞고, 같은 종목이 여러 계좌에 있으면 수량을 합친다.
- `dashboard.pick_scopes` / `apply_scopes` 로 분리 — 위젯은 사이드바 맨 위에 그리고,
  걸러내기는 다른 필터를 다 그린 **뒤에** 한다(분배주기 기본값이 전체 기준으로 잡히도록).
- 종목 목록 표에 `보유` 체크박스 칸 추가(기본 표시 아님, 항목 고르기에서 켤 수 있다).

**실제 데이터 기준 (955개 중)**: 화이트리스트 126 / 보유 10 / 둘 다 8 / 화이트인데 미보유 118.
보유종목.csv 는 15종목인데 10개만 맞물린다 — 나머지 5개(`TIGER 미국우주테크`,
`TIME 코리아밸류업액티브` 등)는 **분배 이력이 없어 분배ETF목록 자체에 없는** 것이라 정상이다.

**검증**: pytest **141개 통과**(신규 `tests/test_scope_filters.py` 7개 포함).
`streamlit.testing` AppTest 로 라디오 2개가 실제로 그려지고 `보유종목만` → 10개로 좁혀지는 것까지 확인.

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중.
실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 11단계: 분배 일정 달력 보기 + 월별 막대그래프 (2026-08-05)

`📅 다가오는 일정` 탭을 **서브탭 3개**로 나눴다. 셋 다 사이드바 필터를 그대로 따른다.

| 서브탭 | 내용 |
|---|---|
| 📋 목록 | 기존 표 (앞으로 며칠까지 슬라이더) |
| 🗓 달력 | 한 달씩 보는 7열 격자. 날짜 칸에 `매수 3` `배당 1` 배지 + 종목명 2개 |
| 📊 월별 건수 | 월별 예정 건수 **세로 막대**(분배주기별 색), 아래에 같은 값의 표 |

**달력**은 `st.markdown(unsafe_allow_html=True)` 로 CSS grid 를 직접 그렸다.
- 일요일 시작(한국 달력), 오늘 칸은 파란 테두리, 칸에 마우스를 올리면 그날 종목 전체가 툴팁
- 종목명은 `html.escape` 로 이스케이프 — 종목명에 `<` 가 들어와도 화면이 안 깨진다
- 배경은 `rgba(128,128,128,.10)` 으로 깔아 밝은/어두운 테마 양쪽에서 다 보인다

**색**은 임의로 고르지 않고 색맹 판별·배경 대비 검사를 통과한 값만 썼다
(`CYCLE_COLORS` / `KIND_COLORS`, 밝은·어두운 테마 각각). 주기별 색은 **고정**이라
필터로 종목이 줄어도 '월'은 늘 같은 색이다. 배지에는 색과 함께 `매수 3` 처럼 글자도 넣어
색만으로 구분하게 두지 않았다.

**막힌 자리 2개**
1. `pivot(index="월", columns="주기")` 에서 주기 값 중에도 `월`이 있어 컬럼이 부딪혔다
   (`ValueError: cannot insert 월, already exists`). 주기 칸을 `월 분배` 로 바꿔 해결.
   → `monthly_counts_wide()` 로 분리하고 테스트를 붙였다.
2. `main()` 의 `except` 가 이 예외를 삼켜 **화면 전체가 안 그려졌다**. 증상은
   "화면을 구성하지 못했습니다" 한 줄뿐이라, 디버그 복사본에서 `traceback` 을 찍어 찾았다.
   → **같은 일이 또 생기면 `src/dashboard.py` 를 복사해 except 를 traceback 출력으로 바꿔 볼 것.**

`tests/test_dashboard_nav.py` 의 AppTest 타임아웃을 30초 → 120초로 올렸다.
첫 렌더가 xlsx·ICS·시세 CSV 를 다 읽느라 테스트 환경에서 40초 안팎 걸린다(캐시 뒤에는 2.7초).

## 12단계: 후보군(pool) 확대 — 국내 상장 ETF 전체 (2026-08-05)

**955개 → 1,182개.** 세이브로에 분배 이력이 없는 상장 ETF 227개를 `미분배` 로 덧붙였다.
신규 상장이라 아직 안 준 것과 원래 분배를 안 하는 것이 섞여 있다.

- `etf_info.py`: KRX 기본정보에서 **종목명(ISU_ABBRV)·운용사(COM_ABBRV)** 를 추가로 받는다.
  전에는 이 두 칸이 없어서 분배 이력이 없는 종목은 이름조차 알 수 없었다.
- `classify.add_undistributed()`: 세이브로에 없는 ISIN 을 KRX 정보에서 가져와 붙인다.
  국내/해외는 **KRX 시장분류 값**으로 정한다(이름으로 추정하지 않음). 분배 칸은 전부 비운다.
- 대시보드: `미분배` 는 분배주기 필터에서 **처음에는 꺼져 있다**. 켜면 후보로 같이 보인다.
  색은 회색 하나(자료 없음이라 주기 5색과 섞지 않는다).

**사이드바 첫 필터를 투자지역으로 올렸다** — 기본값 `한국`·`미국`.
전에는 투자지역이 다른 필터들 사이에 묻혀 있었다. 다 지우면 전체가 나온다.

**검증**: pytest **157개 통과** (신규 `test_calendar_views.py` 8개 + `test_undistributed.py` 7개).
`streamlit.testing` AppTest 로 달력 격자 42칸·오늘 표시·차트 3개까지 확인.

**남은 것**: 미국 **직접 상장** ETF(SCHD·JEPI 등)는 아직 안 들어갔다.
소스는 yfinance(무료) + nasdaqtrader 심볼 파일로 잡아뒀고, 환율·미국 배당세 15% 원천징수·
과세체계가 달라 포트폴리오 계산까지 손봐야 한다. 다음 단계.

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중.
실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 13단계: 미국 직접 상장 ETF 추가 (2026-08-05)

**1,182개 → 3,282개** (국내 1,182 + 미국 2,100).

### 소스 (둘 다 무료, API 키 없음)
| 무엇 | 어디서 |
|---|---|
| 종목 목록 | nasdaqtrader.com 공개 심볼 파일 (`nasdaqlisted.txt`·`otherlisted.txt`, ETF 플래그) |
| 배당·시세 | yfinance — `yf.download(..., actions=True)` 배치 |
| 환율 | yfinance `KRW=X` (수집 당시 1,423.78원) |

`src/us_etf.py` 신규. 미국 상장 ETF 5,575개를 훑어 **최근 1년 배당이 있고 일평균 거래대금
$100k 이상**인 3,514개를 `data/raw/us_etf_20260805.csv` 로 저장한다.
목록에 합칠 때는 `$1M 이상`(2,100개)만 쓴다 — 안 그러면 거의 안 팔리는 종목까지 다 들어온다.

### 야후 호출 제한에 막힌 것 — 총보수·순자산(AUM)
배당·시세(chart API)는 5,575개를 4분에 다 받았는데, **총보수·AUM(quoteSummary API)은
스레드 12개로 돌리자 전부 401 Invalid Crumb** 으로 막혔다. 스레드를 4개로 낮춰도
그날은 이미 `YFRateLimitError` 상태라 0개를 받았다.

→ **`venv\Scripts\python src\us_etf.py fill` 을 나중에(몇 시간 뒤) 돌리면 채워진다.**
거래가 많은 종목부터 400개씩 채우니 여러 번 나눠 돌리면 된다. 배당·시세는 이 제한과 무관하다.
그때까지 미국 종목은 **시가총액·총보수 칸이 비어 있다** (분배율·거래대금·종가는 다 있다).

### 국내와 다르게 다룬 것
- **날짜 기준이 다르다.** 야후가 주는 날짜는 **배당락일(ex-date)**, 세이브로는 지급기준일이다.
  그래서 매수마감이 국내는 T-2, 미국은 **배당락 1영업일 전**이다. 공휴일도 미국 것으로 본다
  (`holidays.UnitedStates`, 캘린더는 `exchange_calendars` XNYS).
- **금액은 원화로 환산**해서 넣는다. 한 표에서 비교하려면 통화가 같아야 한다.
  환율과 기준일은 `us_etf` CSV 의 `환율`·`기준일자` 칸에 남아 있다.
- `ISIN` 은 비워 둔다 → KRX NAV 조회 대상이 아니라서 **NAV 침식·비교표 탭에서 자동으로 빠진다**.
- 종목명은 `SCHD Schwab U.S. Dividend Equity ETF` 처럼 **티커를 앞에 붙였다**.
- 지역·섹터·전략은 국내와 같은 `etf_taxonomy` 규칙에 태운다(종목명 + 야후 카테고리를 근거 문구로).
  미국식 표현 `Option Income`·`Derivative Income`·`WeeklyPay` 를 커버드콜 규칙에 추가했다
  (YieldMax·Roundhill 계열이 전부 여기 걸린다).

### 대시보드
- 사이드바에 **상장시장**(한국/미국) 필터 추가. 종목 목록 표에도 `시장` 칸(기본 꺼짐).
- 위클리 분배가 **0개 → 68개**로 늘었다(국내엔 주간분배 ETF 가 없다).
- 캘린더 이벤트 3,582건 → **19,442건**(6.8MB). ICS 파싱이 6.3초로 늘었지만 캐시 뒤에는 그대로다.

**검증**: pytest **167개 통과** (신규 `test_us_etf.py` 10개).

### 남은 것
1. `us_etf.py fill` 로 총보수·AUM 채우기 (야후 제한이 풀린 뒤)
2. **포트폴리오 탭은 아직 미국 종목을 계산하지 못한다** — 환율·미국 배당세 15% 원천징수·
   `evaluate()` 의 KRX 시세 조회가 모두 국내 기준이다. 보유종목에 미국 티커를 넣으면
   현재가를 못 찾는다. 다음 단계.

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중.
실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 14단계: 미국 ETF — 남은 두 가지 해결 (2026-08-06)

13단계에서 남겨둔 ① 총보수·AUM 빈칸 ② 포트폴리오 미국 미지원 을 처리했다.

### ① 총보수·AUM — 야후 제한이 풀리면 채워진다 (부분 완료)
아침에 다시 시도하니 `yf.Ticker('SCHD').get_info()` 가 정상으로 돌아왔다.
**제한은 하루 단위가 아니라 몇 시간 단위로 풀린다.**

`python src/us_etf.py fill 3600` 으로 **960개(27%)** 를 채웠고, 이어서 2차를 돌리자
곧바로 다시 막혀 0개였다. 거래대금 큰 순서로 채우므로 **주요 종목은 이미 다 들어왔다**
(VTI·VOO·IVV·SPY·VXUS 등). 나머지는 **몇 시간 간격으로 같은 명령을 다시 돌리면** 된다.

대체 소스도 찾아봤지만 stockanalysis.com 의 스크리너 API 는 전부 404 라 못 썼다.

⚠ 미국 시가총액은 야후 `totalAssets` 라 **같은 펀드의 다른 클래스(뮤추얼펀드)까지 합산된**
값이 섞여 있다. VTI 가 3,271조로 나오는 건 그 때문이다. 표의 도움말에 적어 뒀다.

### ② 포트폴리오가 미국 종목을 계산한다
| 무엇 | 어떻게 |
|---|---|
| 현재가 | `us_etf` 수집분의 원화 환산 종가 (`_apply_us_prices`) |
| 받은 분배금 | **배당 이력 CSV 신규** — `data/raw/us_배당_20260806.csv` (21,374건) |
| 세금 | 계좌유형과 무관하게 **현지 15% 원천징수** 고정 |
| 종목 매칭 | 목록에 **`종목코드`** 칸 신설 — 국내는 KRX 6자리, 미국은 티커(SCHD) |

`us_etf.py` 가 앞으로는 수집할 때 배당 이력을 같이 저장한다(`fetch_history` 가 두 표를 돌려준다).

### ③ 계좌 제약 — 연금·ISA 로는 미국 ETF 를 못 산다
사용자 지적을 반영했다.
- 사이드바 **"어느 계좌로 살 건가요?"** — 연금저축·IRP/DC·ISA 를 고르면 미국 종목이 목록에서 빠진다
  (고르면 3,282개 → 국내만 남는다)
- 포트폴리오에 연금·ISA 계좌로 미국 종목이 들어와 있으면 **빨간 경고**
  (`portfolio.account_conflicts`)
- 미국 종목은 매매차익이 **양도소득세 22%**(연 250만원 공제) 대상이라 이 계산에 없다는 안내도 붙였다

### 확인
`SCHD` 5주를 일반계좌 보유로 넣어 계산: 현재가 47,903원 / 평가금액 239,515원 /
받은 분배금 3,631원 / 원천징수 15.0%. 연금저축으로 바꾸면 경고가 뜬다.

**검증**: pytest **173개 통과** (신규 `test_portfolio_us.py` 6개).

### 남은 것
- `python src/us_etf.py fill 3600` 을 몇 시간 간격으로 더 돌려 총보수·AUM 채우기 (현재 960/3,514)
- **수급(거래주체별 매매동향)** — 사용자가 오후에 지시서를 따로 보내기로 함

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중.
실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 15단계: 미국 ETF 정보 보강 + 제목 변경 (2026-08-06)

"미국 ETF 정보가 부실해 활용하기 힘들다"는 지적에서 시작. 세 갈래로 다 손봤다.

### 대시보드 이름을 바꿨다
`국내 분배형 ETF 대시보드` → **`ETF 분배·포트폴리오 대시보드`**.
미국 상장분이 들어오면서 '국내'가 틀린 말이 됐고, 이제 종목 목록만이 아니라
보유 평가·현금흐름·리밸런싱까지 하기 때문이다.

### 종목명 옆에 코드 칸
증권사 앱에서 찾으려면 코드가 필요하다는 요청. **종목명 바로 오른쪽에 항상 붙는 `코드` 칸**을
넣었다(국내 6자리 / 미국 티커). `pick_columns(fixed=[...])` 로 끄고 켤 수 없게 고정한다.
미국 종목명 앞에 붙여 뒀던 티커는 뗐다 — 코드 칸과 겹쳐서다.

### ① 파생 지표 — 추가 호출 없이 만든 값
수집 기간을 1년 → **2년**으로 늘려서(배치 시간은 그대로) 아래를 계산한다.

| 지표 | 뜻 | 채워짐 |
|---|---|---|
| **연간분배율** | 최근 12개월 분배금 합 ÷ 현재가 | 미국 100% / 국내 79% |
| YTD·1년 수익률 | 야후 가격에서 직접 계산 | **0% → 100% / 94%** |
| 분배금성장률 | 직전 12개월 vs 그 앞 12개월 | 미국 88% |
| 변동성·최대낙폭 | 일간 수익률 기준 | 100% |

**연간분배율이 핵심이다.** 전에 쓰던 '분배율'은 직전 1회분이라 위클리와 연 1회를
그대로 비교하면 안 됐다. 국내도 세이브로 이력으로 같이 계산해 붙였다.

### ② 발행사 공식 자료 — `src/us_issuers.py` 신규
야후 호출 제한과 무관한 경로.

| 발행사 | 형식 | 받는 것 |
|---|---|---|
| iShares | JSON (제품 스크리너) | 총보수·순자산·자산분류·지역·12개월 배당수익률·ISIN — 532개 |
| SPDR | XLSX (제품 데이터) | 총보수·순자산·**기초지수명**·분배주기·ISIN — 177개 |

값이 `0.10%`·`$27,846.31 M` 같은 글자로 와서 숫자로 바꾸는 함수를 따로 뒀다.
`python src/us_etf.py issuers` 로 빈칸을 채운다(476개 채움).

### ③ 야후 반복 — `python src/us_etf.py loop`
제한이 풀릴 때까지 **20분 간격으로 8판** 다시 시도한다. 백그라운드로 돌려 두면 된다.
현재 총보수는 미국 2,100개 중 919개(44%). 나머지는 이 loop 가 시간을 두고 채운다.

### 파생 지표가 드러낸 것 — 고배당의 함정
연분배율 상위가 전부 **레버리지·인버스·YieldMax 계열**이었다.
`T-REX 2X Long SMR` 연 980% 분배에 YTD **-98%**, `YieldMax MSTR` 연 247%에 YTD -58%.
분배금을 원금에서 헐어 나눠주는 구조다.

- `etf_taxonomy` 전략 규칙에 미국식 표기를 넣었다: `2X`·`3X`·`Bull`·`Daily Target`·
  `YieldBOOST`(레버리지), `Bear`·`Short`·`UltraShort`(인버스). **방향이 배율보다 중요해서
  인버스를 먼저 검사한다**(`Bear 3X` → 인버스). 미국 2,100개 중 307개가 여기 걸렸다.
- 표에 **`총수익(1년)` = 1년 가격 변화율 + 연간분배율** 칸을 만들어 기본으로 켰다.
  분배한 만큼 가격이 빠지니 둘을 더해야 실제로 번 돈에 가깝다.
- 연분배율 20% 초과 + 총수익 마이너스인 종목이 있으면 표 위에 **노란 경고**가 뜬다(현재 21개).

**검증**: pytest **180개 통과** (신규 `test_us_metrics.py` 7개).

### 남은 것
- `python src/us_etf.py loop` 로 총보수·순자산 나머지 채우기 (현재 44%)
- Vanguard·Invesco·Schwab 발행사 자료는 아직 (API 경로를 못 찾았거나 안 봤다)
- 국내 분배금성장률 — 세이브로 수집분이 12개월치라 24개월로 늘려야 낼 수 있다
- **수급(거래주체별 매매동향)** — 사용자가 오후에 지시서를 보내기로 함

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중.
실행: `venv\Scripts\streamlit run src\dashboard.py`

---

## 16단계: 새 PC 이어받기 — DRM 사고 수습 + 재발 방지 (2026-08-07)

새 PC(`D:\Projects\etf-dividend-dashboard`)에서 클론해 이어받으려는데 대시보드가 안 떴다.
원인은 셋이었고 전부 잡았다. 그리고 **넷째 문제가 뒤늦게 드러났다**(아래 ⑤).

### ① 저장소에 올라간 xlsx가 DRM 봉투였다 (진짜 원인)
`output/분배ETF목록.xlsx` 의 첫 바이트가 `9b20 4452 4d4f 4e45` — **`DRMONE`**.
엑셀이 아니라 회사 DRM이 씌운 암호화 봉투가 그대로 커밋돼 있었다.
어제 그 PC에서 DRM이 걸린 뒤에 커밋·푸시가 된 것이다.

대시보드는 이걸 `pd.read_excel` 로 읽다 `ValueError: Excel file format cannot be
determined` 로 죽는데, `main()` 이 예외를 삼키고 "데이터를 읽지 못했습니다" 만 띄워서
원인이 안 보였다. 테스트도 `test_dashboard_exposes_working_nav_analysis_controls`
하나만 조용히 깨졌다(버튼 목록이 빈 채로).

**나머지 파일은 멀쩡했다.** `data/raw/*.csv` 8개와 `output/분배캘린더.ics` 전부 정상
(BOM+UTF-8 / `BEGIN:VC…`). 즉 **DRM은 엑셀만 건드렸고, 원본 데이터는 살아 있었다.**
그래서 `classify.py` 재실행만으로 완전 복구됐다 — 3,282종목, 데이터 손실 없음.

### ② 재발 방지 — 커밋 자체를 막는다
| 무엇 | 파일 |
|---|---|
| 스테이징된 파일 앞 64바이트에 DRM 서명이 있으면 커밋 중단 | `tools/check_drm.py` 신규 |
| 훅 (`git config core.hooksPath .githooks` 로 켠다) | `.githooks/pre-commit` 신규 |
| 훅이 CRLF로 바뀌면 실행이 안 돼서 LF 고정 + xlsx는 binary 취급 | `.gitattributes` 신규 |

가짜 DRM 파일을 스테이징해 **차단되는 것**과, 실제 스테이징분은 **통과하는 것**을 둘 다 확인했다.

### ③ cp949 콘솔에서 스크립트가 죽던 것
이 PC 콘솔이 cp949라 진행 메시지의 `—` 에서 `UnicodeEncodeError` 로 `classify.py` 가
마지막 줄에 터졌다. **엑셀은 이미 저장된 뒤라 결과는 멀쩡한데 명령만 실패로 보이는** 상태였다.
`src/console.py` 의 `use_utf8_stdout()` 을 만들어 진입점 7개(`classify`·`collect`·
`etf_info`·`etf_returns`·`us_etf`·`us_issuers`·`calendar_ics`)의 `__main__` 에서 부른다.
`PYTHONIOENCODING` 을 안 붙여도 끝까지 돈다.

### ④ 파이썬 버전
이 PC엔 3.14 뿐이었는데 `pandas==2.2.3` 은 3.14용 휠이 없어 소스 빌드로 넘어가고
MSVC가 없어 실패한다. **3.13.14 를 설치해 venv를 만들었다.** README에 3.11~3.13 으로 명시.

**검증**: pytest **180개 통과** (16단계 시작 시점엔 179 통과 / 1 실패였다).

### ⑤ 이 PC에서는 야후 수집이 아예 안 된다 — TLS 가로채기
`us_etf.py loop` 를 돌렸더니 800개씩 2판을 시도해 **둘 다 0개**를 채웠다.
"호출 제한이 풀릴 때까지 기다린다"는 전제 자체가 틀렸다. 진짜 원인은:

```
curl_cffi.requests.exceptions.CertificateVerifyError
  curl: (60) self signed certificate in certificate chain (19)
```

`fetch_details()` 안의 `one()` 이 `except Exception` 으로 **모든 예외를 삼키고**
`{"티커": ticker}` 만 돌려주기 때문에, 밖에서는 "0개 채움"으로만 보였다.
`fetch_details` 가 돌려준 표에 총보수·시가총액 칸이 **아예 없었다**(티커 칸 하나뿐).
뱅가드 `VOO` 까지 비었으니 "데이터가 없는 종목"이 아니라 수집 경로가 막힌 것이다.

`git push`·`pip`·`winget` 은 되는데 이것만 막히는 이유는 **curl_cffi 가 윈도우 인증서
저장소를 안 보고 자체 CA 번들만 보기 때문**이다. HTTPS를 가로채 검사하는 보안장비가
있으면 그 루트 인증서가 번들에 없어서 실패한다. DRM과 같은 계열의 환경 문제다.

야후만이 아니었다. `requests` 로 확인해 보니 **iShares·SPDR 도 전부 SSLError** —
이 PC에서는 외부 데이터 수집이 통째로 막혀 있었다.

### ⑥ 해결 — 회사 인증서를 합친 CA 번들 (`src/corp_ca.py` 신규)
환경변수에 이미 **`NODE_EXTRA_CA_CERTS=C:\certs\somansa-ca.cer`** 이 잡혀 있었다.
Node.js 용으로 추출해 둔 소만사 루트 인증서다(PEM 형식). `git` 이 되는 건
`http.sslbackend=schannel` 이라 윈도우 저장소를 보기 때문이고, 파이썬만 이걸 몰랐다.

`corp_ca.apply()` 가 **certifi 번들 + 회사 인증서**를 합쳐 `data/corp-ca-bundle.pem` 을
만들고 `REQUESTS_CA_BUNDLE`·`CURL_CA_BUNDLE`·`SSL_CERT_FILE` 에 물린다.
회사 인증서가 없는 PC면 아무것도 안 한다(집에서도 그대로 돌아간다).
번들은 PC마다 달라서 `.gitignore` 에 넣었다.

수집 스크립트 6개(`us_etf`·`us_issuers`·`collect`·`etf_info`·`etf_returns`·
`calendar_ics`)의 `__main__` 에서 부른다. 사용자가 직접 잡아 둔 값이 있으면 안 건드린다.

**확인**: `fetch_details(['VOO','SCHD'])` → VOO 총보수 0.03%·순자산 $1.69T,
SCHD 0.06%. `us_issuers.py` 실제 실행 → **709개** (iShares 532 + SPDR 177) 저장.

### ⑦ 조용한 실패를 안 하게 고쳤다
`fetch_details` 가 실패를 예외 종류별로 세어 끝에 출력한다. **전부 실패하면**
"호출 제한이 아니라 환경 문제일 수 있다"고 짚어 준다. 이게 없어서 2판(1,600회 시도,
약 40분)을 헛돌았다.

### ⑧ 그래서 총보수 채우기가 실제로 끝났다 — 40% → 97%
TLS 가 풀리자마자 한 판에 778개가 채워졌다(전에는 같은 조건에서 0개였다).

| 시점 | 총보수 (원본 3,516) | 대시보드 미국 종목 (2,503) |
|---|---|---|
| 오늘 아침 | 1,404 (40%) | — |
| 채우기 후 | 3,046 (**87%**) | 2,439 (**97%**) |

`loop` 의 20분 대기는 두 번 다 중단돼서, `fill 800` 을 연속으로 돌렸다.
세 판째에 **진짜** 호출 제한을 만났다(`YFRateLimitError` 326건 / Invalid Crumb).
⑦의 실패 집계 덕에 TLS 문제와 호출 제한이 이제 한눈에 구분된다.

발행사 자료(`us_etf.py issuers`)로는 0개 추가 — 그 종목들은 이미 야후로 채워진 뒤였다.
**남은 470개**는 야후 제한이 풀린 뒤 `fill 800` 을 한두 판 더 돌리면 된다.

### 남은 것
- 미국 총보수 남은 470개 (야후 제한 해제 대기 — 급하지 않다, 대시보드 기준 이미 97%)
- Vanguard·Invesco·Schwab 발행사 자료 경로
- 국내 분배금성장률 — 세이브로 수집을 24개월로 늘려야 계산 가능
- **수급(거래주체별 매매동향)** — 지시서 대기

**현재 상태**: 환경 복구 완료. 실행: `venv\Scripts\streamlit run src\dashboard.py`

---

## 17단계: 투자 판단에 빠져 있던 지표 보강 (2026-08-07)

"미국 ETF 투자 의사판단에 반드시 알아야 하는데 빠진 게 있나" 라는 질문에서 시작.
현재 칸을 전수 확인하고 네 가지를 짚었고, 그중 둘을 이번에 넣었다.

### ① 야후 응답에 이미 있는데 안 쓰던 값들 (추가 호출 0)
`get_info()` 를 이미 부르고 있으면서 여섯 필드를 버리고 있었다.

| 새 칸 | 야후 필드 | 왜 필요한가 |
|---|---|---|
| 3년·5년 평균수익률 | `threeYearAverageReturn`/`fiveYear…` | 1년만 보면 사이클을 못 본다 |
| 베타 | `beta3Year` | 시장 대비 얼마나 흔들리나 |
| 괴리율 | `navPrice` 와 종가 비교 | **NAV보다 비싸게 사는 만큼이 곧 손실**. 거래 뜸한 종목일수록 크다 |
| 설정일 · 운용기간_년 | `fundInceptionDate` | 3·5년이 빈 게 **신생이라서인지** 데이터가 없어서인지 구분된다 (MSTY = 2024-02 설정) |

야후는 3·5년을 비율(0.1497)로 주고 총보수는 %로 준다 — `_to_percent()` 로 맞춘다.
괴리율은 **환산 전 같은 통화(USD)끼리** 계산한다(한쪽만 환산된 상태로 계산하는 실수 방지).

`fill_details` 의 대상 선정도 고쳤다. 전에는 `총보수 or 시가총액` 이 빈 종목만 봐서,
총보수는 찼는데 새 칸이 빈 3,046개가 영영 안 채워질 뻔했다 → `FILLABLE` 중 **하나라도**
비면 대상으로 본다. 한 판 돌려 790개 채움.

### ② PTP — 팔 때 매도대금의 10%
사용자가 증권사 PTP 목록(140행)을 줬다. `data/reference/ptp_목록.csv` 로 넣고
`src/ptp.py` 가 판정한다. 배당이 아니라 **판 금액 전체**의 10% 라, 손실을 보고 팔아도
떼인다. 연 0.03% 총보수를 비교하다 이걸 놓치면 계산이 통째로 틀어진다.

**면제에는 만료일이 있다** — 이게 핵심이다. 오늘 면제여도 다음 달에 다시 대상이 된다.
그래서 상태만이 아니라 만료일을 같이 보여 준다(`면제~26-09-09`).
같은 코드가 여러 거래소에 있으면 **대상 쪽을 우선**한다(놓쳐서 떼이는 쪽이 손해가 크다).

현재 대시보드 3,282종목 중 **9개**가 걸렸고 전부 '면제'인데,
**2026-09-09 부터 차례로 만료**된다. 표 위에 만료 임박 배너가 뜬다.
USO·UNG 등은 분배가 없어 애초에 이 표에 없다.

### 아직 안 한 것 (같은 질문에서 나온 나머지)
- **ROC(원금 반환) 비중** — 고배당 함정의 원인 지표. 19a-1/8937 이 종목마다 PDF라 어렵다.
  대안으로 **분배금 변동계수**(이미 있는 배당 이력 21,374건으로 계산 가능)를 제안해 뒀다.
- **금융소득종합과세 2,000만원** — 지금은 경고 문구만 있고 계산이 없다.
  포트폴리오 탭에서 "내 연 예상 배당소득 합계 / 한도까지 얼마" 를 낼 수 있다(추가 수집 0).
- **청산 위험 플래그** — 순자산 + 거래대금 둘 다 이미 있어 임계값만 정하면 된다.

### 곁가지로 고친 것
`add_undistributed` 가 `rows[OUTPUT_COLUMNS]` 로 칸을 골라서, OUTPUT_COLUMNS 에
새 칸을 넣자 KeyError 로 **미분배 227종목이 통째로 빠졌다**(3,282 → 3,055).
`reindex` 로 바꿔 다시는 안 그러게 했다.

**검증**: pytest **188개 통과** (신규 `test_ptp.py` 8개). 대시보드 렌더 확인.

---

## 18단계: 청산(상장폐지) 위험 — 시장별 실제 기준으로 (2026-08-07)

17단계에서 남겨 둔 ④번. 처음엔 "국내 50억 / 미국 $50M" 을 통념으로 잡았는데,
**"각 시장의 기준을 찾아서 각자의 기준에 맞게 하라"** 는 지적을 받고 규정을 확인했다.
확인해 보니 **두 기준은 성격 자체가 달랐다.**

### 국내 — 실제 상장폐지 규정 (강제)
순자산총액 **50억원** 미만이 걸리면 실제로 폐지된다.
- 50억원 미만이 1개월 이상 지속
- 상장 1년이 지난 ETF가 50억원 미만이 되고 **다음 반기말**까지 미해소
- 신탁원본액 50억원 미만이 **3개월**간 미해소

LP 부재, 상관계수 미달(패시브 0.9·액티브 0.7) 요건도 있으나 그 자료는 아직 없어
반영하지 않았다. '상장 1년 초과' 조건도 국내 설정일을 안 받아 와서 못 건다.

### 미국 — 규정이 아니라 운용사 재량
거래소(NYSE Arca) 상장유지 요건은 **시가총액 $1M · 주주 50인**으로 훨씬 낮다.
흔히 쓰는 **$50M** 은 규정이 아니라 스폰서들이 손익분기로 잡는 선이고,
그 아래면 접는 일이 잦다. 즉 미국의 '위험'은 규정 위반이 아니라 **접힐 확률이 높다**는 뜻.

### 그래서 표시를 나눴다
| 값 | 국내 | 미국 |
|---|---|---|
| `상폐요건` | 순자산 < 50억원 (**규정**) | 순자산 < $1M (**거래소 요건**) |
| `위험` | — | 순자산 < $50M (관행선) |
| `주의` | < 100억원 | < $100M |

같은 '위험' 글자가 두 시장에서 다른 뜻이 되지 않게, 국내 규정선은 `상폐요건` 으로
따로 이름을 줬다. 툴팁에도 이 차이를 적었다.

**결과**: 국내 상폐요건 40개 · 주의 130개 / 미국 위험 117개 · 주의 99개
(미국 $1M 미만은 0개 — 배당이 있고 거래대금 $100k 이상이라는 수집 조건 때문).
국내 '상폐요건'은 빨간 배너, 미국 '위험'은 노란 배너로 표 위에 띄운다.

환율이 없으면 미국은 **판정하지 않고 비운다** — 달러 기준을 원화로 못 바꾸는데
잘못 표시하느니 비우는 게 낫다.

`일평균거래대금` 칸도 xlsx 로 함께 내보낸다(미국분만 있다).

**검증**: pytest **196개 통과** (신규 `test_liquidation_risk.py` 8개). 대시보드 렌더 확인.

### 남은 것 (17단계에서 이월)
- **분배금 변동계수** — ROC 대용 신호. 배당 이력 21,374건으로 계산 가능
- **금융소득종합과세 2,000만원 계산** — 지금은 경고 문구만, 계산 없음
- 미국 총보수 남은 470개 · 새 칸(설정일·베타 등) 나머지 (야후 제한 대기)

---

## 19단계: 국내 설정일 — 상장 1년 조건 반영 (2026-08-07)

18단계에서 "국내 설정일을 안 받아 와서 '상장 1년 초과' 조건을 못 건다"고 적었는데,
확인해 보니 **`etf_info.py` 가 이미 `상장일` 을 받고 있었다**(1,160개 전부). 수집 불필요.

- `KRX_COLUMNS` 에 `상장일` 추가 → `attach_krx_info` 가 붙인다
- 국내 `상장일` 을 미국 `설정일` 과 **같은 칸에 모았다**. 뜻이 같은 값이라,
  운용기간·청산위험 판정이 두 시장에서 같은 코드로 돈다
- `liquidation_risk(listed_on=...)`: 상장 1년이 안 된 국내 ETF는 `상폐요건` → `주의` 로 낮춘다

**결과는 지금 데이터에서 바뀌지 않았다** — 50억 미만 40개가 전부 상장 1년 초과였다.
규칙은 정확해졌고 단위 테스트로 검증했으니, 앞으로 신생 소형 ETF가 나오면 걸러진다.

### 곁가지 — 또 같은 자리에서 터졌다
`상장일` 을 KRX_COLUMNS 에 넣자 `attach_krx_info` 의 `result.drop(columns=available)` 이
`['상장일'] not found in axis` 로 죽었고, KRX 결합이 통째로 실패해
**미분배 227종목이 또 빠지고 국내 시총·총보수가 전부 비었다**(3,282 → 3,055).
`errors="ignore"` 로 막았다. 17단계의 `reindex` 와 같은 계열의 사고다 —
칸을 추가할 때 이 함수 둘을 항상 같이 봐야 한다.

**검증**: pytest **198개 통과** (`test_liquidation_risk.py` 에 2개 추가).

---

## 20단계: KRX 차단 확인 + 키 전달 통로 + DDR5 수집 시작 (2026-08-07)

수급 PRD 와 시장국면 대시보드 요청을 받고 데이터 경로를 파다가, **국내 데이터가
통째로 막힌 것**을 확인했다.

### ① KRX — pykrx 경로는 약관 위반이라 폐기해야 한다
`data.krx.co.kr` 이 **KRX Data Marketplace** 로 개편됐다. 요청하면 이 안내가 온다:

> 자동화 수단을 통한 비정상 대량 조회가 감지되어 해당 IP의 접속이 일시적으로
> 제한되었습니다. **이용약관 제10조 제2호는 자동화 수단을 이용한 정보 무단
> 수집·복제·배포를 금지**하고 있으며 (…) 해당 IP는 탐지일로부터 **1일간** 제한 (…)
> 화면 다운로드, 데이터 상품 구입 또는 **KRX Open API(openapi.krx.co.kr)** 등
> 공식 경로를 이용해 주시기 바랍니다.

**계정을 넣어도 안 된다.** pykrx 1.2.8 이 `KRX_ID`/`KRX_PW` 로 로그인을 시도하는데,
로그인 **페이지 자체**가 차단 페이지를 반환해 인증을 시도조차 못 한다
(`JSONDecodeError` — HTML 이 왔다). IP 기준이라 하루 뒤 풀리지만 다시 돌리면 재차단된다.

**우회하지 않는다.** 공식 경로인 KRX Open API 로 옮긴다 — 무료, 회원가입 후 인증키
신청, 영업시간 2~3시간 승인, 1년 유효, 서비스 분류별 별도 신청.

영향 범위는 수급 탭만이 아니다. **`etf_returns.py`(전종목 등락률)와 `etf_info.py`
(전종목 기본정보)도 같은 경로**라 함께 옮겨야 한다. 지금 대시보드가 도는 건
8/5~8/6 에 받아 둔 CSV 덕분이다.

### ② 키 전달 통로 — `.env`
키를 대화나 소스에 남기지 않으려고 만들었다.
- `src/env_keys.py` — 루트 `.env` 를 읽어 환경변수로 올린다. **이미 윈도우에 잡힌 값이 우선.**
- `.env.example` — 채울 항목 견본(값이 비어 있어 커밋해도 안전)
- `.gitignore` 에 `.env` (git check-ignore 로 확인)
- `tools/check_drm.py` 가 `.env` 스테이징을 **차단**한다. `git add -f .env` 로 실제 확인.

### ③ DDR5 현물가 — 오늘부터 쌓기 시작
TrendForce 공개 표에서 받는다(`src/ddr5.py`). robots.txt 는 `/price/` 를 막지 않는다.

**과거는 못 채운다.** History 는 유료(Gold+)라 무료로 보이는 건 오늘 값뿐이다.
그래서 **하루씩 쌓는 것 말고 방법이 없다** — 그래서 먼저 시작해 뒀다.
과거 구간까지 금리와 겹쳐 보려면 ECOS 반도체 수출물가지수 같은 대용치가 필요하다.

품목은 전부 저장하고 화면에서 고른다(한 페이지에 다 있어 수집 비용이 같다).
대표 지표는 **`DDR5 16Gb (2Gx8) 4800/5600`**(범용 칩) — 모듈(UDIMM·RDIMM)은
조립 마진·유통이 섞여 있어 금리·수출과의 동행을 볼 목적에는 칩 단가가 깨끗하다.

첫 수집(2026-08-07): 6개 품목. 칩 51.60 / RDIMM 32GB 1,545 / UDIMM 16GB 215 /
SO-DIMM 8GB 115 / eTT 23.64 / GDDR5 8Gb 11.64.

**검증**: pytest **225개 통과** (신규 `test_ddr5.py` 7개, `test_env_keys.py` 7개).

### 막혀 있는 것
- **KRX Open API 인증키** — 신청 후 `.env` 에 `KRX_OPENAPI_KEY`. 이게 있어야
  수급 탭·국내 데이터 갱신이 된다. 신청 화면의 서비스 목록에 '투자자별 거래실적'과
  'ETF' 가 있는지 확인 필요(내 쪽에서는 그 페이지도 차단돼 못 본다)
- **ECOS** — 키는 받았다. 수출·국고채 금리·예탁금 붙이는 작업은 아직

### 20단계-보완: DDR5 매일 자동 수집 등록 (2026-08-07)

과거를 살 수 없으니 하루라도 빨리 쌓아야 한다 → 윈도우 작업 스케줄러에 등록했다.

| 무엇 | 값 |
|---|---|
| 작업 이름 | `ETF-DDR5-daily` |
| 주기 | **평일(월~금) 18:00** |
| 실행 | `tools\collect_ddr5.bat` → `venv\Scripts\python.exe src\ddr5.py` |
| 로그 | `data\ddr5_collect.log` (gitignore) |
| 등록 | `tools\register_ddr5_task.bat` 한 번 실행 |

주말을 뺀 이유: 현물 시장이 주말에 호가를 내지 않아, 돌리면 **금요일 값에 주말 날짜만
찍혀** 가짜 데이터가 생긴다.

**`.bat` 은 ASCII 로만 쓴다.** cmd.exe 가 배치 파일을 시스템 코드페이지(cp949)로 읽어서,
한글 주석을 넣었더니 파싱이 깨져 명령이 통째로 실패했다. 한글 로그는 파이썬이 UTF-8 로 남긴다.

`ddr5.main()` 에 실행 시각 머리글과 `try/except` 를 넣었다. 스케줄러가 부르는 자리라
조용히 죽으면 아무도 모른다 → 실패 사유를 로그에 남긴다.

**확인**: `Start-ScheduledTask` 로 강제 실행 → 종료코드 0, 로그 724바이트 증가,
같은 날 두 번 돌려도 이력은 6행 그대로(중복 방지 동작).

다음 실행: 2026-08-07 18:00.

---

## 21단계: 시장국면 탭 ①② — DRAM·수출 ↔ 국고채 10년 (2026-08-07)

키 세 개(.env)가 다 들어와서 시장국면 대시보드를 시작했다.

### 데이터가 세 갈래로 모였다
| 출처 | 내용 | 구간 |
|---|---|---|
| **블룸버그 수기 추출** (사용자 제공) | DDR4 8Gb 현물·NAND 512Gb·국고채10년, 일별 745행 | 2023-10-02 ~ 2026-08-07 |
| **TrendForce** (`ddr5.py`, 평일 18시 자동) | DDR5 현물 6개 품목 | 2026-08-07 부터 누적 |
| **ECOS** (`ecos.py`) | 국고채10년(일)·수출금액(월)·**투자자예탁금(월) 신규**·DRAM수출물가(월) | 갱신용 |

블룸버그 데이터로 사용자의 가설을 확인했다: **DDR4 현물가 ↔ 국고채 10년 상관이
최근 1년 +0.945** (전체 2.8년은 +0.639 — 구간을 좁히면 부풀 수 있다는 것도 같이 보여준다).
DRAM 은 2.8년 동안 4.05 → 49.66 달러, **12.3배**.

### 대시보드 [📈 시장국면] 탭 신규 (`market_regime.py` + `render_market_regime`)
- ① DDR4(블룸버그)·DDR5(TrendForce)·국고채10년 — 이중축 일별 차트.
  **DDR4 와 DDR5 는 다른 물건이라 선을 잇지 않고 나란히** 둔다(값대가 비슷해도 별개).
  금리는 블룸버그 구간 뒤만 ECOS 로 잇는다(한 날짜에 두 출처를 섞지 않는다).
- ② 수출금액(막대) ↔ 국고채10년 월평균(선) — 월별 이중축.
- 상관계수를 전체/최근 1년 나란히 표시. **"상관은 인과가 아니다"** 경고를 탭 머리에 박았다.

### ECOS 곁가지 수정
DRAM수출물가가 **한 달에 세 줄**(계약통화·달러·원화) 와서 중복 제거 때 아무거나
남고 있었다 → `item2="D"` 달러기준으로 고정. DRAM 거래도, 블룸버그·TrendForce 도 달러다.

### ③ 예탁금/시총 — 둘 다 출처 확보 (다음 단계)
- **투자자예탁금**: 세이브로까지 갈 필요 없었다 — ECOS `901Y056/S23A` 월별,
  **1998-06 부터 28년치**가 있다. SERIES 에 등록하고 12개월치 받아 둠.
  (일별이 필요해지면 금융투자협회 FreeSIS 를 따로 붙인다)
- **전체 시총**: 승인된 KRX Open API 유가증권 일별매매에 `MKTCAP`·`LIST_SHRS` 칸이
  있다. 943종목 합 5,192조원 확인. 코스닥 API 승인이 나면 합산이 완성된다.

**검증**: pytest **230개 통과** (신규 `test_market_regime.py` 5개). 탭 렌더 확인
(상관 +0.639/+0.945 가 실제 화면에 나온다).

### 남은 것
- ③ 예탁금/시총 비중 그래프 — 역사적 평균선 포함 (KRX 코스닥 승인 대기와 무관하게
  KOSPI 만으로 먼저 만들 수 있다)
- ECOS 기간 늘려 받기: 내일 3y, 모레 5y … 예탁금은 최대 28년까지 간다
- KRX Open API: ETF 일별매매·종목기본정보·코스닥 승인 대기 → 수급 fetch.py

### 21단계-보완: 예탁금 꺾은선 + ECOS 3년 확장 (2026-08-07)

- `python src/ecos.py 3y` 실행 — 국고채 733일 / 수출 35개월 / **예탁금 36개월** / DRAM수출물가 35개월
- 예탁금 단위가 내 짐작(십억원)과 달리 **원 단위**로 온다 — 2026-07 = 104조원. 주석·변환 수정
- [시장국면] 탭에 ③ **투자자예탁금 꺾은선 + 기간 평균선** 추가 (`mr.deposits()`)
  - 최신 2026-07 = 104조원, 3년 평균 대비 **+48%**
  - 평균선은 쌓인 구간의 평균이라 이력을 늘릴수록(최대 1998년~) 역사적 평균에 수렴
- 예탁금/시총 **비중**은 코스닥 API 승인 후 (KOSPI 시총은 이미 나온다)

**검증**: pytest **232개 통과**. 렌더 확인.

---

## 22단계: ECOS 전 구간 + 예탁금/시총 비중 (KOSPI 분모) (2026-08-07)

### ECOS — 단계적으로 늘릴 필요가 없었다
"오늘 1년, 내일 3년…" 계획이었는데 한 지표가 최대 6,349행이라 MAX_ROWS(10,000)
안이다. **`ecos.py 28y` 한 번으로 전 구간을 받았다**:
국고채10년 6,349일(2000-12~) / 수출 318개월(2000-01~) / **예탁금 336개월(1998-08~)** /
DRAM수출물가 335개월. 예탁금 절대액 차트의 평균선이 이제 진짜 '역사적 평균'이다.

### KOSPI 월말 시총 — `src/krx_api.py` 신규 (공식 API 전용 층)
- 월말 개장일을 exchange_calendars(XKRX)로 뽑아 그날의 유가증권 전 종목
  `MKTCAP` 합을 받는다. 36개월 수집 완료(2023-08~2026-07)
- **받은 달은 다시 안 부른다**(CSV 캐시). 이번 달만 월말 전이라 갱신 대상
- 호출 사이 0.35초 간격. 오늘(20260807) 데이터는 아직 미배포라 응답이 비었다 —
  다음 실행 때 채워진다
- 앞으로 ETF·코스닥 승인이 나면 수급 fetch 도 이 파일에 얹는다

### ③-2 예탁금/시총 비중 차트
`mr.deposit_ratio()` — 같은 달끼리(YYYYMM) 병합해 비중(%)을 낸다.

**바로 그림이 말을 했다**: 예탁금 절대액은 역대 최고 수준인데,
**비중은 1.91%로 3년 평균(2.50%) 아래다.** 돈이 늘어난 것보다 시장이 더 커졌다 —
절대액만 보면 "역대급 대기자금"으로 읽히지만 비중으로는 오히려 평균 이하.
이 대비가 이 차트를 만든 이유다.

⚠ 분모가 아직 **KOSPI 뿐**이라(코스닥 API 승인 대기) 비중이 실제보다 높게 나온다.
화면에 "수준보다 추이를 보라"고 적었다. 코스닥 승인 후 분모를 합산하면 된다.

**검증**: pytest **235개 통과** (`test_market_regime.py` +3: 비중 병합·결측 달 제외·
시총 캐시 재호출 방지). 렌더 확인.

### 남은 것
- KRX 승인 대기: ETF 일별매매(국내 ETF 갱신 경로 교체) · 코스닥(시총 분모 완성) ·
  종목기본정보 · 투자자별 거래실적(있는지 확인 필요 — 없으면 수급 PRD 재설계)
- 시총 이력을 더 뒤로(60개월 등): `python src/krx_api.py 60`

---

## 23단계: KRX Open API 전면 승인 — 국내 갱신 경로 교체 + 분모 완성 (2026-08-07)

사용자가 서비스 승인을 받아 와서 ("승인되었어. 고고") 막혀 있던 것을 다 뚫었다.
**ETF 일별매매 · 코스닥 일별매매 · 유가/코스닥 종목기본정보 전부 ✅.**

### ① 국내 ETF 갱신 경로 교체 — `src/etf_refresh.py` 신규
pykrx 차단 후 국내 ETF 시세를 새로 받을 방법이 없었다(8/5 CSV 가 마지막).
ETF 일별매매 API 에는 종가·NAV·시총·**순자산총액·상장주식수**·기초지수명이 다 있다.

- **정적 칸은 살리고 시세만 바꾼다**: 마지막 정보 CSV 의 총보수·분류·상장일·ISIN 은
  그대로, 종가·NAV·시가총액만 오늘 값으로. 신규 상장은 API 값만으로 덧붙인다
- **수익률은 직접 계산**: 연초·1년 전 개장일(trading_bounds 재사용) 종가를 받아
  YTD·1년 = 시작 종가 대비 변화율. 그 사이 상장된 종목은 해당 칸만 비운다
- 그날 자료가 미배포면(장 마감 직후) 직전 개장일로 자동 후퇴
- 실행 결과: **1,160종목 시세 갱신** (8/6 기준), YTD 1,039 / 1년 960 계산.
  classify 재실행 → 3,282종목 xlsx 정상

etf_info.py·etf_returns.py 는 CSV 읽기용으로만 남는다(README 갱신).

### ② 예탁금/시총 분모 완성 — KOSPI+KOSDAQ
- `krx_api.py` 를 시장 일반화(collect/load 에 market 인자, 코스닥 CSV 분리)
- 코스닥 월말 시총 36개월 백필 (202308~202607, 마지막 400조원)
- `deposit_ratio()`: **두 시장이 다 있는 달만** 합산(분모가 달라지면 추이가 왜곡).
  코스닥 이력이 없으면 KOSPI 만으로 내되 `분모시장` 칸으로 구분 → 화면 표기 자동
- 합산 결과: 최신 비중 **1.78%** (3년 평균 2.14%) — KOSPI 단독 1.91%/2.50% 에서
  수준은 내려갔지만 **"평균 아래"라는 결론은 같다**

### 곁가지
- 내 기존 비중 테스트가 코스닥 CSV 를 격리하지 않아 실제 데이터가 새어 들어가
  깨졌다 → 두 CSV 를 다 임시 폴더로 돌리는 `caps_sandbox` 로 정리
- ETN·KRX 지수는 여전히 401 (신청 안 한 서비스 — 지금 쓸 데 없음)

**검증**: pytest **243개 통과** (신규 `test_etf_refresh.py` 7개, 비중 합산 1개).
렌더 확인 (분모 KOSPI+KOSDAQ 캡션).

### 남은 것
- **수급 fetch.py** — ETF 일별매매의 LIST_SHRS(상장주식수)로 설정·환매 추적이
  가능해졌다. 투자자별 순매수는 KRX Open API 에 없어 보임 → 신청 화면 목록에
  '투자자별 거래실적'이 있는지 사용자 확인 대기. 없으면 PRD 7.2 를
  상장주식수 중심으로 재설계
- DDR5 처럼 국내 ETF 도 매일 자동 갱신 걸지 검토 (etf_refresh + classify)

---

## 24단계: 수급 탭 v1.1 — 상장주식수(설정·환매) 기반 (2026-08-07)

투자자별 거래실적이 합법 자동화 경로에 없다는 게 확정돼(23단계 + 사용자 스크린샷 +
세이브로는 결제기관이라 해당 데이터 없음 + 네이버는 robots 전면 차단) PRD 를
**상장주식수 기반으로 재설계**했고, 사용자 승인을 받아 구현했다.

### PRD v1.1 — `docs/수급_PRD.md` 신규
원본(v1.0, 대화 제공)의 "기준을 바꾸면 문서를 갱신한다" 규칙에 따라 문서화.
| 플래그 | v1.1 조건 |
|---|---|
| 🔴 이탈 | 20일 **그리고** 60일 상장주식수 감소 (환매 우위 지속) |
| 🟢 유입 | 5거래일 연속 증가 **그리고** 20일 증가 |
| ⚠ 함정 | 연간분배율 상위 30% + 🔴 (변경 없음) |
오경보 방지 원리(단기+중기 동시 요구)는 유지. 사용자 스크린샷 덕에 ETF 일별매매가
**2010-01-04부터** 제공되는 것도 확인 — 과거 백필 여지가 크다.

### 구현
- `flow/fetch.py` — 외부 접점. **날 단위 캐시**(하루 호출 한 번에 전 ETF 가 오니
  종목별 캐시가 무의미하다). 받은 날 재요청 없음·빈 응답은 파일 안 만듦(다음에 재시도)·
  렌더 중 API 호출 없음(버튼/CLI만). 60거래일 백필 → **59일 캐시**(오늘치는 미배포)
- `flow/signals.py` — v1.1 판정식으로 교체. `flag_distribution_trap` 연결 지점 유지
- `flow/metrics.py` — 60일 증감·증감률(%) 추가
- 대시보드 **[🌊 수급] 탭(7번째)**: 관심종목 관리(코드 추가/멀티셀렉트 삭제 —
  v1.0 의 랭킹 표 체크박스는 3,282행 표라 미룸, PRD 에 기록), 기간 선택(20~490),
  요약 표(20·60일 증감%·연속증가일·거래대금·플래그), 상세 차트(주식수+거래대금),
  캐시 부족 시 'N일치' 안내

### 실데이터 확인 (069500 KODEX 200, 59일치)
20일 +7.06% / 60일 +9.07% / 연속증가 1일 → 플래그 없음 (설정 우위, 유입 신호는 미달)

**검증**: pytest **249개 통과** (signals 재설계 6개 + fetch 6개 신규). 렌더 확인.

### 남은 것
- 수급 과거 백필(2010~) 은 필요할 때 `python src/flow/fetch.py 490`
- 투자자별 순매수 v2 후보: 증권사 오픈API(한투 등) 또는 KRX 화면 수동 다운로드 반입
- 국내 ETF 매일 자동 갱신(etf_refresh + fetch + classify 묶어 스케줄러) 검토


---

## 25단계: ⭐ 관심 플랜 — 관심종목 현금흐름 시뮬레이션 (2026-08-08)

"관심종목에 ETF를 넣으면 배당 캘린더와 예상 현금흐름을 계산해 달라"는 요청.
**아직 안 산 종목을 '사면 얼마가 들어오나'** 로 미리 보는 탭이다.

### 설계 — 관심종목 파일은 하나만
수급 탭(24단계)이 이미 `data/reference/관심종목.csv` 를 쓰고 있어서 **같은 파일을 공유**한다.
- `src/watch.py` 신규 — 수량·투자금액·메모 칸을 더해 읽고 쓴다
- `flow/fetch.py.save_watchlist` 수정 — 수급 탭이 저장할 때 (종목코드·종목명)만 쓰면서
  **다른 칸을 지우던 문제**를 기존 파일과 병합해 보존하는 방식으로 바꿨다

### 계산 (가정을 화면에 명시)
- **직전 12개월 분배가 그대로 반복**된다고 가정. 1회분 = 연간주당분배금 ÷ 지급횟수(평균)
- 예상 지급일: 최근 지급기준일 + 분배주기 반복 (`calendar_ics.add_cycle` 재사용 —
  월말 유지·위클리 처리 그대로)
- 매수마감: 국내 T-2 / 미국 T-1, 공휴일은 각 나라 것 (`classify.prev_business_day`)
- 수량이 없으면 투자금액 ÷ 현재가 → **정수 주수**로 내림, 실제 투자금액을 다시 잡는다
- 세후: 계좌유형 선택(원천징수), 미국 상장분은 계좌 무관 15%.
  연금·ISA 계좌 선택 + 미국 종목이면 **매수 불가 경고**
- 가격 변동은 예측하지 않는다 — 1년수익률은 과거 참고값으로만 표시

### 화면 (⭐ 관심 플랜 탭, 8번째)
- 관심종목 편집기(수량 또는 투자금액) + 계좌 선택
- KPI 4개: 투자금액 합계 / 예상 연 분배금 세전·세후 / 예상 연 분배율
- 종목별 표: 연분배금·연분배율·1년수익률(참고)·분배성장·**다음매수마감**
- 월별 예상 현금흐름 12개월 (종목별 누적 막대, 세후) + 다가오는 지급 일정 15건
- 연분배율 20%↑ & 1년수익률 마이너스 종목은 고배당 함정 경고

### 배당 캘린더는 기존 탭 재사용
사이드바 범위 선택에 **관심종목** 라디오(전체/만/제외)를 추가했다 (`watch.mark`).
**'관심종목만'을 고르면 📅 다가오는 일정 탭(목록·달력·월별 건수)이 그대로
관심종목 배당 캘린더가 된다** — 달력을 새로 만들 필요가 없었다.

**검증**: pytest **257개 통과** (신규 `test_watch.py` 8개 — 정수 주수 변환,
미국 15% 세율, 연금계좌 매수 불가, 월배당 12회 일정, 매수마감<기준일).

### 남은 것
- 미국 총보수 나머지 137개 — 야후에 값이 아예 없는 종목들 (발행사 자료 확대로만 가능:
  Vanguard·Invesco·Schwab·Global X 등)
- 국내 분배금성장률 — 세이브로 수집을 24개월로 늘려야 계산 가능
- DDR5 처럼 국내 ETF 도 매일 자동 갱신 걸지 검토 (etf_refresh + classify)

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중 (탭 8개).
실행: `venv\Scripts\streamlit run src\dashboard.py`


---

## 26단계: 미국 총보수·순자산 100% (2026-08-08)

96% 에서 멈춰 있던 나머지 137개를 채웠다. **미국 2,100개 전부 총보수·시가총액이 있다.**

### 왜 야후로는 안 됐나
남은 137개를 야후에 직접 물어보니 **접속은 되는데 값이 `None`** 이었다.
호출 제한이 아니라 **야후에 그 종목 정보가 아예 없는 것**이었다 —
`loop` 를 더 돌려도 소용없는 상태였다.

발행사도 30여 곳에 흩어져 있어(Direxion 9 · Fidelity 7 · Franklin 7 · Innovator 5 …)
발행사별 연동은 수지가 안 맞았다.

### stockanalysis.com — 발행사를 가리지 않는 소스
전에 스크리너 API 는 404 였는데, **종목 페이지의 SvelteKit 데이터**는 열려 있었다:
`https://stockanalysis.com/etf/{ticker}/__data.json`

이 응답은 `data` 배열 안에서 **자리 번호로 서로를 가리키는** 구조라 그대로는 못 읽는다
(`{"expenseRatio": 24}` 는 24번째 자리에 값이 있다는 뜻). 풀어 주는 함수를 붙였다.

받는 값: 총보수 · 순자산 · 배당수익률 · 자산분류. 종목당 요청 1회라
**총보수가 빈 종목만 골라** 부른다: `python src/us_issuers.py missing`

### 결과
| | 전 | 후 |
|---|---|---|
| 총보수 | 3,379 (96.1%) | **3,512 (99.9%)** |
| 시가총액 | 3,379 | **3,512** |
| 운용사 | 3,246 | 3,379 (96.1%) |

목록에 들어가는 미국 2,100개 기준으로는 **총보수·시총 100%**.
(수집분 3,516개 중 남은 4개는 stockanalysis 에도 없는 종목)

**검증**: pytest **260개 통과** (신규 `test_stockanalysis.py` 3개 — 금액 단위 변환,
퍼센트 변환, 자리 번호 참조 풀기).

### 남은 것
- 국내 분배금성장률 — 세이브로 수집을 24개월로 늘려야 계산 가능
- DDR5 처럼 국내 ETF 도 매일 자동 갱신 걸지 검토 (etf_refresh + classify)

**현재 상태**: 대시보드 `http://localhost:8501` 실행 중 (탭 8개).


---

## 27단계: 세이브로 2년치 + 국내 매일 자동 갱신 (2026-08-08)

### ① 분배금성장률 — 국내도 나온다
세이브로 수집을 **1년 → 2년**으로 늘렸다(`collect.py` 기본 `YEARS = 2`,
`python src/collect.py 1` 로 되돌릴 수 있다). 5,541건.

**주의할 자리**: 기간만 늘리면 `n = len(dates)` 가 2배가 되어 **분배주기 판정이 뒤집힌다**.
분기배당이 2년 8회로 세어져 '월'(7~39회)이 된다. 그래서 `classify` 안에서
**최근 12개월로 잘라** 판정하고(`recent`), 그 앞 12개월(`previous`)은 성장률에만 쓴다.
`tests/test_two_year_window.py` 7개가 이 회귀를 잡는다.

**뜻밖의 소득 — 주기 판정이 더 정확해졌다.** 1년치일 땐 반기배당이 1회만 잡혀 '연'으로
새던 것들이 제대로 잡혔다:

| 주기 | 1년치 | 2년치 |
|---|---|---|
| 반기 | 67 | **135** |
| 연 | 529 | 478 |
| 분기 | 195 | 211 |

국내 분배금성장률은 **513개(43%)** 채워졌다(나머지는 앞 12개월 이력이 없는 신규 상장).
⚠ 상장 첫 해에는 지급 **횟수**가 적어 성장률이 수백 %로 찍힌다 — 표 도움말에 적어 뒀다.

### ② 국내 데이터 매일 자동 갱신
DDR5 와 같은 방식(윈도우 작업 스케줄러).

```
toolsegister_etf_task.bat        # 등록 (평일 18:30)
schtasks /Run /TN "ETF-refresh-daily"      # 지금 돌려 보기
schtasks /Delete /TN "ETF-refresh-daily" /F
```

`etf_refresh.py → classify.py → calendar_ics.py` 를 차례로 돌리고 **한 단계라도 실패하면
거기서 멈춘다**(낡은 xlsx 가 새 것인 척하지 않게). 기록은 `data/etf_refresh.log`.
`.env` 의 `KRX_OPENAPI_KEY` 가 필요하다.

이 PC 에 두 태스크를 모두 등록했다 — `ETF-refresh-daily`(18:30) · `ETF-DDR5-daily`(18:00).
미국·세이브로는 자동에 넣지 않았다(5~6분씩 걸리고 매일 바뀌는 값이 아니다).

### 남은 것 (다음에 이어서)
- 스케줄러 첫 자동 실행 결과 확인 — `data/etf_refresh.log` 가 비어 있으면
  `schtasks /Query /TN "ETF-refresh-daily" /V /FO LIST` 로 Last Result 를 본다
- 관심 플랜 탭에 **환율 변동 시나리오**(미국 종목 원화 환산) 넣을지 검토
- 국내 위클리 분배 ETF 가 생기면 캘린더 예측 주기 확인

**현재 상태**: 전체 3,294종목(국내 1,194 + 미국 2,100). 대시보드 탭 8개.


---

## 28단계: 스케줄러가 안 돌던 이유 — 배터리 (2026-08-08)

27단계에서 등록한 `ETF-refresh-daily` 가 `Last Result: 0` 인데도 **로그가 안 생겼다.**
`schtasks /Run` 을 눌러도 상태가 계속 `Queued` 였다.

### 원인
`toolsefresh_etf.bat` 를 직접 실행하니 116초 만에 정상으로 끝났다(로그 4,645바이트).
bat 은 멀쩡했고 **스케줄러가 시작을 안 시킨 것**이었다.

```
DisallowStartIfOnBatteries : true    ← schtasks 기본값
StartWhenAvailable         : false   ← 놓친 실행을 나중에 하지 않음
Win32_Battery.BatteryStatus: 1       ← 이 PC 는 지금 배터리로 구동 중
```

**노트북에서 `schtasks` 로 만든 작업은 배터리 상태면 영원히 `Queued` 로 앉아 있는다.**
게다가 놓친 실행을 만회하지도 않아, PC 가 꺼져 있던 날은 그냥 건너뛴다.

### 고친 것
`schtasks` 에는 이 세 가지를 켜는 스위치가 없어서 PowerShell 로 바꿔야 한다.
등록 스크립트(`register_etf_task.bat` · `register_ddr5_task.bat`) 안에 한 줄 넣었다:

```powershell
New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries `
  -StartWhenAvailable -ExecutionTimeLimit (New-TimeSpan -Hours 2) -MultipleInstances IgnoreNew
```

바꾼 뒤 상태가 `Queued` → `Ready` 가 됐고, `schtasks /Run` 으로 **끝까지 도는 것을 확인**했다:

```
국내 ETF 갱신 (KRX Open API) — 기준일 후보 20260807
  ① krx_etf정보_20260807.csv — 1,160종목 (시세 갱신 1,160 / 신규 0)
  ② krx_etf수익률_20260807.csv — YTD 1,039개 / 1년 960개
전체 종목: 3294개 / 월·위클리: 900개
이벤트 종목: 2913개 / 총 이벤트: 19580개
```

→ **회사 PC 에서도 등록 스크립트를 다시 돌려야 이 설정이 먹는다.**
이미 등록해 뒀다면 `toolsegister_etf_task.bat` 를 한 번 더 실행하면 된다(`/F` 로 덮어씀).

로그 파일 두 개(`data/etf_refresh.log` · `data/ddr5_collect.log`)는 `.gitignore` 로 뺐다.

**검증**: pytest 267개 통과. 두 작업 모두 `Ready`, 다음 실행 8/10(월) 18:00·18:30.

### 남은 것 (다음에 이어서)
- 관심 플랜 탭에 **환율 변동 시나리오**(미국 종목 원화 환산) 넣을지 검토
- 국내 위클리 분배 ETF 가 생기면 캘린더 예측 주기 확인


---

## 29단계: 관심 플랜 — 환율 시나리오 (2026-08-08)

미국 종목의 원화 금액은 **수집 당시 환율**로 굳어 있었다. 수집 환율 1,423.48 원인데
지금은 1,407.45 원 — 이미 1.13% 벌어져 있었다.

### 넣은 것 (⭐ 관심 플랜 탭 · 💱 환율 시나리오)
- **지금 환율 받기** 버튼 (yfinance `KRW=X`)
- 슬라이더로 가정 환율 (수집 시점 ±25%)
- 바꾸면 표·KPI·월별 현금흐름·지급 일정의 **미국 종목 금액만** 다시 계산된다

### 핵심 — 분배율(%)은 환율과 무관하다
환율이 오르면 원화로 받는 분배금이 늘지만 **살 때 원금도 같이 늘어서** 비율은 그대로다.
`apply_fx` 는 금액 칸(현재가·투자금액·연분배금·세전·세후)만 곱하고 **% 칸은 손대지 않는다.**
`test_watch_fx.py` 의 `test_분배율은_환율과_무관하다` 가 이걸 못 박는다.

실제 확인 (SCHD 10주, 환율 1,423 → 1,600 가정):

| | 투자금액 | 예상연분배금 | 연분배율 |
|---|---|---|---|
| SCHD (미국) | 478,859 → **538,240** | 14,918 → **16,768** | 3.115% → **3.115%** |
| KODEX 200 (국내) | 980,900 (그대로) | 8,490 (그대로) | 0.866% |

`schedule()` 결과에 `시장` 칸을 추가했다 — 지급 일정 금액에도 같은 환율을 먹이려면 필요했다.

**검증**: pytest **273개 통과** (신규 `test_watch_fx.py` 6개).

### 남은 것
- 국내 위클리 분배 ETF 가 생기면 캘린더 예측 주기 확인
- (선택) 환율까지 반영한 **총수익 시나리오** — 지금은 분배금만 환산하고 평가금액 변동은 안 본다

---

## 30단계-앞: 이 PC 스케줄러 등록 + 밀린 데이터 갱신 (2026-08-13)

28단계가 "회사 PC 에서도 등록 스크립트를 다시 돌려야 이 설정이 먹는다"고 남겨 둔 것.
이 PC 를 확인해 보니 **`ETF-refresh-daily` 가 아예 없었다**(DDR5 만 있었다).

```
ETF-DDR5-daily      Ready   마지막 8/12 18:00 성공
ETF-refresh-daily   (없음)  ← 미등록
```

그래서 국내 데이터가 **8/11 에 멈춰** 있었다. `tools\register_etf_task.bat` 를 돌려
등록했고, 배터리 설정이 제대로 붙었는지 확인했다:

| 설정 | 값 |
|---|---|
| `DisallowStartIfOnBatteries` | False |
| `StopIfGoingOnBatteries` | False |
| `StartWhenAvailable` | True |
| 상태 / 다음 실행 | `Ready` / 평일 18:30 |

밀린 갱신을 한 번 돌려 **KRX 8/12 분까지** 채웠다(전체 3,297종목).

> **다른 PC 를 새로 쓸 때 확인할 것**: `Get-ScheduledTask -TaskName "ETF-*"` 로
> 두 작업(`ETF-refresh-daily`·`ETF-DDR5-daily`)이 다 `Ready` 인지 본다.
> 없으면 `tools\register_etf_task.bat` 과 `tools\register_ddr5_task.bat` 를 돌린다.
> 훅도 마찬가지다 → `tools\install_hooks.bat` (30단계-보완).

---

## 30단계: 분배금 변동계수 — ROC 대용 신호 (2026-08-13)

17단계부터 두 번 이월된 것. **원금 반환(ROC)** 비중은 19a-1·8937 공시가 종목마다
PDF 라 값으로 받아올 수 없다 → 대신 **회차별 분배금이 얼마나 들쭉날쭉한가**를 잰다.
벌어서 주는 돈(이자·배당)은 회차마다 비슷하고, 원금을 헐거나 옵션 프리미엄에
기대는 돈은 출렁인다. **추가 수집 0** — 이미 있는 분배 이력으로 계산했다.

`분배변동계수` = 최근 12개월 주당분배금의 **표준편차 ÷ 평균 (%)**, 3회 미만은 비운다.
평균으로 나누므로 금액 크기·통화가 약분된다 → 원화 환산된 미국분도 같은 자로 잰다.

### 곁가지 — 미국분이 다른 12개월을 보고 있었다
처음엔 종목마다 **자기 마지막 지급일**부터 12개월을 셌다. 그랬더니 반년 전에
마지막으로 준 종목은 창이 과거로 밀려, `지급횟수_12개월`(us_etf 가 **수집일** 기준으로
낸 값)과 다른 구간을 봤다 — **'반기(2회)인데 변동계수가 있다'** 가 130개 나왔다.
3,516개 중 2,373개가 마지막 지급이 30일 넘게 지난 종목이라 남 얘기가 아니었다.
미국은 공통 기준일(이력의 마지막 배당락일 ≒ 수집일)로 잘라 맞췄다. 국내는
`classify()` 가 주기 판정과 이 값에 **같은 `recent`** 를 써서 원래 문제가 없다.

### 결과 — 주기마다 정상 범위가 딴판이다
| 주기 | 채워진 수 | 중앙값 |
|---|---|---|
| 월 | 832 | **8.2%** |
| 분기 | 1,023 | **31.2%** |
| 위클리 | 68 | **71.5%** |

반기·연은 12개월에 2회 이하라 전부 비었다(1,923/3,297 채움). 그래서 **등급 라벨을
만들지 않았다** — 절대 임계값 하나로 자르면 '위클리는 전부 위험'이 된다. 숫자와
주기별 중앙값을 툴팁에 같이 적어 **같은 주기끼리 비교**하게 했다.

**연분배율과는 별개 축이다.** 월배당 안에서 연분배율 상위 20%의 CV 중앙값 9.8 vs
하위 20% 11.5 — 고배당이라고 더 출렁이지 않는다. 즉 이 값은 연분배율이 못 주는
정보를 더한다(둘을 **함께** 봐야 함정이 보인다). CV 최상위는 크립토·레버리지
ETF(ProShares Ultra Ether 290.8%, Ultra Bitcoin 258.9%)였다.

### 넣은 곳
- `classify.variation_coefficient()` — 국내는 `classify()` 안에서, 미국은
  `us_variation_coefficients()` 가 `us_배당_*.csv` 에서 계산
- 📋 종목 목록 표 `분배변동` 칸 (선택 컬럼) + 해석 툴팁
- ⭐ 관심 플랜 표 `분배변동` 칸 — 이 값이 크면 '예상연분배금'을 그대로 믿기 어렵다.
  `apply_fx` 는 금액 칸 화이트리스트만 곱하므로 % 칸인 이 값은 환율에 안 흔들린다

**검증**: pytest **282개 통과** (신규 `test_variation.py` 9개). 미분배 203종목 유지 —
17·19단계에서 두 번 터졌던 `reindex`/`drop(errors="ignore")` 자리 무사.
Streamlit AppTest 렌더 예외 0.

### 남은 것
- **금융소득종합과세 2,000만원 계산** — 지금도 경고 문구만, 계산 없음 (추가 수집 0)
- 국내 위클리 분배 ETF 가 생기면 캘린더 예측 주기 확인
- (선택) 환율까지 반영한 총수익 시나리오

---

## 30단계-보완: DRM 방어를 푸시 시점까지 (2026-08-13)

`pre-commit` 만으로는 **구조적으로** 못 막는다는 지적을 받고 구멍 셋을 확인했다.
1. `git commit --no-verify` 로 우회된다
2. **훅이 안 깔린 다른 PC에서 만들어져 들어온 커밋**은 애초에 커밋 훅을 안 거친다
   — 16단계 사고가 정확히 이 모양이었다(커밋한 PC와 파일이 안 열려 터진 PC가 다르다)
3. `core.hooksPath` 는 clone 마다 수동 설정이라 새 PC에서 빠진다

### 고친 것
- `check_drm.py --push`: 작업트리가 아니라 **올라가는 커밋 안의 blob** 을 본다.
  경로가 아니라 **blob SHA** 로 내용을 꺼낸다 — `sha:경로` 로 꺼내면 한글 경로
  인코딩에 걸려 검사가 조용히 새기 때문(이 저장소는 산출물 경로가 전부 한글이다)
- `.githooks/pre-push` 신규
- `tools/install_hooks.bat`: 새 PC에서 한 번 돌리면 훅이 켜진다

**실제 확인**: `--no-verify` 로 DRM 파일을 커밋한 뒤 push 시도 → 차단됨.
HEAD 트리 148개 파일 전수 검사 → DRM 봉투 없음.

**아직 남은 구멍**: 로컬 훅은 `--no-verify` 로 push 도 넘길 수 있다. 그것까지 막으려면
**서버 쪽 검사**(GitHub Actions)가 필요하다 — 아직 안 넣었다.

**검증**: pytest 290개 통과 (신규 `test_check_drm.py` 8개).

---

## 31단계: 매수 전 5분 루틴 — 필터로 후보군 줄이기 (2026-08-13)

사용자가 손으로 쓰던 **매수 전 5분 루틴**(사진)을 대시보드에 넣었다.
요구는 "ETF 세부 특징 항목에 필터를 걸어 풀을 줄이는 것".

### 루틴 5개 항목 — 낼 수 있는 것만 냈다
| 루틴 항목 | 여기서 | 왜 |
|---|---|---|
| ① Coverage ≥ 1.0 | ②에 **합침** | 아래 설명 |
| ② NAV 12개월 잠식 -10% 이내 | 1년 **가격** 수익률 | 2,829/3,297 채움. KRX NAV 직접 조회는 종목별 API라 전 종목은 무겁다 |
| ③ 이익초과분배 반복 | **자료 없음** | 세이브로 배당구분이 `이익분배`(5,449)·`청산분배`(92) 2종뿐. 신규 수집 필요 |
| ④ 특별분배 빼고 재계산 | **중앙값 분배율** 신규 | 추가 수집 0 |
| ⑤ 커버드콜 천장 | 점수 없이 '직접 확인' | 데이터로 낼 수 없다 |

**①을 ②에 합친 이유.** 진짜 Coverage(벌어들인 순수익 ÷ 분배금)는 공시 자료가 없다.
우리 데이터로 만들 수 있는 대용은 `총수익률 ÷ 분배율` 인데, 총수익률 = 가격변화 +
분배율 이므로 이 값이 1 이상이라는 건 **가격변화 ≥ 0** 과 같은 말이 된다. ②도 가격이
얼마나 빠졌나를 보는 값이라, 따로 두면 **같은 조건을 두 번 거는 셈**이다. 하나의 축으로
두고 임계값만 나눴다(0% 이상 +1 / -10% 까지 0 / 그 아래 -1).

### 중앙값 분배율 — 특별분배 착시를 걷어낸다
`중앙값연분배율` = **회차 금액의 중앙값** × 지급 횟수 ÷ 현재가. 한 회차만 크게 준
특별분배는 합계(연분배율)를 부풀리지만 중앙값은 못 흔든다. 국내는 `classify()`,
미국은 `us_dividend_stats()` 가 배당 이력에서 낸다. **추가 수집 0.**

전체 종목의 중앙값 괴리는 0.0%(절반이 특별분배 없음), 상위 10%가 24% 이상.
가장 부푼 쪽은 레버리지·크립토였다:

| 종목 | 표시 분배율 | 평상(중앙값) |
|---|---|---|
| Direxion Daily Magnificent 7 Bull 2X | 9.33% | **0.18%** |
| ProShares Ether ETF | 72.07% | **1.50%** |

### 핵심 규칙 — 자료 없음을 통과로 치지 않는다
필터 통과 = 점수 **0 이상**이고, **값이 없는 종목(NA)도 떨어진다.** 빈칸을 0으로
읽거나 NA를 통과로 두면 정작 걸러야 할 종목이 그대로 남는다. `test_routine.py` 의
`test_자료가_하나도_없으면_총점_0이지만_통과는_아니다` 가 이걸 못 박는다.
같은 이유로 분배 안정성은 **주기별 기준**(월 8.2 / 분기 31.2 / 위클리 71.5)을 쓴다 —
절대 임계값 하나면 위클리가 통째로 탈락한다.

### 넣은 곳
- `src/routine.py` 신규 — 항목별 판정(+1/0/-1) · 근거 · 해석, 표 전체 채점, 필터
- 사이드바 **✅ 매수 전 5분 루틴으로 줄이기** — 체크할 때마다 `3,297 → n` 표시
- **✅ 5분 루틴** 탭 신규 — 종목 하나를 골라 판정 카드 5장(사진의 팩터 판정 레이아웃)
- 📋 종목 목록에 `평상분배율` · `루틴` 칸 추가

**풀 축소 확인**: 3,297 → 세 항목 다 통과 **1,200** → 커버드콜까지 빼면 **1,155**.

**검증**: pytest **307개 통과** (신규 `test_routine.py` 17개). AppTest 렌더 예외 0, 탭 16개.

### 남은 것 (다음에 이어서)
- **대안 비교 탭** — 체크박스로 최대 4개 골라 나란히 비교 + 뷰 전환(개요/비용·규모/
  성과/리스크/배당) + 성장 오버레이 차트. 사용자가 이어서 하기로 한 것
- 금융소득종합과세 2,000만원 계산

---

## 31단계-조사: 이익초과분배 자료를 찾아 나섰다 (2026-08-13)

"자료가 없다"로 끝내지 말라는 지시를 받고 **다섯 경로를 실제로 찔러 봤다.**
전부 없었다. 같은 길을 다시 파지 않도록 결과를 남긴다.

| 경로 | 확인 방법 | 결과 |
|---|---|---|
| 세이브로 분배금지급현황 | 실제 요청 1회, 응답 태그 전수 출력 | **태그 12개뿐, 전부 이미 씀.** 버리는 필드 없음 |
| KRX Open API `etp/etf_bydd_trd` | 실제 요청, 필드 전수 출력 | 시세·NAV·순자산·상장주식수만. **분배 관련 필드 0** |
| stockanalysis.com 배당 페이지 | SCHD 응답 본문 검색 | `Return of Capital`·`ROC`·`Capital Gain` **0회** |
| 과표기준가 **절대값** vs NAV | 960종목 비율 분포 | **틀린 접근.** 국내 주식형은 매매차익이 비과세라 과표기준가가 원래 NAV보다 훨씬 낮다(KODEX 200 비율 0.077). 이익초과분배가 아니라 과세구조 차이를 잰 것 |
| 과표기준가 **회차별 변화** | 리츠·커버드콜·배당성장 시계열 | **수학적으로 분리 불가** (아래) |

### 과표기준가로는 왜 안 되나
`과표변화(t) = 기간 과세소득(t) − 과세분배(t−1)` 인데 **미지수가 둘**이다.
TIGER 리츠부동산인프라는 매달 33원을 주는데 과표 변화가 -51 / -18 / -9 / -14 / -6 / **+1.5** 로
들쭉날쭉하다. 과세분배가 33이고 소득이 있었던 건지, 과세분배가 0(전액 이익초과)이고
손실이 났던 건지 이 자료만으로는 갈라낼 수 없다. 1년치를 합쳐도 마찬가지다 —
**분배를 전액 이익으로 하든 전액 원금으로 하든 과표 변화는 0 근처로 같다.**

### 남은 진짜 경로 — 운용사 공시 (원 출처)
`robots.txt` 를 먼저 확인했다(20단계의 "공식 경로만, 우회 안 함" 원칙).
- `kodex.com` → `allow: /etf` (허용)
- `tigeretf.com` → `Allow: /tigeretf/` (허용)

다만 두 사이트 모두 **SPA** 라 정적 HTML 에는 값이 없다(KODEX 상품 페이지에
`이익초과` 0회). 운용사마다 내부 API 를 따로 찾아야 하고 **20곳이 넘는다.**
한 단계를 통째로 써야 하는 일이라, 착수 전에 사용자 판단을 받기로 했다.

미국(19a-1)은 종목마다 PDF 라 더 어렵다.

### 그래서 지금 화면은
5분 루틴의 ③은 **'수집 안 됨'을 그대로 표시**한다. 못 채운 것을 채운 척하지 않는
편이 낫다 — 그럴듯한 대용값을 넣으면 걸러야 할 종목이 통과한다.
부분 대용은 **분배 안정성**(30단계 분배변동계수)이다.

### 정정 — 세이브로에 더 있었다 (같은 날, 브라우저로 재조사)
"ETF는 분배 관련 세이브로가 제일 많지 않나"는 지적을 받고 다시 봤다.
**내가 화면 하나만 보고 '세이브로에 없다'고 한 것이 틀렸다.** 브라우저로 메뉴를
훑으니 ETF 메뉴에는 분배 화면이 하나뿐이지만, **펀드 메뉴에 더 자세한 화면**이 있었다.

`펀드 > 권리행사정보 > 펀드별분배금지급내역` (`BIP_CNTS05008V`, menuNo=152)
- action `exerFundByDtrPaySkedulPList` / task `ksd.safe.bip.cnts.Fund.process.FundExerPTask`
- **ISIN 으로 조회**한다 → 우리 데이터에 ISIN 이 전부 있어 바로 쓸 수 있다
- 화면 정의 XML(`/IPORTAL/user/fund/BIP_CNTS05008V.xml`)에 action·task·파라미터가
  그대로 적혀 있다. **다음에 세이브로 화면을 뚫을 땐 이 XML 부터 받으면 된다**

**우리가 안 쓰던 필드 5개**가 여기 있다:

| 필드 | 뜻 | 예 |
|---|---|---|
| `TAX_TPNM` | **세금구분** | 현금(배당소득) |
| `FIX_TPNM` | 배당확정여부 | 확정 |
| `ALOC_WHNM` | 현금배당방법 | 정액 |
| `TOT_DIV_PAY_AMT` | **총분배금**(종목 전체 지급액) | 12,338,700,000 |
| `PAY_TERM` | 지급기간 | 2026/08/04 ~ 2026/08/04 |

### 그런데 세금구분도 값이 하나였다
`TAX_TPNM` 이 이익초과분배를 가려 줄 것으로 봤는데, **370종목 2,165건을 훑어도
전부 `현금(배당소득)`** 이었다(무작위 100종목 364건 + 리츠·인프라·부동산·채권·상품
270종목 1,801건 — 이익초과분배가 나온다면 여기서 나와야 한다).
`RGT_RSN_DTAIL_SORT_NM` 도 이익분배/청산분배 2종 그대로였다.

**이게 무슨 뜻인지는 단정할 수 없다.** 국내 상장 ETF 에 실제로 이익초과분배가
없어서일 수도 있고, 이 필드가 그렇게까지 세분해 채워지지 않아서일 수도 있다 —
지금 자료로는 둘을 구분 못 한다.

한 가지는 분명해졌다: 루틴 ③이 겨냥한 **ROC 는 본래 미국 ETF 문제**(19a-1)이고,
고배당 함정 종목이 미국에 몰려 있는 것과 앞뒤가 맞는다. 국내 쪽은 세이브로 기록상
전부 배당소득 과세 분배다.

### 아직 안 가져온 것
`TOT_DIV_PAY_AMT`(총분배금)는 지금 우리가 안 받는 **진짜 새 값**이다. 넣을지는
다음 단계에서 정한다 — 종목당 1회 요청이라 국내 1,194종목이면 한 판 더 돌려야 한다.

---

## 32단계: 이익초과분배(ROC) — 진짜 값을 받아 왔다 (2026-08-13)

31단계에서 '자료 없음'으로 남긴 루틴 ③을 채웠다. **국내와 미국의 답이 달랐다.**

### ① 국내 — 전수 확인 결과 '해당 사례 없음' (`src/fund_dist.py` 신규)
세이브로 `펀드별분배금지급내역`을 국내 ETF **1,052종목 5,541건 전수** 수집했다
(collect.py 와 같은 건수 = 같은 모집단). 결과:

| 칸 | 값 종류 |
|---|---|
| 세금구분(`TAX_TPNM`) | **1종 — `현금(배당소득)` 5,541건 전부** |
| 배당확정여부 | 1종 — 확정 |
| 현금배당방법 | 1종 — 정액 |
| 배당구분 | 2종 — 이익분배 5,449 / 청산분배 92 |

표본이 아니라 **전수**라 이제 확실하다. 국내 ETF 분배는 세이브로 기록상 전부
배당소득 과세 분배다 → 루틴 ③은 국내에서 판정할 것이 없다. 화면도 그렇게 말한다.

곁가지 둘:
- `결산기준가`(`SETACC_STDPRC`)는 ETF 에서 **전부 `0.00`** 이다. 못 쓴다.
- `총분배금`(`TOT_DIV_PAY_AMT`)은 채워진다. **총분배금 ÷ 주당분배금 = 분배 시점
  상장주식수** 라, 2년치 주식수 시계열이 공짜로 생긴다(수급 탭은 지금 59일치뿐).
  아직 안 붙였다.

### ② 미국 — 19a-1 공시에서 받아 온다 (`src/roc.py` 신규)
ROC 는 본래 미국 문제였다. Global X 종목 페이지에 **19a Notice 가 `.docx` 로**
걸려 있다. docx 는 zip+XML 이라 추가 의존성 없이 읽힌다.

**표를 칸 단위로 읽는다.** 글자만 뽑으면 워드가 숫자를 쪼개 담아서
`$0. 1 775` · `9 9.29 %` 처럼 나오고, 그걸 다시 자르다 `0.1` 로 잘못 읽기 쉽다.

받아 온 값 (회계연도 누적 원금반환 비율):

| 티커 | 이번 회차 | 누적 |
|---|---|---|
| MLPA | 100.00% | **100.00%** |
| QYLD | 100.00% | **99.29%** |
| QYLG | 99.98% | 96.05% |
| XYLD | 97.97% | 94.70% |
| RYLD | 81.52% | 92.62% |
| DJIA | 100.00% | 87.88% |
| DIV | 51.98% | 38.05% |
| CLIP | 0.00% | 4.72% |

**QYLD 분배금의 99.29%가 원금 반환이다.** 30단계 분배변동계수가 대용으로 잡으려던
바로 그것을, 이제 공시값으로 본다.

### 커버리지는 정직하게 적는다
Global X 44종목 중 **19a 공시가 있는 12종목**만 값이 있다(없는 32종목은 ROC 가
없어 통지할 일이 없는 것으로 보이나, 단정하지 않고 빈칸으로 둔다).

`robots.txt` 를 먼저 확인했다(20단계 원칙). **고배당이 몰린 곳이 하필 막혀 있다**:

| 발행사 | 연분배율 15%+ 종목 | robots |
|---|---|---|
| YieldMax | 37 | `/wp-content/uploads/docs/` **금지** |
| Roundhill | 17 | 전면 허용 — 다음에 붙일 수 있다 |
| Defiance | 12 | `/wp-content/uploads/funddocs/` **금지** |
| Rex | 8 | 허용(`Crawl-delay: 10`) |
| GraniteShares | 8 | 허용(ClaudeBot 명시 허용) |

YieldMax·Defiance 는 **받지 않는다.** 화면 도움말에도 그 이유를 적었다.

### 화면
- 루틴 ③이 점수 항목이 됐다 → 루틴 점수가 **-3~+3 → -4~+4**
- 판정선: 누적 ROC 20% 이하 +1 / 50% 이하 0 / 그 위 -1
  (분배금 절반 넘게가 원금이면 '분배금'이라 부르기 어렵다)
- 📋 종목 목록에 `원금반환` 칸, 사이드바에 **원금 반환이 절반 미만** 필터
- 필터는 여전히 **공시 못 받은 종목을 떨어뜨린다** — 모르는 것을 통과로 치면
  YieldMax 같은 미수집 종목이 그대로 남는다(`test_ROC_필터는_공시_없는_종목을_떨어뜨린다`)

### 곁가지 — 내가 낸 버그
`roc.py` 첫 실행이 44종목 전부 실패했다. **User-Agent 에 한글을 넣어서**
HTTP 헤더 인코딩(latin-1)이 깨진 것이었다. 예외를 삼키고 있어서 원인이 안 보였다.

**검증**: pytest **312개 통과** (`test_routine.py` 17→22개). AppTest 렌더 예외 0.

### 남은 것
- Roundhill·Rex·GraniteShares 19a 붙이기 (허용 확인 완료, 형식 확인 필요)
- `총분배금` → 분배 시점 상장주식수 2년 시계열 (수급 탭 보강)

---

## 33단계: ⚖ 대안 비교 탭 (2026-08-13)

사진 3(etflab '대안 비교')을 우리 데이터로 옮겼다. **네트워크를 쓰지 않는다** —
국내 전용인 `NAV 비교표`(KRX 조회)와 달리 국내·미국을 **한 표에서** 견주려면
두 시장이 공통으로 가진 값만 써야 한다.

### 화면 (`src/compare.py` + `render_compare`)
- **뷰 전환**: 개요 / 비용·규모 / 성과 / 리스크 / 배당 — 고른 기준에 맞춰 그리드 칸이 바뀐다
- **후보 그리드**: 체크박스로 최대 4개. 앞 300개만 올리고, 더 좁히려면 사이드바 필터
- **나란히 비교**: 지표를 행, 종목을 열로 돌려놓는다(사람이 읽는 방향).
  지표마다 유리한 쪽에 `◆`. **총보수·분배변동·원금반환은 낮을수록** 유리로 봤고,
  베타·괴리율·주기처럼 좋고 나쁨을 못 따지는 값에는 안 붙인다
- **구간별 수익률 막대**: YTD/1년/3년/5년

### 핵심은 겹침이다
이미 가진 것과 사실상 같은 ETF 로 갈아타면 비용만 들고 분산은 그대로다.
그래서 후보마다 **내 보유종목 중 가장 많이 겹치는 것**과의 겹침(%)을 보여 준다.
평균이 아니라 **최댓값**이다 — 열 개 중 하나와 90% 겹치면 그것만으로 새로 살 이유가
흔들리는데, 평균을 내면 그 하나가 묻힌다.

⚠ **분류로 잰 근사치이지 실제 구성종목 겹침이 아니다.** 구성종목(PDF)을 아직
수집하지 않았다 — 세이브로 `ETF종합정보 > 보유자산명세` 에 자료가 있으니 나중에
받으면 진짜 겹침으로 바꿀 수 있다. 화면에도 그렇게 적었다.

### 무게를 다시 잡은 이유
처음엔 지역·섹터를 같은 무게(3.0)로 뒀는데 `KODEX 200` 과 `KODEX 반도체` 가
**70%** 로 나왔다. 같은 시장·같은 자산군이면 지역·자산·시장 축이 자동으로 다 맞아
겹침의 **바닥값이 높아지기** 때문이다. 섹터 무게를 5.0 으로 올렸다:

| 두 종목 | 전 | 후 |
|---|---|---|
| KODEX 200 ↔ TIGER 200 (같은 지수) | 100% | **100%** |
| KODEX 반도체 ↔ TIGER 반도체 | — | **100%** |
| KODEX 200 ↔ KODEX 반도체 | 70% | **52.4%** |
| KODEX 200 ↔ KODEX 미국배당다우존스 | — | **23.8%** |

경고선은 80%. 빈 축은 **분모에서 뺀다** — 모르는 축을 '다르다'로 세면 자료가
부실한 종목이 무조건 안 겹치는 것처럼 보인다.
`'nan'`·`'None'` 문자열을 값으로 세지 않는 것도 같은 이유다(엑셀을 거치며 들어온다).

**보유종목이 없으면 겹침은 빈칸**이다. 0 으로 채우면 '안 겹친다'는 뜻이 돼서
확인 안 한 것과 구분이 안 된다.

### 성장 오버레이를 막대로 바꾼 이유
사진은 '기준 100 겹쳐 그리기'인데, 그러려면 **종목별 일별 시세**가 있어야 한다.
국내는 KRX 종목별 조회, 미국은 야후 — 둘 다 네트워크를 타야 해서 이 탭의 성격
(즉시 비교)과 안 맞는다. 이미 가진 구간 수익률(YTD·1년·3년·5년)로 막대를 그렸고,
**분배금이 빠진 가격 기준**이라는 것을 캡션에 적었다.

**검증**: pytest **330개 통과** (신규 `test_compare.py` 18개). AppTest 렌더 예외 0, 탭 17개.

### 남은 것
- 구성종목(보유자산명세) 수집 → 겹침을 **진짜 보유종목 기준**으로
- Roundhill·Rex·GraniteShares 19a
- `총분배금` → 분배 시점 상장주식수 2년 시계열

---

## 34단계: 장기 상장주식수 — 분배 이력에서 되짚었다 (2026-08-13)

32단계가 곁가지로 남긴 것(`총분배금 ÷ 주당분배금 = 상장주식수`)을 실제로 붙였다.
**추가 수집 0** — 오늘 받아 둔 `seibro_분배상세_20260813.csv` 만 쓴다.

### 왜 필요한가
수급 탭의 날 단위 캐시는 **쌓기 시작한 날부터**라 지금 60일치뿐이다. 그런데
설정·환매는 몇 달 단위로 방향이 갈리는 값이라 60일로는 안 보인다. 실제로
KODEX 200 은 **60일로 +7.78%, 2년으로 +31.7%** 다 — 같은 종목이 창에 따라 딴판이다.

### ⭐ 날짜를 하루 미뤄 찍는다 (실측으로 확정)
되짚은 값이 KRX 상장주식수의 **어느 날**에 해당하는지 캐시와 겹치는 826건으로 재 봤다:

| 찍는 자리 | 일치 |
|---|---|
| 기준일 −1거래일 | 698/778 (89.7%) |
| **기준일 당일** | 782/826 (**94.7%**) |
| **기준일 +1거래일** | 826/826 (**100.0%**) |
| 기준일 +2거래일 | 611/826 (74.0%) |

기준일 마감 주주명부가 KRX 상장주식수에 **다음 세션으로** 반영된다. 당일로 찍으면
44건이 어긋나는데, 그중 38건이 정확히 +1 에서 맞았다. 그래서 `_next_sessions()` 가
XKRX 로 다음 개장일에 찍는다 — 일별 차트와 한 화면에 겹쳐도 안 어긋난다.

`test_실데이터가_KRX_캐시와_같은_값이다` 가 이 보정을 못 박는다. 지우면 깨진다.

### 결과
`총분배금` 은 5,541건 **전부** 채워져 있었고 나눗셈이 **100% 정수로 떨어졌다**
(오차 중앙값 0.000000). 청산분배 92건을 빼고 5,449점 / **994종목**,
기간 **2024-08-16 ~ 2026-08-03**. 이 중 **655종목**이 점 2개 이상이라 증감을 낼 수 있다.

### 넣은 곳 (`src/flow/long_shares.py` 신규)
- 🌊 수급 요약 표에 **`2년 증감(%)`·`2년 점`** 칸
- 상세 아래 **장기 흐름 차트**(분배일마다 점) + 시작·끝 주식수와 증감률 캡션
- 일별 캐시에 없는 종목도 **조기 return 하지 않고** 장기 흐름은 보여준다
  (상폐·오래된 종목이 여기 걸린다 — 되짚은 자료는 있을 수 있다)

### 한계 (화면에도 적었다)
- **분배가 있던 날에만 점이 있다.** 연 1회 분배면 2년에 점 두세 개라 추세가 아니라
  두 시점 비교로만 읽어야 한다 → 그래서 `2년 점` 을 나란히 보여준다
- 점이 1개면 증감을 **비운다**(0 이 아니다). 한 점으로 늘었다/줄었다는 말이 안 된다
- 분배 이력이 없는 종목(미분배·신규상장)은 아예 안 나온다. **국내 전용**
- 신규 상장 커버드콜은 첫 회차 기반이 작아 증감률이 폭발한다
  (RISE 미국AI밸류체인데일리고정커버드콜 +7,127%) — 절대 증감도 같이 봐야 한다

**검증**: pytest **346개 통과** (신규 `test_long_shares.py` 16개). AppTest 렌더 예외 0, 탭 17개.

### 34단계-보완: 첫 화면이 23초 느려졌던 것 (같은 날 저녁)
전체 pytest 에서 `test_dashboard_exposes_working_nav_analysis_controls` 가
**120초 타임아웃**으로 깨졌다. 재 보니 `load()` 가 **22.9초**였다 — `derive()` 안의
`exchange_calendars` XKRX 달력 짓기가 거의 전부였다. 테스트만의 문제가 아니라
**대시보드 첫 화면에 23초를 얹은 것**이라 진짜 회귀였다.

결과는 5,449줄짜리 작은 표다 → **CSV 로 캐시**한다
(`data/flow_cache/장기주식수_{원본파일명}.csv`, 이미 gitignore 되는 자리).
**22.9초 → 0.1초.** 전체 pytest 도 534초 → 350초로 줄었다.

캐시가 낡은 값을 물지 않게 두 가지를 건다:
- 캐시가 **원본보다 오래됐으면 버린다**(원본을 고쳤을 때)
- 캐시 이름에 **원본 파일명**을 넣는다(새로 수집하면 파일명이 바뀌니 자연히 갈린다)

`test_long_shares.py` 에 캐시 테스트 5개(16 → 21개). 날짜가 글자로 돌아오면
`growth()` 의 창 자르기가 깨지므로 **datetime 으로 돌아오는지도** 못 박았다.

### 못 한 것 — 구성종목 수집 (세이브로 다운)
원래 이 세션에서 하려던 **1순위는 구성종목(보유자산명세)** 이었다. 18:20 기준
**세이브로 전체가 오류 페이지**(루트 포함, 545바이트 안내)라 착수를 못 했다.
우리 네트워크 문제가 아니다 — 외부 경로로 받아도 같은 오류였다.
16:32 에는 정상이었으니 저녁 점검으로 보인다. **다음 세션에서 살아 있으면 그것부터.**

### 남은 것
- **구성종목(보유자산명세) 수집 → 진짜 겹침** (세이브로 복구 뒤 1순위)
- Roundhill·Rex·GraniteShares 19a
- 금융소득종합과세 2,000만원 계산

---

## 35단계: 19a 발행사 셋 추가 — Roundhill·Rex·GraniteShares (2026-08-13)

**ROC 커버리지 12 → 43종목.** 32단계가 Global X 만 붙여 두고 남긴 것을 마저 했다.
(회사에서 코드까지 하고 멈췄다가 집에서 수집·테스트를 마쳤다.)

### 발행사마다 공시 자리가 달랐다
| 발행사 | 어디서 | 주는 것 |
|---|---|---|
| Global X (기존) | 종목 페이지의 `.docx` | 이번 회차 **+ 회계연도 누적** |
| **Roundhill** | 종목 페이지 **본문 문구** | 이번 회차만 |
| **Rex** | 종목 페이지 **본문 문구** | 이번 회차만 |
| **GraniteShares** | `/product/{id}/en-us/` JSON | 이번 회차만 |

`robots.txt` 를 넷 다 다시 확인했다(20단계 원칙) — 전부 허용.
GraniteShares 는 `ClaudeBot`·`anthropic-ai` 를 명시 허용한다. Rex 는 `Crawl-delay: 10`
이라 호출 간격을 10초로 뒀다(그래서 20종목에 3분 넘게 걸린다).

**Rex 의 19a 링크는 PDF 로 간다.** 그런데 같은 숫자가 본문에 문장으로 박혀 있어
PDF 를 안 열고 그 문장을 읽는다 — 32단계가 "종목마다 PDF 라 못 받는다"고 접었던 자리다.

### ⭐ 조용히 틀릴 뻔한 것 — GraniteShares 의 id 가 두 개다
종목 페이지에 숫자 id 가 **둘** 있다.
```
getProductId() → 1119   ← API 의 id (맞다)
getId()        → 1164   ← CMS 노드 id
```
처음에 `getId()` 를 썼더니 **TSYY 자리에 ANV(전혀 다른 펀드)의 ROC 0.00** 이 들어왔다.
실제 TSYY 는 **95.96%** 다. 4종목 전부 남의 값이었고, 숫자가 그럴듯해서 표만 봐서는
안 보인다. → 받아 온 JSON 의 `Ticker` 가 요청한 티커와 같은지 **대조하는 가드**를 넣었다.

바로잡은 GraniteShares (전부 YieldBOOST 계열):
SEMY 97.70 · COYY 97.23 · MUYY 96.87 · NVYY 96.20 · TSYY 95.96 · XBTY 95.02

### 판정 기준을 바꿨다 — 누적이 없으면 최근 회차로
새 발행사 셋은 **회계연도 누적을 공시하지 않는다.** 누적이 없다고 판정을 포기하면
정작 고배당이 몰린 이 셋이 통째로 필터를 빠져나간다. 그래서 `excess_distribution` 이
**누적 우선, 없으면 최근 회차**로 판정하고 **무엇으로 판정했는지 화면에 적는다**
(`19a 공시 — 최근 회차 원금반환 97.7%` + "누적보다 거칩니다" 덧말).
없는 값을 이번 회차로 **채워 넣지는 않는다** — `누적_원금반환비율` 칸은 빈 채로 둔다.

### 정규식이 보일러플레이트를 안 문다 (확인함)
Roundhill·Rex 페이지에는 "distributions ... will be treated as a return of capital"
같은 **위험고지 문구가 모든 종목에** 따로 있다. 공시값만 잡도록 `(ROC) of 숫자%` 로
좁혔고, 실제로 갈리는 것을 확인했다 — MAGS·CHAT 은 19a 언급 자체가 없고(진짜 공시 없음),
WEEK 는 보일러플레이트만 있어 안 잡힌다. 값도 종목마다 다르다(NVII 97.70, MULL 92.43 …)
= 상수 문구를 잘못 읽은 것이 아니다.

### 곁가지 — 발행사 하나만 돌리면 나머지가 날아갔다
`main()` 이 받은 것만 저장해서, `roc.py GraniteShares` 를 돌리자 CSV 에 6건만 남고
Global X 12건이 사라졌다. → 이제 **기존 파일과 합쳐서** 저장한다(새 값이 이김).

### 결과 — 고배당일수록 원금 반환이었다
`data/raw/us_roc_20260813.csv` — **43종목**
(Roundhill 19 · Global X 12 · Rex 6 · GraniteShares 6). 대상 104종목 중 43종목에
공시가 있었고, 없는 61종목은 레버리지·인버스처럼 ROC 를 통지할 일이 없는 쪽이다.

| 판정값(누적 없으면 최근 회차) | 종목 수 |
|---|---|
| 20% 이하 (+1) | 4 |
| 20~50% (0) | 2 |
| **50% 초과 (-1)** | **37** |

43종목 중 **37종목이 분배금의 절반 넘게를 원금으로 돌려주고 있다.** 주간 분배
커버드콜(QDTE·XDTE·FEPI·AIPI…)이 거의 전부 100% 다. 30단계 분배변동계수가
대용으로 겨우 짐작하던 것을 이제 공시값으로 본다.

### 화면
- 📋 종목 목록 `원금반환` 칸 도움말을 고쳤다 — **발행사마다 내놓는 것이 다르다**는
  것과 받아 오는 곳 넷을 적었다(전에는 "지금은 Global X 뿐입니다"였다)

**검증**: pytest **368개 통과** (신규 `test_roc_issuers.py` 13개 + `test_routine.py` 4개
+ 34단계-보완 캐시 5개). 새 테스트가 못 박는 것 셋 — 보일러플레이트를 공시값으로
안 읽는다 / GraniteShares 티커 대조로 남의 값을 안 받는다 / 없는 누적을 안 채운다.

### 세이브로 상태 — 화면은 죽고 API 는 살아 있다
22:00 기준 다시 확인했다. 처음엔 "세이브로 전체가 다운"이라고 적었는데 **그 판단이
너무 넓었다.** 실제로는 이렇다:

| 무엇 | 상태 |
|---|---|
| 화면(`control.jsp`·`index.jsp`) | ❌ 오류 페이지 (브라우저로 열어도 같다) |
| 화면 정의 XML (`/IPORTAL/.../*.xml`) | ❌ 545바이트 오류 페이지 |
| **내부 API (`callServletService.jsp`)** | ✅ **정상** (세션 쿠키도 발급된다) |

즉 **이미 action·task 를 아는 화면은 지금도 수집할 수 있다**(collect.py·fund_dist.py).
막힌 것은 **새 화면을 뚫는 일**이다 — 보유자산명세의 action 이름을 알아내려면
화면 XML 이나 브라우저 F12 가 필요한데 둘 다 화면이 살아야 한다.

### 남은 것
- **구성종목(보유자산명세) 수집 → 진짜 겹침** — 세이브로 **화면**이 살아나면
  바로 착수. 브라우저로 그 화면을 열어 요청을 잡는 것이 첫 걸음이다(31단계 방식)
- (선택) YieldMax·Defiance 는 robots.txt 로 막혀 있어 앞으로도 안 받는다

---

## 36단계: 금융소득종합과세 2,000만원 계산 (2026-08-13)

8단계부터 이월된 것. 지금까지 화면에는 **"2,000만원을 넘으면 더 낼 수 있습니다"라는
문구만** 있었다. 숫자가 없으면 그 문구로는 아무 판단도 못 한다. **추가 수집 0** —
이미 있는 보유종목·분배 이력으로 센다. (`src/fin_income.py` 신규)

### 무엇을 세고 무엇을 빼나 — 이게 전부다
| 계좌유형 | 합산 | 왜 |
|---|---|---|
| 일반(위탁) | ✅ | 배당소득으로 15.4% 원천징수되고 합산 대상 |
| 연금저축 · 퇴직연금(IRP/DC) | ❌ | **과세이연** — 받을 때 소득이 아니다 |
| ISA | ❌ | 만기에 **분리과세** — 종합소득에 합산 안 됨 |

미국 직접 상장분은 현지 15% 원천징수와 **별개로 국내 금융소득에는 들어간다**
(조세조약으로 국내에서 더 안 뗄 뿐이다). 일반 계좌에 있으면 센다.

**2,000만원은 세전 기준**이다. 세후로 재면 실제보다 낮게 나와 안심하게 된다.

### 실제 값 (내 보유 기준)
| | 금액 |
|---|---|
| 올해 받은 (일반계좌) | 64,158원 |
| 올해 남은 예상 | 28,665원 |
| **연간 합계** | **92,823원** — 기준까지 19,907,177원 남음 |

계좌별로 **왜 안 세어졌는지**를 표로 같이 보여 준다:
삼성 DC 23,475 · 미래에셋 IRP 15,084 · 미래에셋 ISA 12,130 은 받았지만 **합산 제외**다.
연금·ISA 로 옮겨 둔 것이 그대로 절세로 이어진다는 것이 숫자로 보인다.

### 화면 (💼 내 포트폴리오 → 🧮 금융소득 2천만)
- **다른 금융소득** 입력칸 — 예금 이자·타 증권사 배당은 우리 자료에 없다.
  0 으로 두면 실제보다 적게 나오므로 직접 넣게 했다
- **다른 종합소득 과세표준** 입력칸 — 초과분에 걸리는 한계세율을 잡는 데만 쓴다
- 진행 막대 + 넘었으면 초과액·한계세율·추가세금 어림값

### 세액은 어림값이라고 못 박는다
추가세금 = 초과액 × (한계세율 − 14%) × 1.1. 이미 낸 **소득세 14%** 를 빼고
지방소득세 10% 를 얹은 것이다. 한계세율이 14% 아래(6% 구간)면 **더 낼 것이 없다**
— 음수 세금을 만들지 않는다(`test_한계세율이_14퍼센트_아래면_더_낼_것이_없다`).
실제는 비교과세(종합과세와 분리과세 중 큰 쪽)라 다를 수 있고, 세무 상담을
대신하지 않는다고 화면에 적었다.

**검증**: 신규 `test_fin_income.py` **15개**. 못 박은 것 — 연금·ISA 를 세지 않는다 /
작년 분배와 매수일 이전 분배를 빼낸다 / 계좌유형이 비면 일반으로 본다(세금을
과소평가하지 않는 쪽).

---

## 37단계: '주기 확인' 187건을 상장일로 갈랐다 (2026-08-13)

4·5단계부터 이월된 것. 187종목에 `주기 확인` 딱지가 붙어 있는데 **원인을 추측으로
적고 있었다** — 두 판정의 순위만 보고 "상장한 지 얼마 안 된 것으로 보임"이라고 썼다.
상장일이 이미 수집돼 있는데 추측할 이유가 없다. **추가 수집 0.**

### 갈라 보니 셋이 섞여 있었다
| 사유 | 건수 | 실제 |
|---|---|---|
| **신규상장** | 25 | 12개월치가 안 찼다 → **24종목의 주기를 바로잡았다** |
| **실제 불규칙** | 95 | 상장 10년짜리도 있었다. 신규상장 탓이 전혀 아니다 |
| **상장일 없음** | 67 | 미국 66 · 국내 1. 모르는 것을 신규상장으로 몰지 않는다 |

### ⭐ 왜 신규상장이면 간격 기준이 맞나 (그리고 언제는 아닌가)
12개월 창의 지급 **횟수는 적게 나올 수만 있고 많이 나올 수는 없다.** 상장 7개월인
월배당은 6회밖에 안 잡혀 '분기'가 된다. 그래서 신규상장이면서 간격 기준이
**더 잦을 때만** 그것을 따른다.

| 종목 | 전 | 후 | 근거 |
|---|---|---|---|
| ACE 리츠부동산인프라액티브 | 분기 | **월** | 26-02~07 **매월** 지급 |
| DAISHIN343 오피스리츠플러스 | 분기 | **월** | 4/30·5/29·6/30·7/31 |
| ACE 유럽방산TOP10 | 분기 | **분기(유지)** | 3회·간격 106일 — 간격 기준(반기)이 오히려 과하다 |

25건 중 **24건 교정, 1건은 손대지 않았다.** 덜 잦은 쪽으로는 절대 안 바꾼다.
`분배주기` 값을 실제로 고치므로 캘린더·현금흐름까지 함께 맞아진다 → `.ics` 재생성.

### 곁가지 발견 — 우리 5분류에 **연 3회** 자리가 없다
'실제 불규칙' 95건의 다수는 고장이 아니었다. `TIGER 200 생활소비재`·`KTOP30`·
`HANARO 농업융복합산업` 같은 국내 지수형은 **4·7·10월에만 주는 연 3회** 지급이다.
횟수(3회)로는 분기, 간격(136.5일)으로는 반기 — **둘 다 실제와 다르다.**
위클리/월/분기/반기/연 5분류에 연 3회가 없어서 생기는 일이다.

회차를 실제로 거른 것은 **15/72**뿐이었다(26-07 에 13종목). 처음엔 "1월을 걸렀다"는
쪽으로 봤는데 **틀렸다** — 애초에 1월에 안 주는 종목들이었다. 지급 달 집합을
세어 보고서야 갈렸다. 카테고리를 늘리는 것은 파급이 커서 이번엔 **설명만** 넣었다.

### 넣은 곳
- `classify.refine_cycle_flags()` 신규 — 상장일이 붙은 **뒤에** 돌아야 해서
  `attach_krx_info` 다음, `attach_liquidation_risk` 앞에 둔다
- `비고사유` 칸 신설(`신규상장`/`실제 불규칙`/`상장일 없음`), `비고상세` 는 사유별 문장으로 교체
- `classify.py` 실행 시 원인별 건수와 교정 종목 수를 찍는다

**검증**: 신규 `test_cycle_flags.py` **10개**. 못 박은 것 — 덜 잦은 쪽으로 안 바꾼다 /
오래된 종목을 신규상장 탓으로 안 돌린다 / 상장일이 없으면 주기를 안 고친다 /
딱 12개월이면 신규상장이 아니다.

---

## 38단계-앞: 두 PC 동기화 + 보유자산명세 API 확보 (2026-08-14, 회사 PC)

### 동기화
어젯밤 노트북 커밋 5개(19:11~23:36)를 pull 하기 전에, 이 PC 아침 8시 스케줄러
산출물을 stash 로 보관 → pull → **새 코드 + 오늘 아침 KRX 데이터**로 출력 재생성 →
커밋·푸시. 날린 것 없음. ⚠ 사용자가 "오늘 아침 노트북에서도 커밋했다"고 했는데
원격에 없다 — 노트북 쪽 push 확인 필요.

### 세이브로 장애를 뚫고 보유자산명세 API 확보
어제 저녁부터 세이브로 화면이 오류(545바이트)였는데, 오늘 아침 **분 단위로
오류↔정상을 오가며** 부분 복구됐다. 살아있는 틈에 ETF 메뉴 화면 XML 14개를
받아 두고, 목표였던 **보유자산명세**를 찾았다:

- 화면: `BIP_CNTS06034V` (menuNo=182) — `ETF종합정보 > 보유자산명세`
- task `ksd.safe.bip.cnts.etf.process.EtfCompstInfoPTask`
- action `compstInfoHoldAssetList`(섹터 비중) / `compstInfoHoldAssetDetailList`(**개별 구성종목**)
- 파라미터 ISIN + STD_DT (상세는 STD_DT 필수 — 목록 응답의 STD_DT 를 넣는다)
- KODEX 200 실요청으로 검증: 삼성전자 34.23% 등 실제 값이 내려온다. 한 번에
  10건씩이라 **페이징 확인 필요** (JS 에 START_PAGE/END_PAGE 가 주석으로 있다)

### 곁가지 — 회사 PC 함정 둘
- **SSL**: 사내 프록시가 인증서를 바꿔쳐 파이썬 requests 가
  `CERTIFICATE_VERIFY_FAILED` 로 죽는다. venv 에 `truststore` 설치,
  `import truststore; truststore.inject_into_ssl()` 을 requests 앞에 넣으면 된다
- **DRM**: 아침에 재생성한 `분배ETF목록.xlsx` 를 회사 DRM(DRMONE)이 한 시간쯤 뒤
  **백그라운드로 감쌌다**. git blob 은 깨끗했다(감싸이기 전에 커밋됨) —
  로컬만 `git checkout -- 파일` 로 복원하면 된다. 16단계 사고와 같은 유형이지만
  이번엔 저장소는 무사. 이 PC 훅은 설치돼 있음(`core.hooksPath = .githooks`)

---

## 38단계: '연3회' 주기 카테고리 — 14종목 (2026-08-14)

37단계가 "카테고리를 늘리는 것은 파급이 커서 설명만 넣었다"며 남긴 것.
국내 지수형의 **4·7·10월 연 3회** 지급은 횟수로는 분기, 간격으로는 반기가 나와
늘 '주기 확인'으로 샜다. 6번째 카테고리 `연3회` 를 만들었다.

### 판정 — 이름이 아니라 지급 달로
`classify.tri_annual_months()`: **두 해 연속 같은 3개 달**에서만 지급했을 때만
인정한다. 한 해만 보면 분기(4회)에서 한 회 빠진 것과 구분이 안 되기 때문.
확정되면 '주기 확인' 비고를 달지 않는다(확인이 끝났으니까).

결과: **14종목** (TIGER 200 생활소비재·TIGER KTOP30·HANARO Fn K-메타버스MZ 등,
전부 4·7·10월). '주기 확인' 187 → **173건** (실제 불규칙 95 → 81).

### 파급 수정 — 여섯 곳
| 파일 | 무엇 |
|---|---|
| `classify.py` | `TRI_CYCLE`·판정·`지급월목록` 칸 신설("4,7,10") · CYCLE_RANK 순위 |
| `calendar_ics.py` | **지급월목록의 달로 예측** — 간격이 3·3·6개월이라 고정 걸음(+N개월)으로는 달이 밀린다. 달 목록이 없으면 이벤트를 안 만든다(틀린 달을 만들지 않게) |
| `watch.py` | 다음 매수마감 예측에 달 목록 전달, VALID_CYCLES 추가 |
| `portfolio.py` | 월별 현금흐름을 지급 달에만 놓는다 (직전 지급 달까지는 건너뜀) |
| `dashboard.py` | 주기 필터·색(보라)·비고 도움말 |
| `routine.py` | 주석만 (연3회 CV 기준은 CV_DEFAULT 30 사용) |

캘린더 이벤트 19,640 → 19,612 — 연3회 종목이 반기 걸음으로 잘못 만들던 예측이
정리된 것.

**검증**: pytest **402개 통과** (신규 `test_tri_cycle.py` 9개). 못 박은 것 —
한 해만 봐서는 연3회로 안 바꾼다 / 4회면 분기다 / 3·3·6 간격이 그대로 나온다 /
달 목록 없으면 예측 안 한다 / 현금흐름이 지급 달에만 놓인다.

---

## 39단계: 환율 총수익 시나리오 — 산 뒤에 움직이면 (2026-08-14)

29단계가 남긴 것("지금은 분배금만 환산하고 평가금액 변동은 안 본다").

### 두 시나리오는 다른 질문이다
| | 질문 | 원금 | 분배율 |
|---|---|---|---|
| apply_fx (기존) | 가정 환율로 **산다**면? | 같이 움직임 | **그대로** |
| fx_total_return (신규) | 사고 **난 뒤** 움직이면? | 오늘 환율로 고정 → **환차손익** | — |

기존 표는 원금과 분배금이 같은 배율로 움직여 환율 효과가 안 보였다. 실제로
걱정되는 것은 "산 뒤에 환율이 빠지면"인데, 그때 원금에 생기는 환차손익이
분배금 변화보다 훨씬 크다 (투자금 200만원 · 분배율 3%면 환율 -10%에
환차손 -20만원 vs 분배금 변화 -6천원).

### 넣은 곳
- `watch.fx_total_return()` — 미국 종목만: 원금 환차손익 + 새 환율 분배금(세후)
  = **1년 총효과**. 미국 종목이 없으면 **None**(0 으로 채우면 '효과 없음'과 구분 불가)
- ⭐ 관심 플랜 · 💱 환율 시나리오 expander 에 지표 3개
  (원금 환차손익 / 연 분배금 세후 / 1년 총효과 — '환율 그대로면' 값과 견주게 도움말)
- 가격 변동은 없다고 가정하고 **환율 효과만 떼어 본다**고 화면에 적었다

**검증**: pytest **405개 통과** (`test_watch_fx.py` 6 → 9개).

---

## 40단계-조사: 구성종목(PDF) 수집 경로 확정 (2026-08-14)

33단계부터 이월된 "진짜 겹침"의 데이터 경로를 넷 다 실제로 확인했다.

| 경로 | 확인 방법 | 결과 |
|---|---|---|
| 세이브로 보유자산명세 | API 실요청 + 페이징 3방식 시험 + **브라우저로 화면 확인** | ✅ 쓸 수 있음 — 단 **TOP 10 만**. 화면에 "PDF 내 보유종목 TOP10에 대한 비중입니다"라고 명시. 페이징이 없는 게 아니라 원래 10건이 전부 |
| KRX Data Marketplace (구 정보데이터시스템) | 브라우저로 PDF 메뉴 진입 | ❌ **로그인 필수** 팝업. 자동 수집에 부적합 (스크립트에 계정을 넣을 수도 없다) |
| data.krx.co.kr getJsonData | — | ❌ **애초에 금지 경로** — 20단계에서 약관 위반으로 접었던 pykrx 스크래핑 그 자리다 (시험 요청도 400) |
| KRX Open API (보유 키) | 카탈로그 확인 | ❌ 시세·매매 통계 위주, 구성종목 API 없음 |

### 확정 — 세이브로 TOP10 + 섹터 비중으로 '실측 겹침 v1'
전체 PDF 를 공식·무료·자동으로 받을 길은 지금 없다. 대신 세이브로가 주는
두 가지로 33단계의 분류 근사치를 크게 개선할 수 있다:
- `compstInfoHoldAssetList` — **FICS 섹터별 비중** (ETF/시장/시장대비)
- `compstInfoHoldAssetDetailList` — **상위 10종목 실제 비중** (KODEX 200 은 TOP10 이 약 70% 커버)

요청 형식·세션·페이징 확인 내용은 38단계-앞 참조. 수집 구현은 다음 세션에서
(국내 1,194종목 × 2요청, fund_dist.py 틀 재사용).

### 곁가지 — 화면 하나로 안 끝난 것
처음 API 가 10건만 줄 때 페이징 파라미터(START_PAGE/END_PAGE·PAGE_NUM) 문제로
봤는데, 화면 정의 XML 의 JS 에도 페이징이 **주석 처리**돼 있었고, 브라우저로 실제
화면을 열어서야 "TOP10 이 전부"라는 안내문을 봤다. **API 응답만 보고 판단하지
말고 화면의 안내문까지 봐야 한다** — 31단계 정정(세이브로에 더 있었다)과 반대
방향의 같은 교훈.

---

## 40단계: 실측 겹침 v1 — 수집 + 대안 비교 연결 (2026-08-14)

40단계-조사에서 확정한 경로를 그대로 구현했다.

### 수집 (`src/compst.py` 신규)
국내 ETF **1,160종목 전수**, 종목당 2요청, **실패 0**:

| 파일 | 행 | 커버리지 |
|---|---|---|
| `seibro_구성섹터_20260814.csv` | 11,600 | 전 종목 (FICS 10섹터 × 1,160) |
| `seibro_구성TOP10_20260814.csv` | 4,219 | **433종목** — 국내 주식형 위주 |

- ② 상세는 STD_DT 필수 → ① 응답의 기준일을 받아 넣는다
- **100종목마다 저장 + 이어받기** — 같은 날짜 CSV 에 있는 ISIN 은 건너뛴다.
  백그라운드 실행이 중간에 끊겨도 다시 돌리면 이어진다
- '자료 없음'도 `구성없음` CSV 에 남긴다 — '못 받음'(재시도 대상)과 구분
- 회사 PC 프록시 때문에 `truststore` 를 requests 앞에 넣었다 (노트북 무해)

### ⭐ 함정 — FICS 섹터 비중은 국내 주식에만 채워진다
**727/1,160 종목이 섹터 비중이 비어 있었다** (채권·해외주식·원자재·통화).
그대로 쓰면 채권 ETF 둘이 '실측 0% 겹침'으로 나온다 — 실제로는 많이 겹치는데.
→ **비중 합 50% 미만은 실측에서 뺀다** (`MIN_SECTOR_COVER`). 모르는 것을 0 으로
세지 않는다는 30·31단계 원칙 그대로. 가드 통과 = **432종목**.

### 겹침 계산 (`compare.py`)
- `real_overlap` — 두 ETF 의 섹터 비중에서 **겹치는 부분(min)의 합** (겹침의 표준 정의).
  섹터 수준이라 같은 섹터의 다른 종목도 겹침으로 본다고 화면에 적었다
- `top10_overlap` — TOP10 실제 보유종목의 min-합 = **확인된 하한**
- `pair_overlap` — 같은 기초지수 100 → 실측 → 분류 근사 순서로 폴백.
  대안 비교 탭의 겹침 칸·경고가 이걸 쓴다 (미국 상장은 자연히 분류 폴백)

실데이터 확인:

| 조합 | 분류(종전) | **실측** | TOP10 하한 |
|---|---|---|---|
| KODEX 200 ↔ TIGER 200 | 100 | **100** (같은 지수) | 71.0 |
| KODEX 200 ↔ KODEX 반도체 | 52.4 | **68.5** | 48.4 |
| KODEX 200 ↔ KODEX 배당가치 | 52.4 | **51.1** | 20.4 |

반도체와의 겹침이 분류보다 **올라갔다** — 코스피200 자체가 삼성전자+하이닉스에
58%+ 쏠려 있어서다. 분류 근사가 못 보던 것을 실측이 보여준다.

**검증**: pytest **414개 통과** (신규 `test_compst_overlap.py` 9개). 못 박은 것 —
min-합 계산 / 자료 없으면 None(0 아님) / 커버 50% 미만은 실측에서 뺀다 /
실측이 분류를 이긴다 / 같은 지수는 언제나 100.

### 남은 것
- 수집 자동화 여부 (지금은 수동 1회분 — 구성은 천천히 변해서 주 1회면 충분해 보인다)
- 미국 상장분 구성종목 — 발행사별 공시라 국내와 별개 조사 필요

---

## 41단계: 종이 톤 디자인 + 화면 요청 4건 (2026-08-14)

### 디자인 — 참고 리포트의 종이 톤
`개인연금ETF포커스_BM점검_FY26.html` 을 기준으로 삼았다 (사용자 지정).
- `.streamlit/config.toml` [theme] + `PAPER_CSS` 주입 — 종이 `#f4efe6` /
  카드 `#fffdf8` / 잉크 `#23201c` / 골드 `#b8893a` 악센트.
  **두 곳의 색이 어긋나면 안 된다**고 주석으로 못 박았다
- 폰트: 본문 고운바탕(세리프) · 제목/큰 숫자 Fraunces · 표 숫자 tabular-nums
- KPI 를 카드(골드 상단 보더)로, 탭 강조·사이드바도 같은 톤
- 차트 팔레트: **색 계열은 종전 그대로**(주=파랑→네이비, 월=주홍→벽돌…) —
  색을 기억하는 사용자가 헤매지 않게 계열은 두고 채도만 종이에 맞췄다
- ⚠ px.pie 는 색을 명시해야 한다 — 안 그러면 Streamlit 테마가 기본 파랑으로 덮어쓴다

### 화면 요청 4건 (사용자)
1. **1Y·YTD 수익률** — 종목 목록 기본 컬럼에 추가(전에는 선택 컬럼에만),
   대안 비교 '개요' 뷰에도 추가
2. **최근분배율_시장가** 칸 신설 — 직전 회차 분배금 ÷ **지금 시장가**.
   세이브로 '최근_분배율'은 **분배 당시 기준가**로 나눈 값이라 가격이 움직였으면
   둘이 벌어진다. 국내는 attach_krx_info, 미국은 add_us_etfs 에서 같은 종가로 계산
3. **테마섹터 '광범위지수' → '혼합'** — etf_taxonomy 라벨 변경 (여러 섹터가 섞인
   시장 대표지수라는 뜻)
4. **⭐ 관심 플랜 → 관심종목** + 탭 안에 **매수 전 5분 루틴 필터** —
   관심종목 목록 자체를 루틴 5항목(+커버드콜 제외)으로 거른다. `watch.plan` 이
   루틴 판정 칸을 실어 나르고 `routine.apply_filters` 를 그대로 재사용.
   거른 결과가 표·합계 KPI 에 모두 반영된다. **✅ 5분 루틴 탭(종목 하나 상세)은
   그대로 뒀다** — 필터는 거르는 도구, 탭은 근거를 읽는 도구.

**검증**: pytest **415개 통과** (신규 `test_watch.py` 루틴 칸 1개, '혼합' 이름
반영 3개 수정). 브라우저로 종목 목록 새 컬럼·관심종목 필터·색 적용 확인.

### 41단계-보완: 사용자가 잡아낸 것 셋 (같은 날)
- **아이콘 글자 겹침** — 종이 톤 CSS 가 모든 요소의 폰트를 덮어써서, Streamlit
  아이콘("arrow_drop_down" 같은 글자를 **아이콘 폰트**로 그리는 방식)이 글자
  그대로 노출돼 라벨과 겹쳤다. 아이콘 요소만 Material Symbols 로 되돌리는
  예외 규칙을 넣었다. 폰트를 전역으로 덮을 땐 아이콘 폰트를 반드시 빼야 한다
- **'광범위지수' 잔재 265건** — 분류 규칙(etf_taxonomy)만 바꾸면 **KRX 정보
  CSV 에 저장된 옛 값**이 그대로 남는다. `etf_info.load_latest()` 에서 치환 —
  과거 CSV 전부에 통하고, 갱신이 이 값을 이어받아 저장하므로 다음 수집분부터는
  파일에도 새 이름으로 남는다. 이름을 바꿀 땐 규칙과 **저장값** 둘 다 봐야 한다
- **NAV 비교표 조회 전멸** — 회사 PC 프록시의 인증서 바꿔치기로 pykrx(HTTPS)가
  죽은 것. `nav_erosion.py` 에 truststore 주입(+requirements). SSL 을 뚫자
  이번엔 **KRX 자동 로그인(KRX_ID/KRX_PW 환경변수)이 자격 증명 거절** —
  사용자가 KRX 비밀번호를 바꾸고 `.env` 를 갱신했는데 두 가지가 더 막고 있었다:
  ① NAV 경로는 `env_keys.load()` 를 안 불러 `.env` 를 아예 안 읽었다 → 조회
  직전에 로드하게 연결. ② 이 PC 윈도우 User 환경변수의 **옛 비밀번호가 `.env`
  보다 우선**이었다(env_keys 규칙) → 무효해진 옛 값을 삭제, 이제 `.env` 가
  유일한 출처. KODEX 200 실조회로 복구 확인.

### 41단계-차트: 시장국면 표시 조정 (같은 날, 사용자 요청)
- 투자자예탁금 · 예탁금/시총 두 차트에 **3·6·12개월 이동평균** 점선 추가 —
  수평선(전체 평균)은 '지금이 높나'를, 이동평균은 '최근 추세'를 보여준다.
  창이 안 쌓인 앞부분은 비운다(짧은 창으로 그럴듯한 값을 만들지 않는다).
  **시작~종료 기간 선택**(종료 기본 오늘) + plotly 레인지슬라이더.
  이동평균은 전체 이력으로 계산한 뒤 표시만 자른다(구간을 좁혀도 안 흔들리게)
- DRAM·수출↔국고채 차트는 **그래프만 2025년 이후**로 좁혔다 — 이력은 보존하고
  상관계수는 전체 구간으로 계산. **DDR5 16Gb 는 그래프에서 뺐다**(수집은 계속)

### 42단계: 수급 상세를 HTS 스타일로 — 투자자별 매도/매수 (2026-08-14)
상장주식수·거래대금 차트 대신, **주가 차트 + 투자자별 매도(◀)/매수(▶) 가로
막대**(HTS 순매매 화면 모양)로 바꿨다. 기관은 금융투자·보험·투신·사모·은행·
기타금융·연기금으로 분해된다. 기간은 시작~종료 선택(기본 최근 1개월~오늘).

- `src/investor_flow.py` 신규 — KRX [13207] ETF 투자자별 거래실적(개별종목).
  ⚠ **pykrx 공개 함수는 티커 사전이 주식만 담고 있어 ETF 를 넣으면 조용히
  0만 온다** — 내부 클래스(`ETF_투자자별거래실적_개별종목_기간합계`)에 ISIN 을
  직접 넣어야 한다. 기간별 CSV 캐시, 0뿐인 응답은 캐시하지 않는다
- 주가는 KRX Open API 일별 캐시의 종가를 쓴다(추가 네트워크 없음)
- 장기 상장주식수 흐름(34단계)은 그대로 아래에 남겼다

### ⚠ 42단계-사고: KRX 계정 잠금 (CD007)
비밀번호 변경 후 윈도우 환경변수는 지웠지만, **이미 떠 있던 셸이 옛 비밀번호를
물려받은 상태**에서 대시보드를 몇 번 재시작했다. 그 서버들이 NAV 조회 때마다
옛 비밀번호로 자동 로그인을 반복 → `CD007 패스워드 오류수에 의한 잠금`.
- 교훈: **로그인 실패가 보이면 재시도하지 말고 오류 코드부터 본다.**
  pykrx 는 요청마다 재로그인해서 잠금까지 몇 번 안 걸린다
- 조치: 투자자별 조회를 **버튼으로만** 실행(화면 리런마다 로그인하지 않게),
  이 세션에서 띄우는 프로세스는 KRX_ID/KRX_PW 를 비우고 시작
- 잠금 해제는 사용자만 할 수 있다(data.krx.co.kr 비밀번호 재설정/잠금 해제)

**결말** — 잠금 해제 뒤에도 `CD006 패스워드 불일치`가 이어졌다. "웹 로그인은
된다"가 판단을 흐렸는데, **브라우저는 저장된 비밀번호로 로그인해서** 사용자가
기억하는 값과 실제 값이 달라도 성공한다. `tools/krx_login_check.py` 를 만들어
(비밀번호를 직접 타이핑해 **1회만** 시도, 코드만 출력) 가르니 타이핑한 값도
CD006 — 재설정이 다른 값으로 저장돼 있었던 것. 재로그인(재설정) 후 CD001 정상,
수급 탭 투자자별 조회까지 화면에서 확인했다.
교훈: **"웹은 되는데"는 증거가 아니다** — 자동완성이 끼면 비밀번호 검증이 안 된다.

---

## 43단계: 전체 PDF 수집 → 진짜 겹침 v2 (2026-08-14)

40단계에서 "KRX 는 로그인 필수라 접는다"고 했던 **전체 구성종목(PDF)** 이
42단계 로그인 복구로 열렸다. 브라우저 정찰도 필요 없었다 — **pykrx 에 `PDF`
클래스(MDCSTAT05001, [13108] 화면)가 이미 있다.** 아이러니: 40단계 조사에서
같은 bld 를 손으로 두드리다 400을 맞았는데, 문제는 bld 가 아니라 로그인이었다.

### 수집 (`src/krx_pdf.py` 신규)
국내 ETF **1,160종목 전수, 실패 0** — `krx_PDF_20260813.csv` **75,894행**(6.3MB).
- 조회 일자는 KRX 정보 CSV 기준일과 맞춘다(다른 국내 데이터와 같은 날짜)
- 50종목마다 저장 + 이어받기 (compst.py 에서 배운 구조 그대로)
- **채권 ETF 도 구성 채권 목록이 통째로 온다** — 비중 칸은 주식형만 채워지고
  채권형은 평가금액만 온다 → 겹침 계산 때 평가금액 비율로 환산

### 겹침 v2 (`compare.py`)
폴백 사다리: **PDF(진짜 보유종목 min-합)** → 섹터(v1) → 분류.
비중 합 50% 미만은 실측에서 뺀다 — 선물·파생형 ETF 는 PDF 가 선물/현금이라
종목 겹침이 무의미하다(분류로 폴백되는 것이 맞는 동작).
가드 통과 **790종목**(1,160 중 — 나머지는 파생·현금성 구성).

### 실데이터 — 섹터 v1 의 과대측정이 그대로 드러났다
| 조합 | 분류 | v1(섹터) | **v2(진짜 구성)** |
|---|---|---|---|
| KODEX 200 ↔ TIGER 200 | 100 | 100 | **99.3** |
| KODEX 200 ↔ KODEX 반도체 | 52.4 | 68.5 | **49.3** |
| KODEX 200 ↔ KODEX 배당가치 | 52.4 | 51.1 | **32.4** |

v1 은 "같은 섹터의 다른 종목"을 겹침으로 세서 반도체가 68.5%까지 부풀었다.
진짜 보유종목 기준으로는 49.3%다.

**검증**: pytest **419개 통과** (신규 4개 — PDF 우선 / 없으면 섹터 폴백 /
채권형 평가금액 환산 / 3-튜플 로더). 실데이터 4쌍 대조 확인.

### 남은 것
- PDF·구성종목 수집을 주 1회 스케줄러에 붙일지 (구성은 천천히 변한다)
- 미국 상장분 구성종목 (발행사별 공시 — 별개 조사)

---

## 44단계: 구성종목 수집을 주 1회 스케줄러로 (2026-08-15, 집 PC)

43단계가 남긴 "주 1회 스케줄러에 붙일지"를 붙이는 쪽으로 결정하고 실행했다.
구성은 천천히 변하니 주 1회면 충분하고, 안 돌면 겹침이 낡은 값으로 계산될 뿐
폴백 사다리(PDF → 섹터 → 분류)가 있어 아무것도 깨지지 않는다.

### 만든 것
- `tools/collect_holdings.bat` — `krx_pdf.py`(전체 구성) → `compst.py`(TOP10 폴백).
  **PDF 가 실패해도 compst 는 돌린다** — 폴백 자료라서 따로 실패를 기록만 한다.
  로그: `data/holdings_collect.log` (gitignore)
- `tools/register_holdings_task.bat` — `ETF-holdings-weekly`, **토요일 09:00**.
  금요일 18:30 일일 갱신이 만든 KRX 정보 CSV 기준일로 조회하니 경합이 없다.
  배터리 설정 3종 + 실행 제한 3시간(pdf 15분 + compst 20분 + 여유)은
  register_etf_task.bat 패턴 그대로

### 이 PC(집) 등록 확인
| 작업 | 상태 | 다음 실행 |
|---|---|---|
| ETF-DDR5-daily | Ready | 평일 18:00 |
| ETF-refresh-daily | Ready | 평일 18:30 |
| **ETF-holdings-weekly** | **Ready** | **8/22(토) 09:00** |

### ⚠ 이 PC 는 아직 KRX 로그인이 없다 (토요일 실행 전에 채워야 함)
스모크는 "이미 받음 1,160 / 이번에 0종목"으로 통과했지만 이는 8/13 수집분을
이어받기가 건너뛴 것 — **로그인 검증이 아니다.** 확인 결과:
1. `truststore` 가 venv 에 없었다 → `pip install -r requirements.txt` 로 해결
   (어제 pull 로 requirements 가 바뀌었는데 venv 동기화를 안 했던 것)
2. **이 PC `.env` 에 `KRX_ID`/`KRX_PW` 가 없다** — 42단계에서 회사 PC 에만 넣었고
   `.env` 는 gitignore 라 안 넘어온다. **사용자가 직접 추가해야 한다.**
   추가한 뒤 `venv\Scripts\python tools\krx_login_check.py` 로 **1회만** 확인
   (반복 실패는 계정이 잠긴다 — 42단계 사고 참조)

> **다른 PC 를 새로 쓸 때 확인할 것** (30단계-앞 목록에 추가):
> `Get-ScheduledTask -TaskName "ETF-*"` 로 **세 작업**이 다 `Ready` 인지.
> 없으면 `tools\register_etf_task.bat` · `register_ddr5_task.bat` ·
> **`register_holdings_task.bat`** 를 돌린다. `.env` 에는 `KRX_OPENAPI_KEY` 외에
> **`KRX_ID`/`KRX_PW`** 도 있어야 한다(구성종목·투자자별·NAV 조회용).

### 남은 것
- **이 PC `.env` 에 KRX_ID/KRX_PW 추가** (사용자) → `krx_login_check.py` 1회 확인
- 미국 상장분 구성종목 (발행사별 공시 — 별개 조사)

---

## 44단계-취소: KDM 이용 제한 — 자동화 수집 전면 중단 (2026-08-15)

44단계를 등록한 **당일에 되돌렸다.** KRX Data Marketplace 가 자동화 대량 조회를
탐지해 **IP 를 1일 차단**했고, 안내문이 약관(제10조 제2호: 자동화 수단을 이용한
정보 무단 수집 금지)을 명시했다. 어제(8/14) 회사 PC 의 **1,160종목 PDF 전수
수집**이 탐지된 것으로 보인다.

> **정정**: 차단은 IP 단위인데 PDF 전수 수집은 회사 PC(다른 IP)였다. 집 IP 를
> 차단시킨 것은 **오늘 아침 다른 프로젝트(bottom-research)의 data.krx.co.kr
> 조회**였을 가능성이 더 크다. 어느 쪽이든 결론은 같다 — **모든 프로젝트에서
> data.krx.co.kr 자동 조회를 중단**해야 한다. 이 PC 의 bottom-research 관련
> 스케줄러(`breadth-netbuy-slot`)는 Disabled 상태인 것을 확인했다.

### 바로 한 것
- `ETF-holdings-weekly` 스케줄러 **삭제** (등록 몇 시간 만에) — 토요일에 또
  1,160건을 자동 조회하면 재탐지 → 재차단이다
- `collect_holdings.bat` · `register_holdings_task.bat` **파일 삭제** — 존재하면
  다른 PC 에서 실수로 등록한다
- **`krx_pdf.py` 는 다시 돌리지 말 것** (코드는 남긴다 — 수동 다운로드 파싱에
  재활용할 수 있다). 회사 PC 에서도 마찬가지다

### 아까 로그인 '실패'의 재해석
`krx_login_check.py` 의 "자격 증명을 확인하세요"는 **IP 차단이었을 가능성이 크다.**
`.env` 비밀번호는 맞을 수 있다. **차단 해제(1일) 전에는 어떤 확인도 하지 말 것** —
차단 중 반복 시도는 최악이다.

### 계속 되는 것 / 안 되는 것
| 무엇 | 경로 | 판정 |
|---|---|---|
| 일일 시세·수익률 (`etf_refresh.py`) | KRX **Open API** (키 발급) | ✅ 안내문이 권장한 공식 경로 |
| DDR5 현물가 | TrendForce | ✅ KRX 무관 |
| 세이브로 수집 (분배금·분배상세·구성TOP10) | 세이브로 | ✅ KRX 무관 |
| 구성종목 PDF **자동** 수집 (`krx_pdf.py`) | data.krx.co.kr 로그인 | ❌ **약관 금지 — 중단** |
| 투자자별 거래실적 (`investor_flow.py`) · NAV 조회 | data.krx.co.kr | ⚠ 화면 버튼으로 소량 조회지만 같은 자동화 수단 — 사용 자제, 대체 경로 검토 |

### 구성종목의 공식 경로 (안내문이 제시한 것)
1. **KDM 화면 다운로드** — [13108] 화면에서 사람이 받으면 된다. 구성은 천천히
   변하니 필요할 때 수동 1회면 충분하다. 받은 파일을 `krx_PDF_*.csv` 형식으로
   변환하는 파서를 만들면 겹침 v2 가 그대로 산다
2. KRX Open API — 31단계 확인 때 ETF 엔드포인트는 시세뿐이었다 (구성종목 없음
   으로 기억, 재확인 필요)
3. 데이터 상품 구입
4. 폴백은 지금도 있다: 세이브로 구성TOP10(v1) → 분류. **8/13 PDF 스냅샷은
   이미 있으니 겹침 v2 는 당분간 그대로 동작한다**

### 남은 것
- (차단 해제 후, 사용자가 원하면) KDM 화면 다운로드 → 파서로 PDF 갱신
- 투자자별·NAV 조회의 Open API 대체 가능 여부 확인
- 미국 상장분 구성종목 (발행사별 공시 — 별개 조사)

---

## 45단계: KIS Developers 로 갈아타기 — 모듈 준비 완료 (2026-08-15)

KRX 웹 조회의 대체 경로로 **증권사 API 4곳을 비교**하고 한국투자(KIS)로 정했다.

| | 한투 KIS | 키움 REST | KB | 미래에셋 |
|---|---|---|---|---|
| ETF 구성종목 | ✅ 명시 | 확인 필요 | 오픈베타(7/20~) | 개인용 API 없음 |
| 투자자별·NAV | ✅ 명시 | 있음 | 미확인 | — |
| 문서·커뮤니티 | 최고 (공식 GitHub 파이썬 예제) | 2025 신설 | 이제 시작 | — |

**한투를 고른 이유**: 잃은 세 데이터가 전부 명시적으로 있고, 공식 GitHub
(`koreainvestment/open-trading-api`)에 엔드포인트별 파이썬 예제가 있어 스펙을
추측할 필요가 없다. 사용자는 **CMA 계좌 개설로 진행하기로 결정**.

### `src/kis_api.py` 신규 — 키 없이 만들 수 있는 것을 다 만들어 뒀다
공식 GitHub 예제에서 확인한 스펙 그대로:

| 잃은 것 | KIS 엔드포인트 | TR |
|---|---|---|
| 구성종목(krx_pdf) | `inquire-component-stock-price` | FHKST121600C0 |
| NAV 시계열(nav_erosion) | `nav-comparison-daily-trend` (호출당 100건) | FHPST02440200 |
| 투자자별(investor_flow) | `inquire-investor` (장 종료 후) | FHKST01010900 |

- 토큰(유효 1일)은 `data/kis_token.json` 캐시(.gitignore) — **발급마다 알림톡이
  오는 공식 동작**이라 캐시가 있으면 재발급하지 않는다 (테스트로 못 박음)
- `rt_cd != 0` 이면 **빈 표가 아니라 예외** — krx_pdf 때 예외를 삼켜
  User-Agent 사고를 늦게 찾은 교훈. '구성종목 없음'과 '조회 실패'를 가른다
- 100건 제한은 `nav_history()` 가 140일씩 나눠 받아 합친다
- 호출 간격 0.12초 — 실전 한도(초당 20건)의 절반만 쓴다

**검증**: 신규 `test_kis_api.py` **9개** (전부 목 세션 — 키·네트워크 불필요).

### 남은 것 (키 발급 뒤)
1. **사용자**: CMA 개설 → KIS Developers 앱키 발급 → `.env` 에
   `KIS_APP_KEY=` / `KIS_APP_SECRET=` 두 줄
2. `venv\Scripts\python src\kis_api.py` 로 연결 확인 (KODEX 200 3종 조회)
3. 붙이기: 수급 탭(investor_flow) → NAV 침식(nav_erosion) → 구성종목 갱신(compare)
4. 미국 상장분 구성종목 (발행사별 공시 — 별개 조사)

---

## 45단계-보완: 키 연결 + 커버리지 실측 + 프로젝트 간 공유 (2026-08-15)

사용자가 **개인연금 계좌**로 가입해 앱키를 넣었다(조회 API 는 계좌 종류 무관).
세 엔드포인트 모두 실데이터 확인 완료.

### 실측 커버리지 — 쓸 수 있는 것과 없는 것
| 데이터 | 실측 결과 |
|---|---|
| NAV 일별 | **2012년까지 확인** — 사실상 전체 이력. 호출당 100건 |
| 투자자별 (기본) | 최근 30거래일 (개인·외국인·기관) |
| 투자자별 (세부, [0416]) | **증권·투신·기금·은행·보험·종금·사모·기타법인·등록/비등록 외국인**의 매도/매수/순매수 수량·대금. base_date 로 과거 백필 가능 |
| 구성종목 | ⚠ **국내 주식형 TOP30 만** (KODEX 200: 30행 84.5%, 30종목 이하면 100%). **채권형·해외주식형 0행**, 합성형은 원화 담보만 |

→ 구성종목은 **전체 PDF 의 대체가 아니다.** 8/13 KRX PDF 스냅샷(1,160종목 전체)이
기준이고, KIS 는 국내 주식형의 TOP30 갱신용. NAV·투자자별은 완전 대체 이상이다
(투자자별은 KRX 웹보다 세부 분류가 더 많다).

### 유의 — 호출 제한이 문서보다 빡빡했다
초당 20건이라는데 0.12초 간격에서도 "초당 거래건수 초과"가 났고, 간헐적 500도
호출 간격을 1초로 늘리니 사라졌다. **500 에러는 대부분 일시적**이니 데이터가
없다고 단정하기 전에 간격을 늘려 재시도할 것.

### 프로젝트 간 공유 (`~/.kis/`) — VS Code 창 3개가 같은 앱키를 쓴다
토큰·캐시를 프로젝트 안이 아니라 **사용자 홈**에 뒀다:
- `~/.kis/token.json` — 토큰(유효 1일). 프로젝트마다 발급하면 알림톡이 계속 오고
  6시간 뒤엔 기존 토큰이 무효화될 수 있다 → 한 파일 공유가 안전
- `~/.kis/cache/` — 하루 단위 데이터 캐시(구성종목·투자자별·투자자세부).
  같은 날 같은 종목이면 **어느 프로젝트가 불렀든** API 대신 여기서 읽는다.
  평범한 CSV 라 kis_api 를 import 안 하는 프로젝트도 그냥 읽으면 된다
- 경로 변경: `KIS_TOKEN_CACHE` / `KIS_CACHE_DIR` 환경변수
- **다른 프로젝트에서 쓰는 법**: `src/kis_api.py` 를 복사하고 `.env` 에 같은
  앱키를 넣으면 끝. 캐시·토큰이 자동으로 공유된다

**검증**: `test_kis_api.py` 9 → **13개** (세부 투자자 파싱 + 캐시 3종).
실기기 확인 — 2차 호출이 API 없이 캐시에서 0.15초에 돌아온다.

### 남은 것
- 붙이기: 수급 탭(investor_flow → kis) · NAV 침식(nav_erosion → kis) ·
  구성종목 TOP30 주기 갱신(compare)
- 미국 상장분 구성종목 (발행사별 공시 — 별개 조사)

---

## 46단계: KIS 전환 완료 + shared-data 규약 채택 (2026-08-15)

### shared-data 규약 채택 (다른 세션이 만든 것)
peak 세션이 `D:\MJLEE\vibe_inv\shared-data\` 스테이징을 만들었다
(규약: peak `HANDOVER.md` §7, `shared-data/README.md`). 이 프로젝트도 채택:
- **토큰을 그리로 옮겼다** — 내가 몇 시간 전 만든 `~/.kis/` 방식을 버리고
  `shared-data/kis_token.json` ({access_token, expires_at}) 하나를 쓴다.
  **같은 앱키로 재발급하면 기존 토큰이 즉시 무효화**되고 발급도 분당 1회라,
  세션 셋(peak·bottom-research·이곳)이 따로 발급하면 서로를 죽인다.
  재발급은 `kis_token.lock` (O_CREAT|O_EXCL, 90초 회수, 재확인)을 잡고 한다 —
  peak `verify/marketdata.py` 의 `kis_token()` 과 같은 동작, 테스트 3개로 고정
- 종목별 하루 캐시는 스테이징 명명 규칙(`<원천>_<시리즈>_<주기>.parquet`)에
  안 맞는 낱개 파일이라 **저장소 안 `data/kis_cache/`** (gitignore)로 내렸다

### ① 수급 탭 — investor_flow 를 KIS 로 다시 썼다
- KRX [13207](pykrx 로그인) → **한투 [0416]** (`kis_api.investor_detail`)
- 세부 분류가 오히려 늘었다: 금융투자·보험·투신·사모·은행·종금·기금·기타법인
- 30거래일/호출이라 base_date 를 뒤로 밀며 백필 (`_page_span`, 상한 14페이지)
- **실측 검증이 세게 맞았다**: KODEX 200 7/1~8/14 기간합계에서
  **전체 순매수 합 = 정확히 0주** — 판 주식과 산 주식이 일치하는 회계 항등식
- 잡은 함정: KIS 필드 접미사가 그룹마다 다르다 — 순매수 수량이 대부분
  `_ntby_qty` 인데 **사모(pe_fund)·기타법인(etc_corp)은 `_ntby_vol`** 이다.
  처음에 둘이 NaN 으로 새서 실측으로 찾았다
- 화면: 버튼 '(KRX 로그인)' → '(한투 API)', ISIN 대신 종목코드로 조회

### ② NAV 침식 — nav_erosion 의 기본 fetcher 를 KIS 로
`fetch_nav_history` 에 이미 fetcher 주입 자리가 있어 교체가 한 줄이었다.
pykrx `get_etf_price_deviation` → `kis_api.nav_history` (100건씩 자동 분할).
실측: KODEX 200 1년치 253행이 페이지 나눔 없이 이어져 온다. 검증 로직
(`_validate_nav_frame`)과 기간별 CSV 캐시는 그대로다.

### ③ 구성종목 — KIS TOP30 층을 겹침 사다리에 끼웠다
- `src/kis_top30.py` 신규 — 한국 상장 전 종목의 TOP30 을
  `data/raw/kis_TOP30_YYYYMMDD.csv` 로. 하루 캐시 덕에 재실행이 공짜다
- 사다리가 한 층 늘었다: **PDF(8/13 전체, 갱신 불가) → KIS30(상위 30, 갱신
  가능) → 섹터(세이브로) → 분류**. 층을 섞지 않는다 — PDF 는 구성종목ISIN,
  KIS30 은 단축코드가 키라 섞으면 겹침이 0 으로 왜곡된다 (테스트로 고정)
- 같은 가드(비중합 50% 미만 탈락) — 합성형 담보 몇 줄을 실측이라 부르지 않는다

#### ⭐ 비중을 100% 로 정규화해야 했다 (실측으로 잡음)
붙이고 나서 PDF 와 대조해 보니 **KIS30 이 겹침을 체계적으로 낮게** 냈다.
TOP30 이 비중의 84% 만 덮으니 min-합의 천장이 84 라서, 같은 지수를 따르는
`KODEX 200 ↔ TIGER 200` 이 PDF 로는 99.3 인데 KIS30 으로는 **84.4** 였다.
한 표에서 층이 섞이면 사용자가 이 차이를 읽을 수 없다.

→ 각 종목의 TOP30 비중을 100 으로 정규화했다. PDF 가 있는 5쌍 대조:

| 조합 | PDF | 원본 | **정규화** |
|---|---|---|---|
| KODEX 200 ↔ TIGER 200 | 99.3 | 84.4 | **99.9** |
| KODEX 200 ↔ KODEX 배당가치 | 32.4 | 27.4 | **32.4** |
| KODEX 반도체 ↔ TIGER 반도체TOP10 | 65.6 | 65.8 | **66.2** |

평균 오차 **4.1%p → 2.3%p**. 남은 비중이 상위 30개와 비슷하게 퍼져 있다는
가정이라 근사치이고, 그 사실을 함수 문서에 적었다.

#### 수집 결과와 지금의 이득
`kis_TOP30_20260815.csv` — **593종목 10,790행**. 1,194종목 중 569개는 빈 응답
(채권형·해외주식형 — API 가 국내 주식형만 준다), 실패 32건(2.7%).

1차는 0.12초 간격에서 **실패가 184건(20%)** 이나 났다. 0.5초로 늘리자 32건으로
줄었다 — 문서의 '초당 20건'보다 실제 제한이 빡빡하다(45단계-보완에서 본 것과
같은 현상). 하루 캐시 덕에 재실행이 성공분을 건너뛰어 두 번 돌리면 수렴한다.

층별: PDF 790 / **KIS30 590** / 섹터 432. **KIS30 이 새로 덮는 것은 17종목**
으로 지금 당장의 이득은 작다 — 이 층의 값어치는 **PDF 가 낡아갈수록** 커진다
(PDF 는 8/13 에 멈췄고 다시 못 받는다).

정규화 뒤 실측 일치도(PDF 가 있는 쌍): `KODEX 200 ↔ TIGER 200` 99.9(PDF 99.3),
`KODEX 반도체 ↔ TIGER 반도체TOP10` 66.2(PDF 65.6).

**검증**: `test_kis_api.py` 15→18개(사다리 3개), 신규 `test_investor_flow.py`
6개(백필·경계 중복·조기 종료). 관련 스위트 54개 통과.

### 실행 메모
- `venv\Scripts\python src\kis_top30.py` — **두 번 돌린다**(1차 실패분이
  2차에서 채워진다). 급하면 `kis_api.PAUSE` 를 0.5 로 올려서 한 번에
- 다른 KIS 스크립트도 같다: 500 은 대부분 일시적이니 간격을 늘려 재시도

---

## 47단계: KDM 재차단 방지 가드 + 미국 구성종목 보류 (2026-08-15)

### ⚠ pykrx 진입점이 네 군데 살아 있었다
KIS 로 갈아탄 뒤에도 `data.krx.co.kr(KDM)` 을 치는 함수가 코드에 남아 있었다.
**한 번만 실수로 불러도 차단이 재발한다**(8/15 에 실제로 1일 차단당했다).

| 함수 | 무엇 | 조치 |
|---|---|---|
| `etf_info.fetch_basic` | [13104] 전종목 기본정보 | 가드 |
| `etf_info.fetch_marketcap` | [13101] 전종목 시세 | 가드 |
| `etf_returns.fetch_price_change` | [13103] 등락률 | 가드 |
| `krx_pdf.fetch_one` | [13108] 구성종목 — **차단을 부른 그것** | 가드 |

`ETF_ALLOW_KDM=1` 이 없으면 예외를 던진다(오류 문구가 대체 경로를 알려준다).
코드는 지우지 않았다 — 수동 다운로드 파싱에 재활용할 수 있다.

**확인한 것**: 매일 18:30 스케줄러(`etf_refresh.py`)는 **KRX Open API**
(openapi.krx.co.kr, 공식 승인 경로)만 쓴다. `etf_info`/`etf_returns` 에서는
CSV 읽기·날짜 계산만 가져다 쓴다 → 스케줄러는 원래 안전했다. 그 사실을
소스 검사 테스트로 못 박았다(`etf_refresh.py` 에 KDM 함수 호출이 없을 것).

**검증**: 신규 `test_kdm_guard.py` **8개**. 기존 테스트 2개가 가드에 걸려
`ETF_ALLOW_KDM=1` 을 주게 고쳤다(파싱을 보는 테스트라 실제 조회는 가짜).

### KIS `PAUSE` 0.12 → 0.3
전 종목 수집 실측 근거: 0.12초에서 실패 184/1,194(20%), 0.5초에서 32건(2.7%).
문서상 초당 20건(0.05초)보다 실제 제한이 빡빡하다. 낱개 조회 기본을 0.3 으로
올리고, 전수 수집은 호출부에서 더 올린다.

### 미국 상장 구성종목 — **지금은 안 만든다** (범위를 먼저 쟀다)
발행사별 공시를 뒤지기 전에 KIS 부터 봤다: **해외 ETF 구성종목 API 가 없다**
(`examples_llm/overseas_stock` 에 시세·주문뿐). 그래서 2,100종목을 발행사
사이트에서 받아야 하는데, **쓸 자리가 있는지 먼저 셌다**:

| 어디 | 미국 상장 종목 수 |
|---|---|
| 내 보유종목 | **0** |
| 관심종목 | **0** |
| 화이트리스트 | **0** |

겹침은 **후보 ↔ 내 보유**를 재는 값이라, 미국 구성종목을 다 모아도 지금은
쓰이지 않는다(내 보유가 전부 국내라 어느 쌍이든 한쪽이 비어 분류로 폴백된다).
게다가 연금·IRP·ISA 로는 미국 직접 상장을 못 산다 — 살 수 있는 계좌는
종합주식 하나다.

→ **미국 ETF 를 실제로 담거나 관심종목에 넣은 뒤**에 그 발행사만 붙이는 것이
맞다. 그때 순서: 발행사 `robots.txt` 확인(20단계 원칙) → 보유 종목의 발행사만.
미국 발행사 상위: iShares 344 · Invesco 142 · SPDR 114 · Vanguard 102 · Direxion 100.

### 남은 것
- 미국 상장 ETF 를 실제로 담게 되면 그 발행사 구성종목 (지금은 불필요)
- (선택) `truststore` 는 아직 `compst.py`·`krx_pdf.py`·`nav_erosion.py` 에 남아
  있다 — 회사 PC 프록시 대응이라 KIS 경로에서도 무해해서 그대로 뒀다

---

## 48단계: 배수(레버리지·인버스) 칸 + 대안 비교 정렬 기준 (2026-08-15)

### ⭐ 배수는 이름이 아니라 **데이터**로 낸다
6단계가 "레버리지·인버스는 펀드 구조라 기초지수명에 안 나온다"고 접었던 것을
다시 봤다. 실제로 대조해 보니 `KODEX 레버리지` 와 `KODEX 200` 이
**자산분류·시장분류·운용방식·기초지수명까지 전부 동일**했다(둘 다 '코스피 200').

찾은 근거: 한투 [0245] ETF 현재가의 **`etf_trc_ert_mltp`(추적수익률 배수)**.
실측 — 레버리지 **2.00** / 인버스 **-1.00** / 일반 **1.00**.

다른 경로를 먼저 재 봤고 전부 못 쓴다:
| 소스 | 결과 |
|---|---|
| 네이버 금융 | `robots.txt` 가 `Disallow: /` — 개별 종목 페이지 금지(`/sise/`·`/fund/`만 허용) |
| 야후(yfinance) | 국내 ETF 는 **category 조차 없다**(이름뿐). 미국은 `Trading--Leveraged Equity` 처럼 **방향만**, 배수(2X·3X) 표기 **0건** |
| 종목명 파싱 | 즉시 되지만 CLAUDE.md 금지 — 1단계에서 이름의 '위클리'가 실제로는 월 1회였던 함정을 이미 겪었다 |

### 수집 결과 (`src/kis_multiple.py` 신규)
`kis_배수_20260815.csv` — **1,105종목**(1,194 중, 실패 89). 목록에 붙은 뒤:

| 배수 | 종목 수 |
|---|---|
| 2배 | 56 |
| -1배 | 26 |
| -2배 | 9 |
| 1배 | 983 |

**1배가 아닌 것 96개**. 화면에서 1배는 **일부러 비운다** — 거의 다 1배라 전부
적으면 레버리지·인버스가 눈에 안 들어온다.

#### ⚠ 배수 0 은 '0배'가 아니라 **미제공**이었다
31종목이 0 으로 왔는데 그 안에 **`SOL 미국테크TOP10인버스(합성)`** 가 있었다 —
명백한 인버스다. 0 을 값으로 쓰면 인버스가 '0배'가 되고, 1 로 메우면 '일반'이
된다. 둘 다 틀리다 → **0 이면 칸을 아예 안 만든다**(31종목 전부 `대표지수` 도
비어 있었다 — 메타 자체가 없는 종목들이다).

#### 곁가지 — ISIN 으로 이어야 했다
목록의 `종목코드` 는 만기·상폐 **234종목이 비어 있다**(수집기는 ISIN 가운데
6자리로 채워 조회했다). 종목코드만으로 이으면 그 234개가 통째로 안 붙는다
→ `attach_multiple` 이 **ISIN 우선, 없으면 종목코드**로 잇는다.

### ⚖ 대안 비교 — '무엇부터 올릴까' 기준 신설
사용자 질문("대안 비교에 나오는 ETF 기준이 뭐냐")에서 드러난 것: **기준이
없었다.** 사이드바 필터 결과를 정렬 없이 `head(300)` 한 것이라, 올라오는 300개가
`분배ETF목록.xlsx` 저장 순서(주기→가나다) 앞부분이었다 — **5단계에서 NAV
비교표에 있던 것과 똑같은 함정**을 이 탭이 그대로 갖고 있었다.

기준 7개를 `st.pills` 로 고르게 했다(기본 **겹침 낮은 순**):
겹침 낮은 순 · 루틴 점수 높은 순 · 시총 큰 순 · 연분배율 · 평상분배율 ·
총보수 낮은 순 · 1년 수익.

- 기본이 '겹침 낮은 순'인 이유: 이 탭의 본래 목적이 **이미 가진 것과 안 겹치는
  것 찾기**다(33단계). 보유종목이 없으면 안내를 띄운다
- **겹침 기준일 때는 자르기 전에 전체를 계산**한다 — 300개만 재고 정렬하면
  그 300개가 이미 엉뚱한 앞부분이다. 다른 기준은 잘라낸 뒤 계산해 가볍게 둔다
- 값 없는 종목은 항상 뒤로(`na_position="last"`) — 오름차순에서 결측이 1등이면
  '총보수 제일 싼 것'에 자료 없는 종목이 온다

**검증**: pytest **463개 통과** (신규 `test_multiple.py` 11개 — 0 이 미제공인 것,
ISIN 폴백, 1배 비우기, 정렬 기준·결측 뒤로).

### 남은 것
- 배수 실패 89종목은 재실행하면 캐시가 성공분을 건너뛰어 수렴한다
- 미국 상장분 배수는 여전히 빈칸 (야후가 방향만 준다) — `전략` 칸으로 본다

---

## 49단계: KPI 차트를 눌러 종목 펼치기 + 0배 잔재 정리 (2026-08-15)

### 차트 클릭 → 아래에 목록
`분배주기별 종목 수` 막대와 `국내/해외` 도넛을 `on_select="rerun"` 으로 바꿔,
칸을 누르면 그 종목들이 **차트 바로 아래**에 시총 큰 순으로 펼쳐진다.
둘을 같이 고르면 **겹치는 것만** 남는다(예: 주 + 국내).

- **사이드바 필터를 대신하지 않는다** — 차트가 이미 필터 결과를 그린 것이라
  여기서 또 좁히면 두 번 거르는 셈이다. 화면 아래 목록만 보여 준다
- 차트는 짧은 이름(`주`)으로 그리는데 데이터 값은 `위클리` 다 →
  `CYCLE_LABEL` 을 뒤집어 되돌려야 걸러진다(안 하면 0개가 나온다)

#### ⚠ 선택 이벤트가 dict 가 아니다
Streamlit 이 주는 것은 **`AttributeDictionary`** 라 `isinstance(event, dict)` 로
거르면 **항상 빈 값**이 된다 — 눌러도 아무 일이 없는 조용한 고장이다.
처음에 그렇게 짰다가 실제 이벤트 모양을 찍어 보고 발견했다.
`_dig()` 가 `[]` 먼저, 안 되면 `getattr` 로 읽는다.
막대는 `x`, 파이 조각은 `label` 에 이름이 들어 있어 둘 다 본다.

**검증**: AppTest 로 클릭을 흉내 내 `주` 막대 → **68행**이 펼쳐지는 것 확인
(차트에 적힌 68 과 일치). 신규 `test_chart_drilldown.py` 8개 —
dict 아닌 이벤트·막대/파이 양쪽·중복 제거·'주'→'위클리' 되돌리기.

### 0배 31종목이 화면까지 새어 나갔다
48단계에서 수집 시점에 0 을 걸렀는데, **CSV 는 그 처리 전에 저장된 것**이라
0 이 그대로 남아 있었고 화면에 `0배` 로 떴다. 읽는 쪽(`load_latest`)에서도
지우게 했다 — 수집·적재 **양쪽에 가드**를 둔다.
결과: 배수 채움 1,105 → **1,074종목**, 1배 아닌 것 96 → **65개**.

### 곁가지 — 화면의 `None` 은 코드가 아니라 캐시였다
표의 배수 칸에 `None` 이 떠서 버그로 의심했는데, 돌고 있던 Streamlit 서버가
**배수 칸이 없던 시절의 xlsx 를 `@st.cache_data` 로 들고 있던 것**이었다.
서버를 재시작하니 사라졌다. → **산출물을 다시 만들면 서버도 재시작할 것.**

**검증**: pytest **472개 통과**.

### 남은 것
- 브라우저 렌더러가 첫 로딩(ICS 19,709건 + 2,494행 + plotly)에서 멈추는 일이
  있었다. 서버는 HTTP 200 이고 AppTest 도 통과 — 화면 쪽 문제로 보이나
  재현되면 ICS 파싱을 더 줄일지 볼 것
